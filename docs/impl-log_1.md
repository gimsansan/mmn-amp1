# 구현 · 결과 로그 (이어서)

> **정본**: 이 파일. 새 항목은 **위에 추가**(최신이 위).  
> **이전**: [`impl-log.md`](./impl-log.md) — 2026-08-19 01:04까지. 거기에는 새 항목을 넣지 않음.  
> **목적**: 기능 구현·설정 변경 후 **무엇을 했는지 / 결과가 어땠는지**를 날짜별로 남긴다.  
> **대상**: 코드·네이티브·오디오·훈련 로직 등 실질 변경. 대화만으로 끝난 합의·설계는 `amp-mdt-training-design.md` 쪽.  
> **규칙**: `.cursor/rules/android-dev-client.mdc`

---

## 기록 원칙 (확실 vs 단정 금지)

한 항목에 **두 축을 같이** 남긴다. 한쪽만 적고 끝내면 안 된다.

| 축 | 남길 것 | 예 |
|----|---------|-----|
| **확실한 쪽** | 근거(확인한 사실·파일), 결정, 구현 결과 | “`package.json`에 `^0.13.2` 확인”, “② MVP 순음 재생 성공” |
| **단정 금지 쪽** | 추정·가설·미검증·주의·성능 우려 | “실생활 청취 개선은 미검증”, “이 스텝 크기는 파일럿 추정” |

**표기 접두(설계 문서와 동일 취지)**  
`관례` · `가설` · `미검증` · `추정` · `주의` — 확신이 약한 문장 앞에 붙이거나, 아래 표의 **단정 금지** 칸에 모은다.  
없으면 `단정 금지 | 없음`이라고 명시한다(빈칸으로 두지 않음).

---

## 기록 형식

새 항목은 **위에 추가**(최신이 위). 복사용:

```md
### YYYY-MM-DD — 짧은 제목

| 항목 | 내용 |
|------|------|
| 트랙 | ② 주파수 / ① AM / 공통·인프라 / 문서 |
| 근거·결정 | 확인한 사실·합의·왜 이렇게 했는지 (확실한 쪽) |
| 변경 요약 | 한두 문장 |
| 주요 경로 | `src/...`, `docs/...` |
| 결과 | 성공 / 부분 / 실패 · 기기·명령 있으면 적기 |
| 확인 | 수동·doctor·테스트 등 |
| 단정 금지 | 추정·가설·미검증·주의 (없으면 `없음`) |
| 성능·주의 | 부정적 영향 있으면 명시, 없으면 `없음` |
| 다음 | 이어서 할 일(있으면) |
```

---

## 로그

### 2026-08-19 01:17 — 경량화 = 중사양 기준 · 측정만 가볍게

| 항목 | 내용 |
|------|------|
| 트랙 | 문서 |
| 근거·결정 | 사용자: 중사양 기준, 저사양 전용 불필요. 측정 화면은 듣는 중 애니메이션 금지(배터리·발열·오디오). 결과·연출은 중사양에 맞춰 사용. |
| 변경 요약 | 규칙·README·`dev-client-setup-context` §5를 그 의견으로 짧게 맞춤. 앱 코드 변경 없음. |
| 주요 경로 | `.cursor/rules/android-dev-client.mdc`, `README.md`, `docs/dev-client-setup-context.md` |
| 결과 | 문서 반영. |
| 확인 | 세 파일 문구. |
| 단정 금지 | `없음` |
| 성능·주의 | 없음(문서). **dev client 리빌드 불필요.** |
| 다음 | 없음 |

### 2026-08-19 01:09 — 인계에서 합의·안 한 일·다음 생략

| 항목 | 내용 |
|------|------|
| 트랙 | 문서 |
| 근거·결정 | 사용자: 앞으로 인계의 `### 합의` / `### 안 한 일` / `### 다음`은 기록하지 말 것. |
| 변경 요약 | 규칙 포함 항목을 한 일·핵심 경로·단정 금지로. `handoff2.md` 안내 추가. 과거 블록은 그대로. |
| 주요 경로 | `.cursor/rules/android-dev-client.mdc`, `docs/handoff2.md` |
| 결과 | 이후 인계부터 세 절 없음. |
| 확인 | 규칙·파일 상단 문구. |
| 단정 금지 | `없음` |
| 성능·주의 | 없음(문서). **dev client 리빌드 불필요.** |
| 다음 | 없음 |

### 2026-08-19 01:06 — impl-log를 `_1`로 이어감

| 항목 | 내용 |
|------|------|
| 트랙 | 문서 |
| 근거·결정 | 사용자: `impl-log.md` 기록을 `impl-log_1.md`로 생성해 이어가라. `handoff` → `handoff2`와 같은 방식. |
| 변경 요약 | `docs/impl-log_1.md` 신설(이후 정본). `impl-log.md`는 과거(01:04까지). 규칙·문서 지도 링크를 `_1`로. |
| 주요 경로 | `docs/impl-log_1.md`, `docs/impl-log.md`, `.cursor/rules/android-dev-client.mdc`, `docs/README.md` |
| 결과 | 이후 항목은 `_1`에만 추가. |
| 확인 | 파일 생성·규칙 문구 확인. |
| 단정 금지 | `없음` |
| 성능·주의 | 없음(문서). **dev client 리빌드 불필요.** |
| 다음 | 이후 구현 로그는 `docs/impl-log_1.md`만. |
