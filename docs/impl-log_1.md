# 구현 · 결과 로그 (이어서)

> **정본**: 이 파일. 새 항목은 **위에 추가**(최신이 위).  
> **이전**: [`impl-log.md`](./impl-log.md) — 2026-08-19 01:04까지. 거기에는 새 항목을 넣지 않음.  
> **목적**: 기능 구현·설정 변경 후 **무엇을 했는지 / 결과가 어땠는지**를 날짜별로 남긴다.  
> **대상**: 코드·네이티브·오디오·훈련 로직 등 실질 변경. 대화만으로 끝난 합의·설계는 `amp-mdt-training-design.md` 쪽.  
> **규칙**: `.cursor/rules/android-dev-client.mdc`

---

## 기록 원칙 (확실 vs 단정 금지)

한 항목에 **두 축을 같이** 남긴다. 한쪽만 적고 끝내면 안 된다.

| 축 | 남길 것 | 예 |
|----|---------|-----|
| **확실한 쪽** | 근거(확인한 사실·파일), 결정, 구현 결과 | “`package.json`에 `^0.13.2` 확인”, “② MVP 순음 재생 성공” |
| **단정 금지 쪽** | 추정·가설·미검증·주의·성능 우려 | “실생활 청취 개선은 미검증”, “이 스텝 크기는 파일럿 추정” |

**표기 접두(설계 문서와 동일 취지)**  
`관례` · `가설` · `미검증` · `추정` · `주의` — 확신이 약한 문장 앞에 붙이거나, 아래 표의 **단정 금지** 칸에 모은다.  
없으면 `단정 금지 | 없음`이라고 명시한다(빈칸으로 두지 않음).

---

## 기록 형식

새 항목은 **위에 추가**(최신이 위). 복사용:

```md
### YYYY-MM-DD — 짧은 제목

| 항목 | 내용 |
|------|------|
| 트랙 | ② 주파수 / ① AM / 공통·인프라 / 문서 |
| 근거·결정 | 확인한 사실·합의·왜 이렇게 했는지 (확실한 쪽) |
| 변경 요약 | 한두 문장 |
| 주요 경로 | `src/...`, `docs/...` |
| 결과 | 성공 / 부분 / 실패 · 기기·명령 있으면 적기 |
| 확인 | 수동·doctor·테스트 등 |
| 단정 금지 | 추정·가설·미검증·주의 (없으면 `없음`) |
| 성능·주의 | 부정적 영향 있으면 명시, 없으면 `없음` |
| 다음 | 이어서 할 일(있으면) |
```

---

## 로그

### 2026-08-19 15:11 — 빙고 진입 아이콘을 줄 완성으로

| 항목 | 내용 |
|------|------|
| 트랙 | 단어인지도 |
| 근거·결정 | 사용자: `grid`(칸 배열) 대신 컨텐츠 성격. 1순위 = 3×3 중 한 줄이 이어진 그림. |
| 변경 요약 | `grid` 제거. `bingoLine` 추가(가운데 가로 채움, 나머지 흐린 선). 헤더 버튼이 이 이름 사용. |
| 주요 경로 | `src/components/ui/icon.tsx`, `src/components/ui/bingo-entry-button.tsx` |
| 결과 | 코드 수정. |
| 확인 | 파일 확인. |
| 단정 금지 | `미검증`: 실기기 28px 가독성. |
| 성능·주의 | SVG 경로만. **dev client 리빌드 불필요.** |
| 다음 | 없음 |

### 2026-08-19 14:59 — 빙고 판 바둑격자·0.9 축소

| 항목 | 내용 |
|------|------|
| 트랙 | 단어인지도 |
| 근거·결정 | 사용자: 3×3을 보더로 바둑판 느낌. `paddingBottom: BottomTabInset` 유지. 판만 0.9. |
| 변경 요약 | 칸 간격·둥근 모서리·그림자 제거. 외곽 2px + 내부 격자. 너비 90%. |
| 주요 경로 | `src/training/wrs/WrsBingoScreen.tsx` |
| 결과 | 코드 수정. |
| 확인 | 파일 확인. |
| 단정 금지 | `미검증`: 실기기 격자·글자 크기. |
| 성능·주의 | StyleSheet만. **dev client 리빌드 불필요.** |
| 다음 | 없음 |

### 2026-08-19 14:49 — 빙고 하단을 BottomTabInset만

| 항목 | 내용 |
|------|------|
| 트랙 | 단어인지도 |
| 근거·결정 | 사용자: `- Spacing.four`(안드 56) 대신 다른 화면처럼 더하기의 최소인 `BottomTabInset`(80)로 한번 보기. |
| 변경 요약 | `paddingBottom: BottomTabInset`. 빼기 없음. 이전 실측보다 버튼이 위로. |
| 주요 경로 | `src/training/wrs/WrsBingoScreen.tsx` |
| 결과 | 코드 수정. |
| 확인 | 파일 확인. |
| 단정 금지 | `미검증`: 실기기에서 탭과의 간격. |
| 성능·주의 | 여백만. **dev client 리빌드 불필요.** |
| 다음 | 없음 |

### 2026-08-19 14:44 — 빙고 버튼을 탭 쪽으로 조금

| 항목 | 내용 |
|------|------|
| 트랙 | 단어인지도 |
| 근거·결정 | 사용자: 「다시 하기」+하단 3버튼을 탭 방향으로 약간. |
| 변경 요약 | `paddingBottom`을 `BottomTabInset + Spacing.two` → `+ Spacing.one`. |
| 주요 경로 | `src/training/wrs/WrsBingoScreen.tsx` |
| 결과 | 코드 수정. |
| 확인 | 파일 확인. |
| 단정 금지 | `미검증`: 실기기 탭 겹침. |
| 성능·주의 | 여백만. **dev client 리빌드 불필요.** |
| 다음 | 없음 |

### 2026-08-19 14:42 — 빙고 「다시 하기」를 흰 면으로

| 항목 | 내용 |
|------|------|
| 트랙 | 단어인지도 |
| 근거·결정 | 사용자: 맞힌 칸(accent)과 「다시 하기」(primary)가 나란히라 가독성 불리. |
| 변경 요약 | 요약 「다시 하기」를 secondary(흰 면). 칸 색은 그대로. |
| 주요 경로 | `src/training/wrs/WrsBingoScreen.tsx` |
| 결과 | 코드 수정. |
| 확인 | 파일 확인. |
| 단정 금지 | `미검증`: 실기기 대비. |
| 성능·주의 | 버튼 variant만. **dev client 리빌드 불필요.** |
| 다음 | 없음 |

### 2026-08-19 14:39 — 빙고 요약에 다시 하기

| 항목 | 내용 |
|------|------|
| 트랙 | 단어인지도 |
| 근거·결정 | 사용자: 줄 완성 후 바로 요약이라 같은 난이도 재시작이 없음. 판과 하단 한 줄 사이에 「다시 하기」. 방금 난이도·새 9칸. |
| 변경 요약 | `lastDifficulty` 기억. 요약에 primary 「다시 하기」. 하단 세 버튼은 secondary. idle은 그대로. |
| 주요 경로 | `src/training/wrs/WrsBingoScreen.tsx` |
| 결과 | 코드 수정. |
| 확인 | ReadLints 0. |
| 단정 금지 | `미검증`: 실기기에서 두 줄 버튼·탭 여백. |
| 성능·주의 | 레이아웃만. **dev client 리빌드 불필요.** |
| 다음 | 없음 |

### 2026-08-19 14:32 — 빙고 한 줄이면 바로 요약

| 항목 | 내용 |
|------|------|
| 트랙 | 단어인지도 |
| 근거·결정 | 사용자: 마지막 정답으로 줄이 끝났는데 「다음」은 다음 단어처럼 들림. 요약 판에 이미 줄 색이 있음. |
| 변경 요약 | 줄이 완성되면 피드백 없이 `finishRun`. 「맞았어요」+「다음」 생략. |
| 주요 경로 | `src/training/wrs/WrsBingoScreen.tsx` |
| 결과 | 코드 수정. |
| 확인 | 파일 확인. |
| 단정 금지 | `미검증`: 실기기에서 마지막 칸→요약 전환 체감. |
| 성능·주의 | 화면 전환만. **dev client 리빌드 불필요.** |
| 다음 | 없음 |

### 2026-08-19 14:29 — 빙고 칸 색 변수 TS 오류

| 항목 | 내용 |
|------|------|
| 트랙 | 단어인지도 |
| 근거·결정 | `Colors.light`가 `as const`라 `let backgroundColor = theme.surface`가 `"#FFFFFF"`로 좁혀져 accent 대입 시 TS 오류 5개. |
| 변경 요약 | `BingoCell` 색 변수를 `string`으로 명시. |
| 주요 경로 | `src/training/wrs/WrsBingoScreen.tsx` |
| 결과 | 해당 파일 린트 0. |
| 확인 | ReadLints. |
| 단정 금지 | 없음 |
| 성능·주의 | 타입만. **dev client 리빌드 불필요.** |
| 다음 | 없음 |

### 2026-08-19 14:26 — 빙고 동시 두 줄 문구·TTS 오류 잔류

| 항목 | 내용 |
|------|------|
| 트랙 | 단어인지도 |
| 근거·결정 | 한 칸으로 두 줄이 나도 `findBingoLine`이 첫 줄만 반환하고 문구가 「한 줄」고정. TTS `onError`의 `lastError`가 맞힌 뒤·요약까지 남음. |
| 변경 요약 | 완성 줄을 모두 세어 「두 줄이 이어졌어요」·모든 줄 칸 accent. 맞히거나 요약이면 `lastError` 제거. 요약 화면에는 TTS 문구 안 냄. |
| 주요 경로 | `src/training/wrs/wrsBingo.ts`, `src/training/wrs/WrsBingoScreen.tsx`, `src/training/wrs/__tests__/wrsBingo.test.ts` |
| 결과 | 테스트 7 passed. |
| 확인 | `npx jest src/training/wrs/__tests__/wrsBingo.test.ts` |
| 단정 금지 | `미검증`: 실기기에서 두 줄 색·문구. TTS `onError` 원인 자체는 이번 수정 범위 아님. |
| 성능·주의 | 로직·문구만. **dev client 리빌드 불필요.** |
| 다음 | 없음 |

### 2026-08-19 14:19 — 빙고 하단 버튼 한 줄·탭에 가깝게

| 항목 | 내용 |
|------|------|
| 트랙 | 단어인지도 |
| 근거·결정 | 사용자: 세 버튼을 하단 탭 근처로 옮겨 본문(판) 공간 확보. |
| 변경 요약 | idle·요약 버튼을 가로 한 줄(`fill`). 아래 여백 `Spacing.two`. 라벨 「비슷한 소리」는 폭 때문에 줄임. |
| 주요 경로 | `src/training/wrs/WrsBingoScreen.tsx` |
| 결과 | 코드 수정. |
| 확인 | 파일 확인. |
| 단정 금지 | `미검증`: 실기기에서 세 버튼 글자 잘림·탭과 겹침. |
| 성능·주의 | 레이아웃만. **dev client 리빌드 불필요.** |
| 다음 | 없음 |

### 2026-08-19 14:14 — 빙고 한 줄 색을 진행 중에도

| 항목 | 내용 |
|------|------|
| 트랙 | 단어인지도 |
| 근거·결정 | 사용자: 499–510 원인 수정. 진행 중 `line={null}`이라 한 줄이 연한 칠만 되고, 끝 화면에서만 진한 색. |
| 변경 요약 | 한 줄이면 바로 요약 가지 않고 피드백에서 `line` 표시. 진행 판에 `line` 전달. 다음에서 요약. |
| 주요 경로 | `src/training/wrs/WrsBingoScreen.tsx` |
| 결과 | 코드 수정. |
| 확인 | 파일 확인. |
| 단정 금지 | `미검증`: 실기기에서 세 칸 진한 파랑. |
| 성능·주의 | StyleSheet만. **dev client 리빌드 불필요.** |
| 다음 | 없음 |

### 2026-08-19 14:11 — 빙고 요약은 제목만

| 항목 | 내용 |
|------|------|
| 트랙 | 단어인지도 |
| 근거·결정 | 사용자: 끝나고 「한 줄이 이어졌어요」가 제목+카드로 중복. 제목만 두기. |
| 변경 요약 | 요약 카드(`N칸을 칠하고…`) 제거. 제목 `bingoResultCopy` + 판만. |
| 주요 경로 | `src/training/wrs/WrsBingoScreen.tsx` |
| 결과 | 코드 삭제. |
| 확인 | 파일 확인. |
| 단정 금지 | 없음 |
| 성능·주의 | UI만. **dev client 리빌드 불필요.** |
| 다음 | 없음 |

### 2026-08-19 14:07 — 빙고 요약 각주 문구 삭제

| 항목 | 내용 |
|------|------|
| 트랙 | 단어인지도 |
| 근거·결정 | 사용자: `WrsBingoScreen` 요약 카드 각주(314–321) 삭제. |
| 변경 요약 | 「연습이에요… 맞히기 기록에는 안 남아요」와 `footnote` 스타일 제거. 결과 한 줄은 유지. |
| 주요 경로 | `src/training/wrs/WrsBingoScreen.tsx` |
| 결과 | 코드 삭제. |
| 확인 | 파일 확인. |
| 단정 금지 | 없음 |
| 성능·주의 | UI 문구만. **dev client 리빌드 불필요.** |
| 다음 | 없음 |

### 2026-08-19 13:59 — 빙고 재생 단어 __DEV__ 로그

| 항목 | 내용 |
|------|------|
| 트랙 | 단어인지도 |
| 근거·결정 | 사용자: `WrsBingoScreen`에 재생된 소리 알려주는 로그. |
| 변경 요약 | `playCue`에서 TTS 직전에 `[WrsBingo] cue: 단어`. `__DEV__`만. |
| 주요 경로 | `src/training/wrs/WrsBingoScreen.tsx` |
| 결과 | 코드 추가. |
| 확인 | 파일 확인. |
| 단정 금지 | `미검증`: 실기기 Metro 로그. |
| 성능·주의 | 개발만. **dev client 리빌드 불필요.** |
| 다음 | 없음 |

### 2026-08-19 13:51 — 단어인지도 헤더에서 빙고 스와프

| 항목 | 내용 |
|------|------|
| 트랙 | 단어인지도 |
| 근거·결정 | 사용자: A(4지선다)가 본문. 헤더에서 예전 통계처럼 같은 탭 안 빙고로 갈아끼움. 링 6 헤더는 아님. 듣기 중 Skia·Rive 안 씀. 한 줄이면 칸 색. 기록은 A %와 안 섞음. |
| 변경 요약 | 헤더 `BingoEntryButton`(3×3 아이콘). `WrsBingoScreen` 3×3·TTS·가로/세로/대각 1줄. 쉬움/비슷한 소리 판. 맞히기 기록 저장 없음. |
| 주요 경로 | `src/training/wrs/wrsBingo.ts`, `src/training/wrs/WrsBingoScreen.tsx`, `src/training/wrs/WrsSessionScreen.tsx`, `src/components/ui/bingo-entry-button.tsx`, `src/components/ui/icon.tsx` |
| 결과 | `jest wrsBingo` 5/5. |
| 확인 | `npx jest src/training/wrs/__tests__/wrsBingo.test.ts --no-coverage` |
| 단정 금지 | `미검증`: 실기기 TTS·헤더 터치·한 줄 색. `주의`: 상한 18큐. 축 선택·소음·빙고 저장은 안 넣음. |
| 성능·주의 | 듣기 중 StyleSheet만. JS만. **dev client 리빌드 불필요.** |
| 다음 | 실기기에서 헤더 → 빙고 → 맞히기로. |

### 2026-08-19 13:14 — 세 연습 측정 상한을 트랙별 50으로

| 항목 | 내용 |
|------|------|
| 트랙 | 공통·인프라 |
| 근거·결정 | 사용자: 제안대로. 링 6 50일·WRS 50세션·귀풀기 공유 30은 유지. 세 연습 측정만 트랙별 50이라 그래프가 서로 안 밀림. |
| 변경 요약 | `capByMode`에서 측정은 `freq`/`am`/`pitch2` 각 50. 귀풀기는 합쳐 30. 합계 상한 없음(최대 180). |
| 주요 경로 | `src/training/sessionStore.ts`, `src/training/__tests__/sessionStore.test.ts`, `docs/improvement-backlog.md` §0 |
| 결과 | `jest sessionStore` 43/43. |
| 확인 | `npx jest src/training/__tests__/sessionStore.test.ts --no-coverage` |
| 단정 금지 | `미검증`: 실기기에서 트랙별 그래프 길이. 기존 기기는 다음 append 때 새 상한 적용(읽기만으로는 안 자름). |
| 성능·주의 | 목록 최대 180건. JS만. **dev client 리빌드 불필요.** |
| 다음 | 없음 |

### 2026-08-19 12:59 — 인계 정본을 handoff3.md로

| 항목 | 내용 |
|------|------|
| 트랙 | 문서 |
| 근거·결정 | 사용자: 앞으로는 `handoff3`에서 기록을 이어 가라. `handoff` → `handoff2`와 같은 방식. |
| 변경 요약 | 규칙 저장 경로를 `handoff3.md`로. `handoff.md`·`handoff2.md`는 과거 안내만. 기존 `handoff2` 블록은 옮기지 않음. |
| 주요 경로 | `docs/handoff3.md`, `.cursor/rules/android-dev-client.mdc`, `docs/handoff2.md`, `docs/handoff.md` |
| 결과 | 새 파일 생성. 규칙 반영. |
| 확인 | 규칙에 `handoff3.md`만 저장 위치로. |
| 단정 금지 | 없음 |
| 성능·주의 | 문서만. **dev client 리빌드 불필요.** |
| 다음 | 이후 인계는 `handoff3.md`만. |

### 2026-08-19 12:57 — 링 6·단어인지도에서 세 연습 통계 버튼 제거

| 항목 | 내용 |
|------|------|
| 트랙 | 링 6 / 단어인지도 |
| 근거·결정 | 사용자: 헤더 `StatsEntryButton`이 여는 `SessionHistoryScreen`은 `sessionStore`(다른 음·음높이·포락선)만 보여 이 두 탭과 무관. 탭 안 추이(`Ling6ProgressPanel` / `WrsProgressPanel`)는 그대로. PTA·AM은 유지. |
| 변경 요약 | 두 화면에서 차트 버튼·`showStats` 오버레이·통계 뒤로가기를 뺌. 연습 중 종료 확인 뒤로가기는 유지. |
| 주요 경로 | `src/training/ling6/Ling6SessionScreen.tsx`, `src/training/wrs/WrsSessionScreen.tsx` |
| 결과 | 코드 반영. 실기기는 안 봄. |
| 확인 | 린트(기존 복잡도 경고만). 두 파일에 `showStats`/`StatsEntryButton`/`SessionHistoryScreen` 없음. |
| 단정 금지 | `미검증`: 실기기에서 헤더·뒤로가기. |
| 성능·주의 | JS만. **dev client 리빌드 불필요.** |
| 다음 | 없음 |

### 2026-08-19 12:42 — 단어인지도 추이 미리보기 제거

| 항목 | 내용 |
|------|------|
| 트랙 | 단어인지도 |
| 근거·결정 | 사용자: 가짜 점 없이 실제 기록 2회 이상일 때만 그래프. 링 6 미리보기를 따라 넣은 것은 이 탭에서 원치 않음. |
| 변경 요약 | `ensureTwoWrsSessions`·미리보기 삭제. 0회는 패널 없음, 1회는 목록만, 2회+만 선 그래프. |
| 주요 경로 | `src/training/wrs/wrsTrend.ts`, `src/training/wrs/WrsProgressPanel.tsx` |
| 결과 | 단위 테스트 17/17. |
| 확인 | `npx jest src/training/wrs --no-coverage` |
| 단정 금지 | `미검증`: 실기기에서 1회 목록만·2회 그래프. |
| 성능·주의 | 가짜 점 계산 없음. **dev client 리빌드 불필요.** |
| 다음 | 없음 |

### 2026-08-19 12:36 — 단어인지도 맞힌 비율 추이(링 6형)

| 항목 | 내용 |
|------|------|
| 트랙 | 단어인지도 |
| 근거·결정 | 사용자: PI-PB(dB HL×%)는 기기 보정 불가라 가로를 시간으로. 세 연습(계단식 최소 차이) 그래프는 부적합, 링 6처럼 시간×맞힌 정도. |
| 변경 요약 | idle·요약에 링 6형 선 그래프(0~100%). 점 부족 시 미리보기만 채움(저장 안 함). 최근 목록은 실제 기록만. |
| 주요 경로 | `src/training/wrs/WrsProgressPanel.tsx`, `src/training/wrs/wrsTrend.ts`, `src/training/wrs/WrsSessionScreen.tsx` |
| 결과 | 단위 테스트 19/19. 실기기 그래프는 안 봄. |
| 확인 | `npx jest src/training/wrs --no-coverage` |
| 단정 금지 | `미검증`: 실기기에서 미리보기·2회 이후 선. `주의`: 세로 %는 연습 기록이지 PI-PB·진단이 아님. `추정`: TTS·목록 셔플로 선이 흔들릴 수 있음. |
| 성능·주의 | SVG 한 장. **dev client 리빌드 불필요.** |
| 다음 | 없음 |

### 2026-08-19 11:36 — 단어인지도 4지선다(TTS)

| 항목 | 내용 |
|------|------|
| 트랙 | 공통·인프라 / 단어인지도 |
| 근거·결정 | 사용자: A(4지선다) + TTS, 브랜치 `feat_wrs`. 병원식 WRS(dB·개방형)가 아니라 폐쇄형 단음절·정답률. 오답은 초성·중성·종성 혼동 규칙. 25단어 목록. 링 6처럼 탭이 곧 그 연습. 기록은 `sessionStore`와 분리. |
| 변경 요약 | 단음절 200풀 + 혼동 오답 + TTS 재생 + 25시행 화면. 25개를 다 고른 세션만 저장. `expo-speech` 추가. |
| 주요 경로 | `src/training/wrs/*`, `package.json` (`expo-speech` `~57.0.1`) |
| 결과 | 단위 테스트 15/15. 실기기·TTS는 안 함. |
| 확인 | `npx jest src/training/wrs --no-coverage` |
| 단정 금지 | `미검증`: 실기기 한국어 TTS·단음절 받침 명료도. `주의`: TTS는 사람 말과 다름. `추정`: 혼동 규칙은 구조만(수치 행렬 없음). `주의`: **dev client 리빌드 필요**(`expo-speech` 네이티브). |
| 성능·주의 | 듣기 중 화면은 정적(보기 그리드). TTS는 시스템 엔진이라 오디오 API 합성 경로와 다름. |
| 다음 | 리빌드 후 실기기에서 연습 시작. 빙고·축 선택·소음은 안 넣음. |

### 2026-08-19 03:07 — 떨림 듣기 준비를 화면 전환으로

| 항목 | 내용 |
|------|------|
| 트랙 | ① AM |
| 근거·결정 | 떨림 「연습 시작」이 세션을 안 켬. 듣기 준비를 시작 화면 **위에 겹침**. Android에서 아래 파란 버튼 `elevation: 4`가 오버레이(elevation 0)를 뚫어, 듣기 준비 「연습 시작」이 아래 `onBeforeStart`(아직 `checked=false`)로 새고 세션을 안 만듦. 소리 높낮이는 화면을 갈아끼워 아래 버튼이 없음. |
| 변경 요약 | 듣기 준비를 PTA처럼 **교체**. 귀풀기/연습은 탭이 들고 다시 마운트해도 유지. 통과 후 `autoStart`는 그대로. |
| 주요 경로 | `src/training/am/AmTabScreen.tsx`, `src/training/am/AmSessionScreen.tsx` |
| 결과 | 린트 없음. 실기기 확인은 안 함. |
| 확인 | 린트. |
| 단정 금지 | `미검증`: 실기기에서 듣기 준비 「연습 시작」→ 소리·선택 화면. `추정`: 리빌드 불필요. `주의`: `autoStart`는 마운트 `useEffect`(제스처 밖). RN Android는 보통 됨. |
| 성능·주의 | 듣기 준비 동안 `AmSessionScreen` 언마운트. **dev client 리빌드 불필요.** |
| 다음 | 없음 |

### 2026-08-19 02:54 — 기록 화면 새로고침 버튼 제거

| 항목 | 내용 |
|------|------|
| 트랙 | 공통·인프라 |
| 근거·결정 | 사용자: 포커스 자동 갱신 있으니 「새로고침」은 불필요. 가로 한 줄 파란 「돌아가기」는 과해서, 채움은 유지하되 가로는 글자+여백만·가운데. |
| 변경 요약 | 「새로고침」 제거. 「돌아가기」 `fill={false}` + 행 가운데. `reload`는 마운트·포커스·삭제에서 유지. |
| 주요 경로 | `src/training/SessionHistoryScreen.tsx` |
| 결과 | 린트 없음. 실기기 확인은 안 함. |
| 확인 | 린트. |
| 단정 금지 | `미검증`: 실기기 가운데 정렬·탭 복귀 자동 갱신. |
| 성능·주의 | 버튼 1개 감소. **dev client 리빌드 불필요.** |
| 다음 | 없음 |

### 2026-08-19 02:42 — 링 6 「처음으로」 라벨 잘림

| 항목 | 내용 |
|------|------|
| 트랙 | 링 6 |
| 근거·결정 | 사용자: 요약 「처음으로」 글자가 조금 가려져 전부 안 보임. 세로 `actions`인데 `ActionButton` 기본 `fill`(flex 1)이라 높이 48에 패딩+줄간격이 잘릴 수 있음. 탭바에도 가장 가까움. |
| 변경 요약 | 링 6 하단 버튼 `fill={false}`. `paddingBottom`을 `BottomTabInset + Spacing.four`로. |
| 주요 경로 | `src/training/ling6/Ling6SessionScreen.tsx` |
| 결과 | 린트 없음. 실기기 확인은 안 함. |
| 확인 | 린트. |
| 단정 금지 | `미검증`: 실기기에서 「처음으로」·탭바 겹침. |
| 성능·주의 | 레이아웃만. **dev client 리빌드 불필요.** |
| 다음 | 없음 |

### 2026-08-19 02:26 — 통계 지우기를 탭에 맞게

| 항목 | 내용 |
|------|------|
| 트랙 | 공통·인프라 |
| 근거·결정 | 사용자: 기록 목록·그래프는 이전처럼 전부 보여도 됨. 지우기 버튼만 그 탭에 해당하는 것. 소리 높낮이는 `pitch2`/`freq` 둘. 하단 3버튼을 줄여 스크롤 공간 확보. |
| 변경 요약 | `SessionHistoryScreen`에 `clearTracks`. 떨림=`am`, 소리 높낮이=`pitch2`+`freq`. 링 6·단어인지도는 버튼 없음(링 6 일자 지우기는 idle·요약 유지). |
| 주요 경로 | `src/training/SessionHistoryScreen.tsx`, `src/training/am/AmTabScreen.tsx`, `src/training/pta/PtaSessionScreen.tsx` |
| 결과 | 린트 없음. 실기기 확인은 안 함. |
| 확인 | 린트. 스토어 API는 그대로라 테스트 추가 없음. |
| 단정 금지 | `미검증`: 실기기에서 탭별 버튼 개수·확인·그래프 갱신. |
| 성능·주의 | UI 분기만. **dev client 리빌드 불필요.** |
| 다음 | 없음 |

### 2026-08-19 02:19 — 훈련 기록 트랙별 지우기

| 항목 | 내용 |
|------|------|
| 트랙 | 공통·인프라 |
| 근거·결정 | 사용자: 한 화면에서 3트랙을 보는 건 유지. 전체 삭제는 UX가 거칠어서 **트랙별 지우기**. 건별 삭제는 그대로. 링 6는 별도 저장소라 이 화면에 안 넣음. |
| 변경 요약 | `deleteSavedSessionsByTrack` 추가. 「기록 전체 삭제」를 「높낮이 비교 / 다른 음 찾기 / 떨림 찾기 지우기」3개로 교체. 해당 트랙 없으면 비활성. `clearSavedSessions`는 테스트·개발용으로 남김. |
| 주요 경로 | `src/training/sessionStore.ts`, `src/training/SessionHistoryScreen.tsx`, `src/training/__tests__/sessionStore.test.ts` |
| 결과 | 테스트 41/41. 실기기 확인은 안 함. |
| 확인 | `npx jest src/training/__tests__/sessionStore.test.ts` 41/41. 린트 없음. |
| 단정 금지 | `미검증`: 실기기에서 3버튼·확인 문구·그래프 갱신. |
| 성능·주의 | 트랙 필터 후 쓰기 1회. **dev client 리빌드 불필요.** |
| 다음 | 없음 |

### 2026-08-19 02:13 — 링 6 기록 지우기

| 항목 | 내용 |
|------|------|
| 트랙 | 링 6 |
| 근거·결정 | 사용자: 링 6 탭에서 초기화할 버튼이 필요. 기록은 `ling6Store`만이고 헤더 통계는 `sessionStore`라 그쪽 삭제로 안 지워짐. 측정 통계 「기록 전체 삭제」와 같이 확인 Alert. |
| 변경 요약 | `clearLing6DailyRecords` 추가. idle·요약에서 기록이 있을 때만 「링 6 기록 지우기」. 진행 중 숨김. 다른 탭 측정 기록은 안 건드림. |
| 주요 경로 | `src/training/ling6/ling6Store.ts`, `src/training/ling6/Ling6SessionScreen.tsx`, `src/training/ling6/__tests__/ling6Store.test.ts` |
| 결과 | 테스트 6/6. 실기기 확인은 안 함. |
| 확인 | `npx jest src/training/ling6/__tests__/ling6Store.test.ts` 6/6. 린트 없음. |
| 단정 금지 | `미검증`: 실기기 Alert·그리드 갱신. `주의`: 요약 문구(오늘 맞힌 개수)는 화면 상태라 지우기 직후에도 남고, 「처음으로」가면 빈 그리드. |
| 성능·주의 | 키 1개 `removeItem`. **dev client 리빌드 불필요.** |
| 다음 | 없음 |

### 2026-08-19 02:04 — 연습 탭 → 떨림 전용 탭

| 항목 | 내용 |
|------|------|
| 트랙 | ① AM / 공통·인프라 |
| 근거·결정 | 사용자: 연습 탭에 떨림만 남았으니 링 6·단어인지도처럼 탭이 곧 그 연습. 하단 라벨·아이콘도 떨림에 맞출 것. 소리 높낮이는 2종이라 선택 화면 유지. |
| 변경 요약 | 「연습 선택」카드 제거. `index.tsx`는 `AmTabScreen`만. 탭 라벨 「떨림」, 아이콘 `vibration`. 랜딩은 떨림 시작 화면. 첫 「연습 시작」만 듣기 준비 후 바로 세션. 통계는 idle·요약 헤더. |
| 주요 경로 | `src/app/index.tsx`, `src/training/am/AmTabScreen.tsx`, `src/training/am/AmSessionScreen.tsx`, `src/components/app-tabs.tsx` |
| 결과 | 코드 반영. 실기기 탭 아이콘·라벨은 미확인. |
| 확인 | 린트 없음. |
| 단정 금지 | `미검증`: Android `md="vibration"` / iOS SF 심볼이 NativeTabs에서 보이는지. `추정`: **리빌드 불필요**(JS·시스템 심볼). `주의`: 진행 중 하드웨어 뒤로가기는 예전처럼 목록으로 안 돌아감(전용 탭). |
| 성능·주의 | 듣기 준비는 오버레이라 시작 화면을 안 버림(모드 유지). **dev client 리빌드 불필요.** |
| 다음 | 없음 |

### 2026-08-19 01:46 — 연습 탭 최근 peek 카드 제거

| 항목 | 내용 |
|------|------|
| 트랙 | 공통·인프라 |
| 근거·결정 | 사용자: 연습 탭에만 있던 최근 연습 1줄(peek)을 뺀다. 4탭 모두 헤더 `StatsEntryButton`으로 같은 통계 화면에 들어가고, peek는 전역 측정 최신을 연습 탭에 붙여 다른 탭(높낮이 등) 결과가 뜰 수 있음. |
| 변경 요약 | `index.tsx`에서 peek UI·상태·`listSavedSessions` 로드 제거. 통계 진입은 헤더 버튼만. `SessionHistoryScreen`의 `peekLatestSession`/`LatestSessionPeek`도 호출처가 없어 삭제. |
| 주요 경로 | `src/app/index.tsx`, `src/training/SessionHistoryScreen.tsx` |
| 결과 | 연습 선택 화면이 소리 높낮이 탭과 같이 제목·안내·트랙 카드만. |
| 확인 | 린트 없음. 실기기 화면 확인은 안 함. |
| 단정 금지 | `미검증`: 실기기에서 헤더 통계만으로 발견성이 충분한지는 확인 안 함. |
| 성능·주의 | 연습 목록 복귀 시 `listSavedSessions` 1회가 빠짐. **dev client 리빌드 불필요.** |
| 다음 | 없음 |

### 2026-08-19 01:17 — 경량화 = 중사양 기준 · 측정만 가볍게

| 항목 | 내용 |
|------|------|
| 트랙 | 문서 |
| 근거·결정 | 사용자: 중사양 기준, 저사양 전용 불필요. 측정 화면은 듣는 중 애니메이션 금지(배터리·발열·오디오). 결과·연출은 중사양에 맞춰 사용. |
| 변경 요약 | 규칙·README·`dev-client-setup-context` §5를 그 의견으로 짧게 맞춤. 앱 코드 변경 없음. |
| 주요 경로 | `.cursor/rules/android-dev-client.mdc`, `README.md`, `docs/dev-client-setup-context.md` |
| 결과 | 문서 반영. |
| 확인 | 세 파일 문구. |
| 단정 금지 | `없음` |
| 성능·주의 | 없음(문서). **dev client 리빌드 불필요.** |
| 다음 | 없음 |

### 2026-08-19 01:09 — 인계에서 합의·안 한 일·다음 생략

| 항목 | 내용 |
|------|------|
| 트랙 | 문서 |
| 근거·결정 | 사용자: 앞으로 인계의 `### 합의` / `### 안 한 일` / `### 다음`은 기록하지 말 것. |
| 변경 요약 | 규칙 포함 항목을 한 일·핵심 경로·단정 금지로. `handoff2.md` 안내 추가. 과거 블록은 그대로. |
| 주요 경로 | `.cursor/rules/android-dev-client.mdc`, `docs/handoff2.md` |
| 결과 | 이후 인계부터 세 절 없음. |
| 확인 | 규칙·파일 상단 문구. |
| 단정 금지 | `없음` |
| 성능·주의 | 없음(문서). **dev client 리빌드 불필요.** |
| 다음 | 없음 |

### 2026-08-19 01:06 — impl-log를 `_1`로 이어감

| 항목 | 내용 |
|------|------|
| 트랙 | 문서 |
| 근거·결정 | 사용자: `impl-log.md` 기록을 `impl-log_1.md`로 생성해 이어가라. `handoff` → `handoff2`와 같은 방식. |
| 변경 요약 | `docs/impl-log_1.md` 신설(이후 정본). `impl-log.md`는 과거(01:04까지). 규칙·문서 지도 링크를 `_1`로. |
| 주요 경로 | `docs/impl-log_1.md`, `docs/impl-log.md`, `.cursor/rules/android-dev-client.mdc`, `docs/README.md` |
| 결과 | 이후 항목은 `_1`에만 추가. |
| 확인 | 파일 생성·규칙 문구 확인. |
| 단정 금지 | `없음` |
| 성능·주의 | 없음(문서). **dev client 리빌드 불필요.** |
| 다음 | 이후 구현 로그는 `docs/impl-log_1.md`만. |
