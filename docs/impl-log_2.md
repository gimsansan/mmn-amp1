# 구현 · 결과 로그 (이어서 2)

> **정본**: 이 파일. 새 항목은 **위에 추가**(최신이 위).
> **이전**: [`impl-log_1.md`](./impl-log_1.md) — 2026-08-26 12:45까지. 거기에는 새 항목을 넣지 않음.
> **그 이전**: [`impl-log.md`](./impl-log.md) — 2026-08-19 01:04까지. 역시 새 항목 금지.
> **목적**: 기능 구현·설정 변경 후 **무엇을 했는지 / 결과가 어땠는지**를 날짜별로 남긴다.
> **대상**: 코드·네이티브·오디오·훈련 로직 등 실질 변경. 대화만으로 끝난 합의·설계는 `amp-mdt-training-design.md` 쪽.
> **규칙**: `.cursor/rules/android-dev-client.mdc`

---

## 기록 원칙 (확실 vs 단정 금지)

한 항목에 **두 축을 같이** 남긴다. 한쪽만 적고 끝내면 안 된다.

| 축 | 남길 것 | 예 |
|----|---------|-----|
| **확실한 쪽** | 근거(확인한 사실·파일), 결정, 구현 결과 | “`package.json`에 `^0.13.2` 확인”, “② MVP 순음 재생 성공” |
| **단정 금지 쪽** | 추정·가설·미검증·주의·성능 우려 | “실생활 청취 개선은 미검증”, “이 스텝 크기는 파일럿 추정” |

**표기 접두(설계 문서와 동일 취지)**
`관례` · `가설` · `미검증` · `추정` · `주의` — 확신이 약한 문장 앞에 붙이거나, 아래 표의 **단정 금지** 칸에 모은다.
없으면 `단정 금지 | 없음`이라고 명시한다(빈칸으로 두지 않음).

---

## 기록 형식

새 항목은 **위에 추가**(최신이 위). 복사용:

```md
### YYYY-MM-DD — 짧은 제목

| 항목 | 내용 |
|------|------|
| 트랙 | ② 주파수 / ① AM / 공통·인프라 / 문서 |
| 근거·결정 | 확인한 사실·합의·왜 이렇게 했는지 (확실한 쪽) |
| 변경 요약 | 한두 문장 |
| 주요 경로 | `src/...`, `docs/...` |
| 결과 | 성공 / 부분 / 실패 · 기기·명령 있으면 적기 |
| 확인 | 수동·doctor·테스트 등 |
| 단정 금지 | 추정·가설·미검증·주의 (없으면 `없음`) |
| 성능·주의 | 부정적 영향 있으면 명시, 없으면 `없음` |
| 다음 | 이어서 할 일(있으면) |
```

### 줄 길이 — 한 줄 200자 이내 (필수)

- 표 한 칸에 **문단을 몰아넣지 않는다.** 내용이 많으면 **같은 칸 이름으로 행을 여러 개** 쓴다.
- 이유: grep은 **줄 단위**로 돌려준다. 한 줄이 1298자면 한 글자를 찾아도 1298자가 온다.
- 이유: git diff가 안 읽힌다. 한 문장을 고쳐도 그 긴 줄 전체가 바뀐 것으로 보인다.
- 실제 사례: `impl-log_1.md`에 **1298자** 줄이 있다. 근거·측정은 `docs/ask-token-budget.md` §4.
- 규칙 파일에도 박아 뒀다(`.cursor/rules/android-dev-client.mdc` 「줄 길이」).

---

## 로그

### 2026-08-26 — 링 6 화면 명칭을 「소리 구분」으로

| 항목 | 내용 |
|------|------|
| 트랙 | 공통·인프라 |
| 근거·결정 | 사용자: 웰니스·훈련 컨셉. Ling Six 검사 이름을 UI에 쓰지 않음. 「듣기」도 아님(변별). |
| 근거·결정 | 확정 문구 **소리 구분**. 코드 `ling6`·폴더·저장 키는 그대로. |
| 변경 요약 | 탭·헤더·통계 칩 `KIND_LABEL`. `training-stats-recommendation.md` 탭 표·칩 목록. |
| 주요 경로 | `src/components/app-tabs.tsx` · `Ling6SessionScreen.tsx` · `statsFeed.ts` |
| 주요 경로 | `src/app/ling6.tsx` · `_layout.tsx` · `docs/training-stats-recommendation.md` |
| 결과 | JS만. 과거 impl-log·인계 블록은 안 고침. |
| 확인 | 실기기 탭 라벨은 안 봄. |
| 단정 금지 | `미검증`: 실기기 하단 탭 「소리 구분」 잘림. |
| 성능·주의 | 없음. **리빌드 불필요**(JS 문구만). |
| 다음 | 없음. |

### 2026-08-26 — 문장 듣기 idle 미리보기 9장→6장

| 항목 | 내용 |
|------|------|
| 트랙 | 공통·인프라 |
| 근거·결정 | 사용자: idle 미리보기만 9→6. 순서 항상 랜덤. 줄어든 칸만큼 그림 확대. |
| 근거·결정 | 연습 중 고르기 3장·장면 9·18문항은 그대로. |
| 변경 요약 | `SCENES`를 섞어 6장만. idle+기록 0일 때만. 다시 idle이면 다시 섞음. |
| 변경 요약 | 칸 높이 100→150. `PREVIEW_FIGURE_SCALE` 1.3→1.5. 가로 3칸 유지(3×2). |
| 주요 경로 | `src/training/sentClosed/SentClosedSessionScreen.tsx` |
| 결과 | JS만. 장면·문항·고르기 칸은 미변경. |
| 확인 | 실기기 idle 미리보기 레이아웃은 안 봄. |
| 단정 금지 | `미검증`: 실기기에서 6장·배율 1.5 잘림. |
| 단정 금지 | `주의`: 기록 1회 있으면 미리보기 자체는 안 나옴(기존과 같음). |
| 성능·주의 | 미리보기 이미지 9→6. 고르기 3장은 그대로. **리빌드 불필요**(JS만). |
| 다음 | 없음. |

### 2026-08-26 — 빙고 요약에서 「다시 하기」 제거

| 항목 | 내용 |
|------|------|
| 트랙 | 공통·인프라 |
| 근거·결정 | 사용자: 요약에 난이도 시작 버튼이 있어 「다시 하기」가 겹침. 버튼 수 축소. |
| 변경 요약 | 요약 전용 「다시 하기」 행 삭제. idle·요약 모두 비슷한 소리/쉬운 판/뒤로 가기. |
| 변경 요약 | 「다시 하기」만 쓰던 `lastDifficulty` 상태도 삭제. |
| 주요 경로 | `src/training/wrs/WrsBingoScreen.tsx` |
| 결과 | JS만. `jest` `WrsBingoScreen.test` 통과. |
| 확인 | 요약 화면 수동(실기기)은 안 함. |
| 단정 금지 | `미검증`: 실기기 요약→쉬운 판/비슷한 소리로 재시작. |
| 단정 금지 | `주의`: 같은 난이도 한 탭 재시작은 사라짐. 방금 쓰던 난이도 버튼을 다시 누르면 됨. |
| 성능·주의 | 없음. **리빌드 불필요**(JS만). |
| 다음 | 없음. |

### 2026-08-26 — 빙고 난이도를 목록 카드로 옮김

| 항목 | 내용 |
|------|------|
| 트랙 | 공통·인프라 |
| 근거·결정 | 사용자: 단어 듣기 목록에서 쉬운 판/비슷한 소리를 고르고 바로 시작. idle 난이도 한 단계 축소. |
| 변경 요약 | 목록 카드를 4장으로. `빙고 · 쉬운 판` / `빙고 · 비슷한 소리`. 한·두 글자와 같이 `autoStart`. |
| 변경 요약 | `WrsBingoScreen`에 `autoStart`·`initialDifficulty`. 음성 안내 재시도는 `pendingDifficulty`로 같은 판을 유지. |
| 변경 요약 | idle은 중지 후 `resetRun`용으로 유지. 요약의 난이도·다시 하기는 그대로. |
| 주요 경로 | `src/training/wrs/WrsTabScreen.tsx`, `src/training/wrs/WrsBingoScreen.tsx` |
| 주요 경로 | `src/training/wrs/__tests__/WrsBingoScreen.test.tsx` |
| 결과 | JS만. `tsc --noEmit` 통과(exit 0). |
| 확인 | `jest` `WrsBingoScreen.test`·`WrsSessionScreen.test` 통과. |
| 단정 금지 | `미검증`: 실기기에서 목록 카드→바로 낭독. |
| 단정 금지 | `주의`: `autoStart`는 마운트 `useEffect`(제스처 밖). 한·두 글자와 동일. |
| 단정 금지 | `주의`: 첫 페인트에 idle이 한 프레임 보일 수 있음(한·두 글자와 동일). |
| 성능·주의 | 목록 진입 즉시 판 생성+TTS. 한·두 글자 `autoStart`와 같은 부담. **리빌드 불필요**(JS만). |
| 다음 | 없음. |

### 2026-08-26 — 떨림 요약 버튼 정리(다시 연습 → 처음으로)

| 항목 | 내용 |
|------|------|
| 트랙 | ① AM |
| 근거·결정 | 단어 듣기 정리의 연장. 떨림 요약에 「다시 연습」+「처음으로」 둘 다 있었음. |
| 근거·결정 | 떨림은 `onBack`이 없어(탭이 곧 연습) idle 안내로 돌아갈 길이 「처음으로」뿐. 「다시 연습」은 바로 새 세션이라 소리가 곧장 남. |
| 근거·결정 | 사용자: 하나만 남긴다면 「처음으로」가 나음(idle 복귀 보존). |
| 변경 요약 | `AmSessionScreen` 요약 액션: primary 버튼을 idle=「연습 시작」/summary=「처음으로」로 분기. 요약의 「다시 연습」 제거. |
| 변경 요약 | `onStart`은 idle에서 계속 사용(미사용 아님). `resetToIdle`은 요약 primary로 승격. |
| 주요 경로 | `src/training/am/AmSessionScreen.tsx` |
| 결과 | JS만. `tsc --noEmit` 통과(exit 0). |
| 확인 | `tsc` 통과. 린트: 신규 오류 없음(L114 복잡도·L326 optional chain은 기존, 이번 변경과 무관). |
| 단정 금지 | `미검증`: 실기기에서 요약→「처음으로」→idle 흐름. |
| 단정 금지 | `주의`: 요약에서 곧바로 재시작하려면 「처음으로」→idle→「연습 시작」 두 번. 한 번 재시작을 없앤 트레이드오프. |
| 성능·주의 | 없음. **리빌드 불필요**(JS만). |
| 다음 | 없음. |

### 2026-08-26 — 단어 듣기 라우팅 정리(idle 재진입·목록 통계)

| 항목 | 내용 |
|------|------|
| 트랙 | 공통·인프라 |
| 근거·결정 | 사용자: 앱을 라우팅으로 훑어 불필요한 단계 줄이기. 단어 듣기가 특히 지적됨. |
| 근거·결정 | idle 재진입은 한 글자·**두 글자 둘 다** 해당. (첫 커밋 때 「두 글자엔 없다」고 적었으나 오독 — 두 글자 486행에도 있었음. 후속 커밋에서 제거.) 높낮이·주파수엔 「처음으로」 없음. |
| 근거·결정 | 목록에서 `autoStart`로 idle을 건너뛰는데, 요약 「처음으로」(`resetRun`)가 다시 idle로 돌려보내 목록엔 없던 안내 화면이 뜸. |
| 근거·결정 | 목록(picker)에 통계 진입점이 없었음. 한/두는 idle을 건너뛰어 통계가 요약에서만 열림. 소리 높낮이 picker와 맞춤. |
| 변경 요약 | `WrsSessionScreen`·`WrsTwoCharScreen`: 요약의 「처음으로」 버튼 삭제. 요약은 「다시 연습」+「뒤로 가기」(목록)만. `resetRun`은 중지 확인에 계속 사용. |
| 변경 요약 | `WrsTabScreen`: 목록 헤더에 `StatsEntryButton` 추가. `showStats` 상태·`StatsScreen(initialKind="wrs1")`·BackHandler 분기 추가. |
| 주요 경로 | `src/training/wrs/WrsSessionScreen.tsx` · `src/training/wrs/WrsTwoCharScreen.tsx` · `src/training/wrs/WrsTabScreen.tsx` |
| 결과 | JS만. `tsc --noEmit` 통과(exit 0). |
| 확인 | `tsc` 통과. 린트: 신규 오류 없음(`WrsSessionScreen` L59·`WrsTwoCharScreen` L73 복잡도 경고는 기존, 이번 변경과 무관). |
| 단정 금지 | `미검증`: 실기기에서 요약 「다시 연습」→ 바로 낭독 흐름, 목록 통계 버튼 위치. |
| 단정 금지 | `주의`: 목록 통계는 `wrs1`로 열림. 두 글자(`wrs2`)는 `StatsScreen` 안 탭 전환으로 봐야 함(첫 화면 아님). |
| 단정 금지 | `주의`: 빙고 목록 뒤 idle(난이도 선택)은 그대로 둠. 파일 라우트 승격도 안 함. |
| 성능·주의 | 없음. **리빌드 불필요**(JS만). |
| 다음 | 선택: 빙고 난이도를 목록 카드로 올려 idle 제거. am(떨림) 요약 「처음으로」도 같은 정리 대상인지 검토. |

### 2026-08-26 — 토큰 절약 문서의 범용판 신설

| 항목 | 내용 |
|------|------|
| 트랙 | 문서 |
| 근거 | 사용자 지시: `ask-token-budget.md`를 여러 상황 전제로 더 범용적으로 다시 쓸 것. |
| 근거 | 기존 문서는 2026-08-26 세션 실측에 묶여 있음(§1이 그 세션 숫자). 139~280행에 그때 채팅 로그가 붙어 있음. |
| 근거 | 이미 `ask-file-behavior.md`(범용) : `ask-app-behavior.md`(이 앱) 쌍이 있어 같은 구조를 따름. |
| 결정 | 신규 파일로 분리. 기존 문서는 **삭제·수정하지 않음**(이 앱 실측 기록으로 유지). |
| 결정 | 파일명은 사용자가 준 `ask-token-budge-universal.md` 그대로. (`budget` 오타 여부 미확인.) |
| 변경 요약 | `docs/ask-token-budge-universal.md` 신설. §0 세 줄 요약 ~ §9 시작 전 30초 체크. |
| 변경 요약 | 내용: 비용 원천 표, 탐색 계단 5단계, 상황별 처방 10개(라이브러리·버그·테스트·로그·리팩터링 등). |
| 변경 요약 | 내용: 세션 운영, 요청 쓰는 법, 문서 형식 비용, 하지 말 것, 단정 금지. |
| 변경 요약 | `docs/README.md` 4곳(축 ⑥, 역할표, 결정표, 파일 목록)에 등록. 기존 문서 설명을 「이 앱 실측」으로 조정. |
| 주요 경로 | `docs/ask-token-budge-universal.md`(신) · `docs/README.md` |
| 결과 | 문서만. 코드 변경 0. 12.4 KB, 최장 줄 103자(200자 초과 0건). |
| 확인 | PowerShell `Measure-Object -Property Length -Maximum` → max=103. |
| 단정 금지 | `추정`: 400줄·200자·앞뒤 3줄 기준은 편의값. 문서 안에도 §8로 명시. |
| 단정 금지 | `미검증`: 탐색을 하위 에이전트에 위임하는 것이 실제로 더 싼지. 문서에 그대로 표기. |
| 단정 금지 | `주의`: 두 문서가 같은 주제라 중복 위험. 범용=방법, 기존=이 앱 실측으로 역할을 갈라 두었다. |
| 성능·주의 | 앱 런타임 영향 없음. **리빌드 불필요.** 항상 로드되는 규칙 파일은 건드리지 않아 세션 고정 비용 증가 없음. |
| 다음 | 미결정 — 파일명 `budge` → `budget` 정정 여부(정정 시 README 4곳·이 항목 경로 동반 수정). |

### 2026-08-26 — 통계 화면 「다른 연습」 줄 제거

| 항목 | 내용 |
|------|------|
| 트랙 | 공통·인프라 |
| 근거·결정 | 사용자: 칩을 누르면 고른 칩 내용만. 다른 종목 근황 줄은 빼기로. |
| 변경 요약 | `StatsScreen`에서 `OtherTrainingCard` 삭제. 본문은 제목·그래프·지우기만. |
| 변경 요약 | `statsFeed`의 `GROUP_LABEL`·`glanceOf*`·`relativeDayCopy` 등 근황 API 삭제. |
| 주요 경로 | `src/training/StatsScreen.tsx` · `src/training/statsFeed.ts` |
| 주요 경로 | `src/training/__tests__/statsFeed.test.ts` · `docs/training-stats-recommendation.md` |
| 결과 | 성공. 칩 전환은 그대로. 저장소·키 변경 없음. |
| 확인 | `npx jest src/training/__tests__/statsFeed.test.ts` 1 passed. |
| 단정 금지 | `미검증`: 실기기에서 칩만으로 다른 종목 찾기 편한지. |
| 성능·주의 | 근황 줄 Pressable 4개 감소. `loadStatsFeed`는 칩 전환용으로 5키 그대로. 리빌드 불필요. |
| 다음 | 없음 |

### 2026-08-26 — 기록 줄 길이 200자 규칙 확정

| 항목 | 내용 |
|------|------|
| 트랙 | 문서 |
| 근거 | 앞 항목에서 「미결정」으로 남겼던 것. 사용자가 넣으라고 지시. |
| 근거 | `impl-log_1.md`에 1298자 줄이 실재. 표 한 칸에 문단을 넣는 템플릿이 원인. |
| 근거 | grep은 줄 단위 → 한 글자를 찾아도 긴 줄이 통째로 온다. git diff도 못 읽는다. |
| 결정 | 대상은 `impl-log_2.md`·`handoff4.md`. **한 줄 200자 이내**, 길면 같은 칸 이름으로 행 나누기. |
| 결정 | 사용자가 말한 「`impl-log_1.md` §기록 형식」은 그 파일이 방금 닫혔으므로 `impl-log_2.md`에 적용. |
| 결정 | 과거 파일의 긴 줄은 고치지 않음(누적형 원칙). |
| 변경 요약 | `.cursor/rules/android-dev-client.mdc` 기록 원칙 아래에 「줄 길이」 한 줄 추가. |
| 변경 요약 | `impl-log_2.md` §기록 형식에 「줄 길이 — 한 줄 200자 이내 (필수)」 절 신설. |
| 주요 경로 | `.cursor/rules/android-dev-client.mdc` · `docs/impl-log_2.md` |
| 결과 | 규칙·문서만. 코드 변경 0. |
| 확인 | 이 항목을 200자 이내 줄로 작성. |
| 단정 금지 | `추정`: 200자는 편의 기준. 근거는 「grep 한 줄이 화면 몇 줄을 넘지 않을 정도」. |
| 단정 금지 | `주의`: 규칙 파일은 매 세션 로드된다. 한 줄 추가분이 항상 비용에 들어간다(작지만 실재). |
| 성능·주의 | 앱 런타임 영향 없음. **리빌드 불필요.** |
| 다음 | 없음. 앞 항목의 미결정 2건 모두 해소. |

### 2026-08-26 — impl-log를 `impl-log_2.md`로 넘김

| 항목 | 내용 |
|------|------|
| 트랙 | 문서 |
| 근거 | `impl-log_1.md`가 126.6 KB. 닫힌 `impl-log.md`(135.3 KB)와 거의 같은 크기가 됐다. |
| 근거 | `Read`로 여는 비용은 파일 크기에 비례. 「왜 이렇게 고쳤지?」를 볼 때마다 127 KB를 연다. |
| 근거 | 넘기는 기준은 줄 수가 아니라 **읽기 불편해졌는지**(`docs/README.md` §3 중복 금지 5). |
| 결정 | 사용자 지시: 로테이션만. 규칙 파일·템플릿의 「한 줄 200자」 강제는 **하지 않음**(미결정 유지). |
| 결정 | 내용은 **복사하지 않음**. 옛 파일은 그 자리에 두고 헤더만 「과거·새 항목 금지」로 바꿈. |
| 변경 요약 | `impl-log_2.md` 신설(이 파일). 기록 원칙·기록 형식은 활성 파일에 있어야 하므로 승계. |
| 변경 요약 | `impl-log_1.md` 헤더를 「(과거)」로. 로그 858줄은 그대로 둠. |
| 변경 요약 | `.cursor/rules/android-dev-client.mdc` 2곳 — 기록 대상을 `impl-log_2.md`로, 금지 목록에 `impl-log_1.md` 추가. |
| 변경 요약 | `docs/README.md` 4곳(역할표·결정표 각주·읽는 순서·파일 목록) 갱신. |
| 변경 요약 | 기록 형식에 「칸이 길면 행을 나눠라」 한 줄 추가 — 강제 규칙이 아니라 템플릿 안내. |
| 주요 경로 | `docs/impl-log_2.md`(신) · `docs/impl-log_1.md` · `docs/README.md` · `.cursor/rules/android-dev-client.mdc` |
| 결과 | 문서·규칙만. 코드 변경 0. |
| 확인 | `impl-log_1.md`를 참조하던 곳을 grep으로 확인해 규칙·지도만 갱신. 과거 인계·분석 문서의 언급은 그때 기록이라 두었다. |
| 단정 금지 | `추정`: 다음 로테이션 시점은 다시 120 KB 근처로 보지만, 항목 길이가 줄면 늦어진다. |
| 단정 금지 | `주의`: 새 항목을 `impl-log_1.md`에 넣으면 규칙 위반. 정본은 이 파일. |
| 성능·주의 | 앱 런타임 영향 없음. **리빌드 불필요.** |
| 다음 | 미결정 — 규칙 파일과 템플릿에 「한 줄 200자」를 강제로 넣을지. |
