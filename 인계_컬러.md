# 인계문 — 컬러(색 대비) 작업

> **읽는 사람**: 이 대화를 처음 보는 세션.
> **한 줄**: 팔레트 대비 1차 교정은 **이미 끝나 커밋됐다.** 남은 건 아래 5건이고,
> 그중 2건은 실기기 확인 없이는 결정할 수 없다.
> **먼저 할 일**: 이 문서를 끝까지 읽고, `ui_tn수정.md` 맨 위 3블록을 읽을 것.

---

## 0. 프로젝트가 뭔지

- Expo/React Native 앱. 난청 관련 **듣기 연습**(청력 검사가 아님) — 링 6 · 소리 높낮이 · 단어 듣기 · 떨림 4탭
- **대상 사용자가 고령·난청자다.** 이게 색·크기 판단의 기준선이다. "세련됨"보다 "보이는가"가 이긴다
- 앱 전체가 **라이트 전용**. 다크 모드는 2026-08-11에 **안 하기로 결정**됐고 `src/constants/theme.ts:14-16`에 적혀 있다
- 팔레트 이름은 「Clean Clinical」 — 화이트·블루 기반 정밀 의료기기 톤. 이 톤을 깨는 제안은 거절당할 가능성이 높다

## 1. 저장소 상태

- 브랜치 `ui_chips_suisei` (`main`이 아님, `feat_wrs_voice_guide`에서 분기)
- 커밋 4개, 워킹 트리 clean:

```
a45da1c  동작 — 뒤로가기 가로채기·터치 타깃·파괴적 동작 위치
26fe5df  공용 ScreenHeader — 일곱 화면이 복사해 두던 제목줄을 하나로
a1a5bd7  가독성 — 14px 하한과 대비비를 규칙대로 맞춘다      ← 컬러 변경이 여기 있다
0b2c535  통계 화면 종목 칩을 탭바로
```

- **푸시 안 함.** 실기기 확인이 남아서 일부러 세 커밋으로 갈라 뒀다 —
  글자가 넘치면 `a1a5bd7`만 되돌리면 된다
- 작업 로그는 `ui_tn수정.md` (**누적 문서**: 새 기록은 **맨 위**에 쌓고 기존 블록은 고치지 않는다.
  판단이 뒤집히면 옛 블록을 수정하지 말고 새 블록에서 닫는다 — 실제로 그렇게 운영해 왔다)

## 2. 이미 끝난 것 — **다시 하지 말 것**

`a1a5bd7`에서 `src/constants/theme.ts`의 `Colors.light` 4개를 바꿨다. 색조는 두고 명도만 낮췄다.

| 토큰 | 이전 | **현재** | 흰 카드 대비 |
|---|---|---|---|
| `textMuted` | `#8A9BAD` | **`#607489`** | 2.85 → 4.82 |
| `textSecondary` | `#6B7A8A` | **`#667484`** | 4.40 → 4.78 |
| `positive` | `#1F9D6B` | **`#1A865B`** | 3.45 → 4.56 |
| `highlight` | `#F5833F` | **`#F36C1C`** | 2.56 → 3.01 |

각 값 옆에 대비비를 주석으로 박아 뒀다. `highlight`에는 **「글자색으로 쓰지 말 것」**이 적혀 있다
(3.01은 비텍스트 기준 3:1이고, 본문 4.5:1을 맞추려면 벽돌색 `#C8510A`가 되어 「따뜻한 대비색」
이라는 원래 의도가 죽는다).

같은 커밋에 글자 크기 47곳을 14px로 올린 것도 들어 있다. **색과 크기는 곱해져서 나빠지므로
한 덩어리로 처리했다** — 색만 따로 논하지 말 것.

## 3. 현재 팔레트 대비 전수표

숫자는 아래 4절의 스크립트로 재현된다. `AA` = 4.5:1 이상(본문 통과), `*3:1` = 3:1은 넘고 4.5 미달,
`X` = 3:1 미달.

```
전경 \ 배경        surface   background  accentTint  backgroundSelected
text               15.87 AA   15.03 AA    14.08 AA    14.08 AA
textSecondary       4.78 AA    4.52 AA   4.24 *3:1   4.24 *3:1
textMuted           4.82 AA    4.56 AA   4.28 *3:1   4.28 *3:1
accent              5.09 AA    4.82 AA     4.52 AA     4.52 AA
danger              6.81 AA    6.45 AA     6.04 AA     6.04 AA
positive            4.56 AA   4.32 *3:1  4.05 *3:1   4.05 *3:1
highlight         3.01 *3:1     2.85 X      2.67 X      2.67 X
```

읽는 법: **흰 카드 위는 전부 통과했다.** 남은 미달은 전부 **옅은 파란 면(`accentTint` /
`backgroundSelected`) 위** 아니면 `highlight`다.

## 4. 검증 방법 — 추측하지 말고 계산할 것

WCAG 2.1 상대휘도. 스크래치 파일에 넣고 `node`로 돌리면 된다.

```js
const lin = v => { v /= 255; return v <= 0.03928 ? v / 12.92 : Math.pow((v + 0.055) / 1.055, 2.4); };
const L = h => { const r = parseInt(h.slice(1,3),16), g = parseInt(h.slice(3,5),16), b = parseInt(h.slice(5,7),16);
  return 0.2126*lin(r) + 0.7152*lin(g) + 0.0722*lin(b); };
const CR = (a,b) => { const l1 = L(a), l2 = L(b); return (Math.max(l1,l2)+0.05)/(Math.min(l1,l2)+0.05); };
// 기준: 본문 4.5:1 · 대형 텍스트(18.66px 굵게 또는 24px) 3:1 · 비텍스트 UI 요소 3:1
```

**주의**: 이 앱에서 「대형 텍스트」 예외는 거의 못 쓴다. `smallBold`가 14px 굵게인데
WCAG 대형 기준은 18.66px 굵게부터라 **해당되지 않는다.**

## 5. 남은 과제 — 우선순위 순

### ① `SessionModeToggle` 활성 세그먼트의 힌트 글자 — **확정 미달, 바로 고칠 수 있음**

`src/training/SessionModeToggle.tsx:110`

```tsx
<ThemedText type="small" themeColor="textMuted" style={scaledHint}>
```

이 힌트가 **활성 세그먼트 안**에 있는데, 활성이면 배경이 `accentTint`로 바뀐다(`:95`).
→ `textMuted` on `accentTint` = **4.28:1**, AA 미달.

- 비활성일 때는 흰 면이라 4.82로 통과한다. **활성일 때만 깨진다**
- 선택지: (a) 활성일 때 힌트를 `textSecondary`→도 4.24라 안 됨, (b) 활성 배경을 더 옅게,
  (c) `accentTint` 전용 텍스트 토큰을 새로 만들기, (d) 활성일 때 힌트를 `text`로
- **판단 필요.** (c)가 근본적이지만 토큰이 늘어난다

### ② 카드 경계 — **판단 사항, 위반 아님. 실기기 필요**

- `border` `#E4EBF3` on 흰 카드 = **1.20:1**
- 카드(흰색) vs 배경 `#F6F9FD` = **1.06:1**
- 즉 카드 윤곽이 거의 테두리 + `Shadows.card`(opacity 0.08)에만 의존한다
- WCAG 1.4.11의 3:1을 맞추려면 `#7397C1` 수준 → **「Clean Clinical」 톤이 무너진다**
- **규칙 위반으로 처리하지 말 것.** 실기기에서 노안 조건으로 카드가 구분되는지 보고 결정한다
- 중간 안: 테두리를 조금만 진하게(`#D3DEEA` 수준 ≈ 1.5) + 그림자 강화. 눈으로 봐야 안다

### ③ `accentTint` 위 텍스트 규칙이 없다 — **규칙화 필요**

3절 표에서 보듯 `accentTint`/`backgroundSelected` 위에서는
`textSecondary`(4.24) · `textMuted`(4.28) · `positive`(4.05)가 전부 미달이다.
`accent`(4.52)와 `text`(14.08)만 안전하다.

- 지금 `accentTint`를 쓰는 곳은 17군데인데 **대부분 아이콘 타일**이라 내용물이 `accent`다(안전)
- 실제로 깨진 건 ①뿐이다 — 나머지는 확인했고 문제없다:
  `pill.tsx:70` 은 `accent` 사용, 선택지 타일들(`AmSessionScreen:489` 등)은 `pressed` 순간에만
  `accentTint`이고 글자는 `text`
- **할 일**: 「옅은 파란 면 위에는 `accent` 또는 `text`만」을 `theme.ts` 주석에 규칙으로 박아
  다음 사람이 `textMuted`를 얹지 않게 할 것

### ④ `highlight` 3.01 — 감시가 필요한 규칙

- 현재 쓰임: 추이 그래프 최근 점(`TrendChart.tsx:153-154` · `WrsProgressPanel.tsx:160-161` ·
  `Ling6ProgressPanel.tsx:240-241`), 약한 소리 막대(`Ling6ProgressPanel.tsx:128`),
  아이콘 막대(`icon.tsx:102`), 컨페티(`WrsBingoScreen.tsx:476`)
- **전부 비텍스트다.** 3:1 기준으로 충분하다
- **색 단독 인코딩은 이미 방어돼 있다**(확인 완료, 손댈 것 없음):
  약한 소리 막대 = 높이 + 라벨 색 전환 + `accessibilityLabel`에 횟수 /
  추이 그래프 최근 점 = 채움 원 vs 빈 원
- 위험은 **앞으로 누가 글자색으로 쓰는 것.** 주석은 이미 달아 뒀다

### ⑤ 하드코딩된 hex 2개

`src/training/ling6/Ling6ProgressPanel.tsx:29-30`

```
const BAR_TRACK_BG = "#EEF3F8";   // = theme.borderSubtle 과 같은 값
const BAR_MUTED    = "#D5DDE6";   // 토큰에 없는 값
```

토큰으로 흡수할지 판단할 것. `BAR_MUTED`는 「아쉬움 없음」 막대라 비텍스트다.
(`animated-icon.tsx:143`의 `#208AEF`는 스플래시 전용이라 별개다.)

## 6. 반드시 지킬 제약

1. **`Colors.light` 한 곳만 고친다.** 화면에서 hex를 직접 쓰지 말 것 — 지금 예외는 ⑤의 2개뿐이다
2. **다크 모드를 만들지 말 것.** 결정된 사항이다(`theme.ts:14-16`)
3. **글자 크기를 다시 건드리지 말 것.** 14px 하한은 `themed-text.tsx`의 규칙이고 이미 맞춰 놨다.
   화면에서 인라인 `fontSize`로 줄이는 제안은 금지
4. **「Clean Clinical」 톤을 깨는 큰 변경은 먼저 물어볼 것** — 특히 ②
5. 검증 명령: `npx eslint src --ext .ts,.tsx` · `npx tsc --noEmit -p tsconfig.json` · `npx jest`
   (현재 기준선: eslint 0 errors / 경고 14건은 저장소 테스트의 `import/first`로 **손대지 않기로 한 것** ·
   tsc 0 · jest 23 suites 230 tests 통과)
6. `eslint --fix`를 전체에 돌리지 말 것 — 저장소 테스트 4개의 `jest.mock` 아래 import를
   위로 끌어올려 의도를 깬다. 한 번 겪고 되돌렸다

## 7. 아직 아무도 확인하지 않은 것

**실기기·에뮬레이터로 화면을 본 적이 없다.** 대비비는 전부 계산값이다.
②는 실기기 없이는 결정할 수 없고, 글자 14px 상향이 좁은 화면에서 넘치는지도 미확인이다
(특히 `SessionModeToggle` 10.5→14, `SessionTrendPanel` 8곳).

색 작업을 시작하기 전에 **`/run`으로 앱을 띄워 보는 것**을 권한다. 실제로 보고 나면
②의 답이 나오고, ①의 선택지도 좁혀진다.

## 8. 작업 후

`ui_tn수정.md` **맨 위**에 새 블록으로 기록할 것. 형식은 기존 블록을 따를 것 —
요청 / 판단 / 실제 변경 / **막힌 곳** / 검증 / 미결. 특히 **틀렸던 판단은 숨기지 말고
새 블록에서 철회**한다(앞서 3번 항목을 그렇게 철회했다).
