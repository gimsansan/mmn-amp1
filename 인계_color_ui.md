# 인계문 — `color_ui` 브랜치 (UI·컬러 작업 종료 시점)

> **읽는 사람**: 이 대화를 처음 보는 세션.
> **한 줄**: UI·컬러 작업은 **끝났고 푸시됐다.** 남은 건 실기기로만 답할 수 있는 3건뿐이다.
> **가장 중요한 것**: 5절 「**다시 열지 말 것**」을 먼저 읽어라. 이 갈래는 이미 여러 번
> 「닫은 판단을 다시 여는」 낭비를 겪었고, 그 기록이 `ui_tn수정.md`에 남아 있다.
> **먼저 할 일**: 이 문서를 끝까지 읽고, `ui_tn수정.md` **맨 위 3블록**을 읽을 것.

---

## 0. 프로젝트가 뭔지

- Expo/React Native 앱. 난청 관련 **듣기 연습**(청력 검사가 아니다) — 링 6 · 소리 높낮이 ·
  단어 듣기 · 떨림 4탭
- **대상 사용자가 고령·난청자다.** 이게 색·크기 판단의 기준선이다. 「세련됨」보다 「보이는가」가 이긴다
- **안드로이드 전용이다.** `ios/` 디렉터리가 없다. RN 0.86 · newArch
- 앱 전체가 **라이트 전용**. 다크 모드는 2026-08-11에 **안 하기로 결정**됐고
  `src/constants/theme.ts:14-16`에 적혀 있다
- 팔레트 이름은 「Clean Clinical」 — 화이트·블루 기반 정밀 의료기기 톤

## 1. 저장소 상태

- 브랜치 **`color_ui`** — `ui_chips_suisei`에서 분기, 그건 또 `feat_wrs_voice_guide`에서 분기
- **`origin/color_ui`에 푸시됨. 로컬·원격 동일. 워킹 트리 clean.** 커밋 11개
- `main`에는 아직 안 들어갔다. PR도 안 열었다

```
5771b3e  깃 학습 질문지
9d731e9  작업 로그 — 푸시 · 팔레트 이력 · 「색이 안 변했다」의 원인
f03d3bc  ② 카드 경계 — 현행 유지로 닫는다
57ee21a  인계문 — 컬러(색 대비) 작업            ← 앞 세션의 인계문
779e102  작업 로그 — 컬러 인계문 ①③⑤ 처리 기록
fd2e693  링 6 아쉬움 막대 — chartMuted 토큰화     ← 되돌릴 가능성 있음
95d50ac  옅은 파란 면 위 글자색 규칙
a45da1c  동작 — 뒤로가기 가로채기·터치 타깃
26fe5df  공용 ScreenHeader
a1a5bd7  가독성 — 색 4개 + 글자 42곳            ← 되돌릴 가능성 가장 높음
0b2c535  통계 화면 종목 칩을 탭바로
```

**커밋을 성격별로 갈라 둔 것은 의도다.** 되돌릴 이유가 다르면 커밋도 다르다.
실기기에서 글자가 넘치면 `a1a5bd7`만, 막대가 너무 진하면 `fd2e693`만 빼면 된다.

### 문서 세 개

| 파일 | 무엇 |
|---|---|
| `ui_tn수정.md` | **작업 로그. 누적 문서** — 새 기록은 **맨 위**에, 기존 블록은 **고치지 않는다.** 판단이 뒤집히면 옛 블록을 수정하지 말고 **새 블록에서 닫는다** |
| `인계_컬러.md` | 앞 세션의 인계문. ①③⑤는 닫혔고 ②도 닫혔다. **이 문서의 미해결 목록을 그대로 믿지 말 것** |
| `깃_학습_질문지.md` | 사용자용 학습 자료. 코드와 무관 |

## 2. 지금 팔레트 (`src/constants/theme.ts`)

전부 흰 카드 기준 대비비다. 값 옆에 주석으로 박아 뒀다.

```
text            #10233A   15.87   background      #F6F9FD
textSecondary   #667484    4.78   surface         #FFFFFF
textMuted       #607489    4.82   border          #E4EBF3   (1.20 — 의도된 값)
accent          #1668E3    5.09   borderSubtle    #EEF3F8
danger          #9B3B3B    6.81   accentTint      #EAF2FE
positive        #1A865B    4.56   accentBorder    #CFE0F7
highlight       #F36C1C    3.01   ← 비텍스트 전용. 글자색 금지
chartMuted      #94A7BB           ← 트랙 대비 2.21. 비텍스트 전용
```

**규칙 하나가 주석으로 못박혀 있다**: `accentTint`/`backgroundSelected` 위에서는
`accent`(4.52)와 `text`(14.08)만 AA를 넘는다. `textMuted` 4.28 · `textSecondary` 4.24 ·
`positive` 4.05는 **미달**이다. 흰 면 기준으로 고른 글자색을 옅은 파란 면으로 옮기면 깨진다.

`src/` 안에서 팔레트를 우회하는 hex는 **splash 3개뿐**이고
(`src/components/animated-icon.tsx` — `#208AEF` + 그라디언트 2),
그건 `app.json`의 네이티브 splash 설정과 **같아야 하는 사본**이라 토큰으로 옮기면 안 된다.

## 3. 남은 과제 — 셋뿐이고 전부 실기기가 있어야 한다

계산으로는 더 할 게 없다. **눈으로 봐야만 답이 나오는 것들만 남았다.**

### ① 글자 14px 상향이 좁은 화면에서 넘치는가

`a1a5bd7`이 42곳을 14px로 올렸다. 가장 작던 것은 **10.5px**였다.
의심 지점: `SessionModeToggle`(10.5 → 14, 배율 33%) · `SessionTrendPanel`(8곳).
넘치면 **`a1a5bd7`만 되돌린다.**

### ② `chartMuted` 2.21:1이 충분한가

링 6 아쉬움 막대 색. 이전 `#D5DDE6`는 트랙 대비 1.23이라 사실상 안 보였다.
부족하면 `#8A9EB4`(2.47)까지가 `highlight`(트랙 대비 2.70)를 넘지 않는 한계다.

**함정**: 이 패널은 링 6 기록이 **서로 다른 7일치** 쌓여야 렌더된다
(`ling6Session.ts:206` — `records.length < LING6_WEAKNESS_WINDOW`면 `ready: false`).
그전에는 **화면에 아예 안 나온다.** 「안 보인다」와 「없다」를 헷갈리지 말 것.

### ③ 안드로이드 그림자가 실제로 먹는가

`Shadows.card`가 `shadowOpacity 0.08` / `shadowRadius 18`처럼 **iOS 모양의 값**인데
이 저장소엔 `ios/`가 없다. 안드로이드에서 무엇이 적용되는지 확인하지 않았다.
카드 윤곽을 다시 보게 되면 **테두리보다 여기가 헐거울 수 있다.**

## 4. 검증 — 기준선

```
npx eslint src --ext .ts,.tsx        → 0 errors / 경고 14 (아래 참고)
npx tsc --noEmit -p tsconfig.json    → exit 0
npx jest                             → 23 suites · 230 tests 통과
```

경고 14건은 저장소 테스트 4개의 `import/first`이고 **손대지 않기로 한 것**이다.

**`eslint --fix`를 전체에 돌리지 말 것.** 그 4개 파일의 `jest.mock` 아래 import를 위로
끌어올려 의도를 깬다. 한 번 겪고 되돌렸다.

대비비는 추측하지 말고 계산할 것 (WCAG 2.1 상대휘도):

```js
const lin = v => { v /= 255; return v <= 0.03928 ? v / 12.92 : Math.pow((v + 0.055) / 1.055, 2.4); };
const L = h => { const r = parseInt(h.slice(1,3),16), g = parseInt(h.slice(3,5),16), b = parseInt(h.slice(5,7),16);
  return 0.2126*lin(r) + 0.7152*lin(g) + 0.0722*lin(b); };
const CR = (a,b) => { const l1 = L(a), l2 = L(b); return (Math.max(l1,l2)+0.05)/(Math.min(l1,l2)+0.05); };
// 본문 4.5:1 · 비텍스트 UI 3:1 · 대형 텍스트(18.66px 굵게)는 이 앱에서 거의 해당 없음
```

## 5. **다시 열지 말 것** — 이미 닫힌 판단들

이 갈래가 반복해서 낭비한 지점이다. 각각 이유가 문서에 남아 있다.

| 항목 | 상태 | 다시 열 조건 |
|---|---|---|
| **카드 경계 대비** (`border` 1.20 · 카드 vs 배경 1.06) | **닫힘** — 현행 유지 | 실사용자가 「카드가 어디서 끝나는지 모르겠다」고 말할 때. 그때도 테두리가 아니라 **간격·그림자**부터 |
| **배경을 낮춰 카드를 띄우는 안** | **막힘** | 없음. `themed-view.tsx:15`가 `background`를 화면 바탕으로 깔아서, 낮추면 `textMuted`가 4.56 → AA 아래로 떨어진다 |
| **`highlight` 색 단독 인코딩** | **방어됨** | 없음. 높이 + 라벨 색 전환 + `accessibilityLabel` |
| **다크 모드** | **안 하기로 결정** (2026-08-11) | 없음 |
| **글자 크기 재조정** | **끝남** | 실기기에서 넘칠 때만 |
| **`AmSessionScreen`에 `ScreenHeader` 추가** | **틀린 진단이었음** | 없음. 제목이 없는 게 아니라 히어로 안에 있다(`:341`) |

**카드 경계를 다시 계산하려 들지 말 것.** 천장이 이미 재어져 있다 —
톤을 지키면 `1.20 → 1.36~1.65`가 전부고, WCAG 3:1(`#7397C1`)은 「Clean Clinical」을 죽인다.

## 6. 이 저장소에서 걸려 넘어지는 것들

실제로 시간을 잡아먹은 것만 적는다.

1. **`theme.ts`는 CRLF, `.tsx`는 LF다.** 여러 줄 문자열 치환이 `theme.ts`에서만 조용히
   실패한다. 스크립트로 고칠 때 줄바꿈을 파일에 맞춰야 한다
2. **Bash 히어독이 큰 한글 문서에서 깨진다** (`unexpected EOF while looking for matching`).
   문서를 만들 때는 **Write 도구**를 쓸 것. 두 번 겪었다
3. **Node에서 `/c/Users/...` 경로가 안 먹는다** — Git Bash 경로다. Node에는 `C:/Users/...`로 줄 것
4. **비교 기준선을 먼저 말할 것.** 이 브랜치는 부모(`ui_chips_suisei`) 대비로는 색이 거의
   안 바뀌었고, `d37a654` 대비로는 뚜렷하게 바뀌었다. **같은 작업이 「전부 바뀜」도 되고
   「그대로」도 된다.** 「변했나」를 답하기 전에 무엇과 비교하는지부터 정할 것
5. **`git log --oneline --branches --not --remotes`** — 푸시 안 된 커밋을 뽑는다.
   `ui_chips_suisei`는 업스트림이 없지만 그 커밋은 `origin/color_ui`에 포함돼 있어 안전하다

## 7. 사용자에 대해

- **기록에 의지한다.** 「나는 기록한 내용에 의지하는 편」이라고 직접 말했다.
  중요한 판단은 반드시 `ui_tn수정.md`에 남길 것. 대화에만 두면 잃는다
- **시각적 설명을 원한다.** 색·대비 이야기는 터미널 텍스트로는 전달되지 않는다.
  실제 색을 보여줄 방법(아티팩트 등)을 쓰면 훨씬 빨리 합의된다
- **깃 개념을 배우는 중이다.** 커밋·푸시·브랜치·업스트림 같은 용어를 설명 없이 쓰지 말 것.
  `깃_학습_질문지.md`가 그래서 생겼다
- 판단을 물으면 **선택지를 나열하지 말고 추천을 줄 것.** 다만 근거는 수치로

## 8. 지금 시작한다면

남은 셋이 전부 실기기 의존이므로, **`/run`으로 앱을 띄우는 것**이 사실상 유일한 다음 단계다.
띄우면 3절 ①②③이 한 번에 답해진다.

띄울 수 없다면 이 갈래에서 더 할 일은 없다. 그때는 `main` 병합이 다음 순서다 —
`color_ui`는 푸시돼 있고 PR 링크는 `https://github.com/gimsansan/mmn-amp1/pull/new/color_ui`.

## 9. 작업 후

`ui_tn수정.md` **맨 위**에 새 블록으로 기록할 것. 형식은 기존 블록을 따를 것 —
요청 / 판단 / 실제 변경 / **막힌 곳** / 검증 / 미결.
**틀렸던 판단은 숨기지 말고 새 블록에서 철회한다.** 이 문서 계보가 실제로 그렇게 운영돼 왔다
(3번 항목 철회 · 「47곳」→「42곳」 정정 · ②를 「미확인」에서 「결정」으로 뒤집기).
