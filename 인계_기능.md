# 인계문 — `color_ui` 브랜치 (**기능 갈래**)

> **읽는 사람**: 이 대화를 처음 보는 세션.
> **한 줄**: **반응형·UI는 전부 닫혔다.** 이제 남은 것은 **기능**이다.
> **먼저 할 일**: 이 문서를 끝까지 읽고 → `ui_tn수정.md` **맨 위 두 블록** →
> `docs/improvement-backlog.md`의 **「진행 현황」 표**(11번째 줄).
> **가장 중요한 것**: 1절(★ 사용자 시간 3시간 사고)과 6절(다시 열지 말 것).
> 이 계보는 닫은 판단을 다시 여는 낭비를 반복해 왔다. **6절을 다시 열지 마라.**

---

## 0. 프로젝트가 뭔지

- Expo/React Native 앱. 난청 관련 **듣기 연습**(청력 검사가 아니다) — 링 6 · 소리 높낮이 ·
  단어 듣기 · 떨림 **4탭**
- **대상 사용자가 고령·난청자다.** 이게 판단의 기준선이다. 「세련됨」보다
  「보이는가·누를 수 있는가」가 이긴다
- **안드로이드 전용.** `ios/` 없음. Expo SDK 57 · RN 0.86 · React 19 · newArch ·
  라이트 전용(다크 모드는 2026-08-11 안 함)
- 오디오는 `react-native-audio-api`, 단어 듣기는 **기기 TTS**

## 1. ★★ **먼저 읽어라 — 이 계보가 사용자 시간을 3시간 태웠다**

**사용자가 직접 말했다(2026-08-22): 「이전에 작업 오류인지 3시간 동안 작업이 계속되어
내가 강제 종료함」.** 그 세션은 두 번 더 중간에 끊겼다.

**원인은 버그가 아니라 작업 방식이었다.** 큰 덩어리로 잡아서 **중간에 사용자가 볼
결과물이 오래 안 나왔다.**

### 그래서 이렇게 해라

1. **한 화면·한 항목 끝나면 바로 보고해라.** 다 끝내고 한 번에 보고하지 마라
2. **오래 걸릴 것 같으면 시작 전에 「왜 오래 걸리는지」와 「어디서 끊어 보고할지」를
   같이 말해라.** 사용자는 속도에 민감하고 **진행이 안 보이면 죽인다**
3. **실기기 왕복은 비싸다.** 화면 하나당 최소 4~6번의 `adb` 왕복이 든다고 잡아라
4. **스크린샷은 모아 두지 말고 그때그때 보내라**

> 이 규칙을 지킨 두 세션(폴드 실측 · 「중지」 수정)은 **중단 없이 끝났다.**

## 2. **실기기가 붙어 있다. 이게 이 갈래의 전제다**

```
adb 경로     /d/Android/Sdk/platform-tools/adb      ← PATH에 없다. 전체 경로로 부를 것
기기         RF8MC0XZYLK  SC-01M (갤럭시 노트10+)
규격         w411dp · 420dpi · 1080x2280
앱           com.rnhear.app (개발 빌드)
Metro        8081에서 돌고 있다 — 화면에 보이는 게 워킹 트리의 코드다. 리로드 12초
```

```bash
export MSYS_NO_PATHCONV=1
ADB=/d/Android/Sdk/platform-tools/adb

$ADB exec-out screencap -p > shot.png
$ADB shell input tap <x> <y>
$ADB shell input keyevent 4        # 시스템 뒤로가기
$ADB shell am force-stop com.rnhear.app   # 콜드 스타트 재현
$ADB logcat -c; $ADB logcat -d | grep "내태그"   # console.log 읽기
```

> **화면 크기·글씨 크기는 이제 건드릴 일이 없다.** 반응형은 닫혔다(6절).
> 굳이 바꿨다면 **반드시 명시값으로 되돌려라** — `1080x2280` · `420` · `font_scale 1.0`.
> `wm size reset`을 쓰면 사용자가 낮춰 쓰는 설정까지 날아간다.

## 3. 저장소 상태

- 브랜치 **`color_ui`** · 워킹 트리 clean
- **★ 로컬이 원격보다 앞서 있다.** `origin/color_ui`는 `5e67def`, 로컬은 `ea192c3`
- **`main` 병합은 사용자가 미루라고 했다(2026-08-22).** 먼저 병합하지 마라
  (참고: `main`은 `5fc1eed "init"` 하나뿐이고 `color_ui`가 78+ 커밋 앞선다.
  fast-forward라 충돌 여지는 없다)

> **원격 상태를 말하기 전에 반드시 `git fetch origin`을 먼저 돌려라.**
> 안 하면 틀린 숫자를 말하게 된다(이 계보가 실제로 두 번 틀렸다).

### 문서 지도

| 파일 | 무엇 |
|---|---|
| `docs/improvement-backlog.md` | **기능 갈래의 정본.** 11번째 줄 「진행 현황」 표가 상태의 정본 |
| `docs/fix-reviews.md` | 각 수정의 상세·확인 절차 |
| `docs/impl-log_1.md` · `impl-log.md` | 날짜별 작업 기록 |
| `ui_tn수정.md` | **UI 작업 로그. 누적 문서** — 새 기록은 **맨 위**에. **CRLF다** |
| `인계_마무리.md` | **반응형 갈래 인계문. 그쪽은 전부 닫혔다** |
| `인계_폴드.md` 외 | 더 앞 인계문. 역사용 |

## 4. **지금 할 일 — 우선순위**

### ① 소리 높낮이 갈래의 「확인 없이 나가기」 (작다 · 판단만 받으면 된다)

연습 중에 **시스템 뒤로가기**를 누르면 갈래마다 다르게 동작한다.

| 갈래 | 지금 동작 |
|---|---|
| 소리 높낮이(높낮이 비교·다른 음 찾기) | **묻지 않고** 고르기 화면으로. **세션이 버려진다** |
| 링 6 · 단어 듣기 3종 | 확인 모달 → `resetRun`(버림) |
| 떨림 | 확인 모달 → 요약(**기록 보존**) · 요약에서 한 번 더 → idle |

`PtaSessionScreen:107`의 `backToPicker()`가 그냥 불린다. **사용자가 2026-08-22에
「떨림만 고친다」로 범위를 잘라서 안 건드렸다.** 열려 있는 항목이다.
**시작 전에 사용자에게 물을 것.**

### ② ★ 링 6 · 단어 듣기 **기능** 점검 (제일 크고, 아무도 안 봤다)

`docs/improvement-backlog.md`는 **2026-08-07 `two_feat` 브랜치(「단일 홈 + 연습 3종」)
기준으로 쓰였다.** 지금 앱은 **4탭이고 링 6와 단어 듣기가 그 뒤에 들어왔다.**
문서 전체에서 링6 언급 5회, 단어 듣기 2회뿐이다.

**즉 이 둘은 기능 점검을 제대로 받은 적이 없다.** 지금까지 실기기로 만진 것은
「화면이 잘리는가」였지 「소리가 제대로 나는가 · 기록이 맞게 쌓이는가」가 아니다.

봐야 할 것(제안):

- 링 6: 날짜 upsert 저장(`upsertLing6DailyRecord`)이 하루 두 번 하면 어떻게 되는지 ·
  「못 들었어요」 처리 · 6음을 다 안 고르고 끝냈을 때
- 단어 듣기: TTS가 느리거나 실패할 때 · 25개를 다 안 채우고 끝냈을 때 ·
  두 글자의 목록 순환(`nextTwoCharListIndex`) · 빙고의 기회 소진
- 공통: 탭을 빠르게 오갈 때 저장이 겹치는지(P0-2 큐는 있지만 경로가 늘었다)

**큰 작업이다. 시작 전에 범위를 사용자와 자를 것.**

### ③ 백로그의 남은 항목

| 항목 | 상태 |
|---|---|
| **P0-4** 테마 널 가드 | 고쳤지만 **테스트 없음** |
| **P2-3** 헤드폰·볼륨 안내 | 고쳤지만 **실기기 확인 안 함** |
| **P1-1** 단위 테스트 | 일부 — freq/am 세션·자극 테스트가 없다 |
| **P2-4** 세션 중 통제 | 일부 — 다시 듣기·일시정지 없음 |
| **P2-5** 피드백 내부 수치 | 미착수 · 제품 판단 대기 |
| **P2-7** 기록 내보내기 | 없음 |

### ④ **P3-1 Expo 로고 스플래시 — 배포 전 필수**

개인 파일럿 동안은 불필요하다고 보류 중. **배포를 말하기 시작하면 이게 먼저다.**

## 5. 직전 두 세션이 한 것 (요약)

### `5e67def` — 폴드 두 크기 + 남은 픽셀 대조

폴드 겉화면(344dp x 882dp)·속화면(690dp x 829dp) 실측. 파손 없음.
`eaecb2a` 회귀 대조를 7개 전부 끝냄. 빙고만 판이 커졌는데 **회귀가 아니라
눌려 있던 게 펴진 것**이다(6절).

### `ea192c3` — 「중지」가 소리를 먼저 끊는다 (이번 세션)

사용자가 P0-1을 실기기로 확인했더니 **소리가 계속 났다.** 진단 결과
**P0-1의 수정은 살아 있었고**, 그 뒤에 붙은 오탭 방지 모달(`confirmEndSession`)이
중단 시점을 「끝내기」 뒤로 밀어 같은 증상을 되살린 것이었다.

- `confirmEndSession`에 `onOpen`(띄우기 직전 재생 중단)·`onCancel`(취소 시 다시 냄) 훅
- **7개 화면**에 적용 · 화면마다 `runSeqRef` 세대 카운터
- **문항 0인 세션 저장 차단** — 떨림·다른 음 찾기·높낮이 비교
- **떨림 탭의 막힌 길** — 요약에 「처음으로」 + 시스템 뒤로가기

**소리가 실제로 끊기는 것은 사용자가 귀로 확인했다(2026-08-22).**

## 6. **다시 열지 말 것**

| 항목 | 상태 |
|---|---|
| **반응형 전체**(세로 고정·dp·`MaxContentWidth` 560·브레이크포인트) | **닫힘.** `인계_마무리.md` 5·6절 |
| **연습 중 화면 스크롤 7개** | **닫힘.** `eaecb2a` · 6개 크기 실측 |
| **빙고 판이 커진 것** | **회귀가 아니다.** `tile`의 `flex:1`+`aspectRatio:1`이 눌려 있다 펴진 것. **되돌리지 마라** |
| **P0-1 「중지」** | **닫힘.** `ea192c3` · 사용자가 귀로 확인 |
| **0문항 세션 저장** | **닫힘.** 3개 화면에 가드 |
| **떨림 모드 잠김** | **닫힘.** 「처음으로」 + 뒤로가기 |
| **떨림 뒤로가기를 「버리는」 방식으로 바꾸기** | **안 한다.** 「중지」가 기록을 남기는데 뒤로가기만 버리면 이상하다 |
| **다크 모드 · 글자 크기 재조정 · 태블릿 세로 배치** | **안 하기로 결정** |
| **P1-3 린트/CI · P2-1 ISI 지터 · P2-2 AudioContext 통합 · P2-6 접근성·햅틱 · P1-6 저장 키 통합** | **안 하기로 닫음.** 사유는 백로그 §6 |
| **`eslint --fix` 전체 실행** | **하지 마라.** 저장소 테스트 4개의 `jest.mock` 아래 import를 위로 끌어올려 의도를 깬다 |
| **`main` 병합** | 사용자가 하자고 할 때 |

## 7. 검증 — 기준선

```
npx tsc --noEmit -p tsconfig.json    → exit 0
npx eslint src --ext .ts,.tsx        → 0 errors / 경고 14
npx jest                             → 23 suites · 230 tests 통과
```

경고 14건은 저장소 테스트 4개의 `import/first`이고 **손대지 않기로 한 것**이다.

## 8. 이 저장소에서 걸려 넘어지는 것들

1. **★ `sed -i`와 `cat >>`가 줄바꿈을 갈아엎는다.** `core.autocrlf=true`이고
   **`git diff`는 멀쩡해 보이는데 워킹 트리가 바뀐다.**
   → **줄바꿈을 보존하는 Node 스크립트로 고칠 것.** 치환 건수를 세서 1건이 아니면
   멈추는 형태가 좋다. 끝나면 `file -b`로 확인.
   **`.tsx`·`ui_tn수정.md`는 CRLF, `인계_*.md`는 LF**(작업 트리 기준)
2. **큰 한글 문서를 히어독이나 `node -e`에 인라인으로 넣지 말 것.** 파일로 쓰고 실행
3. **커밋 메시지는 Bash에서 `git commit -F - <<'EOF'`**. PowerShell 히어스트링 금지
4. **Node에서 `/c/Users/...`가 안 먹는다** — `C:/Users/...`
5. **`adb`가 PATH에 없다** · **`export MSYS_NO_PATHCONV=1` 먼저**
6. **★ 오디오가 실제로 나는지 `dumpsys`로 판정할 수 없다.**
   `media.audio_flinger`의 `Standby`를 써 보려 했지만 앱이 `AudioMmapOut` 스트림을
   계속 열어 두어 **무음과 소리가 구분되지 않는다.** `dumpsys audio`의 AAudio
   `state:started`도 마찬가지다. **소리 판정은 사용자에게 부탁할 것**
7. **★ 안드로이드 `Alert`는 버튼으로 닫아도 `onDismiss`가 뒤따라 불린다.**
   `confirmEndSession`은 `settled` 빗장으로 막아 뒀다. **비슷한 대화상자를 새로
   만들면 같은 함정에 빠진다**
8. **★ `abortRef` 하나로는 두 재생 루프를 구분할 수 없다.** 끊은 뒤 다시 틀면
   이전 루프가 되살아나 소리가 겹친다. 7개 화면에 `runSeqRef` 세대 카운터를 뒀다.
   **새 재생 경로를 만들면 같은 패턴을 따를 것**
9. **★ 옛 코드와 대조할 때 `git stash`는 안 통한다**(변경이 커밋돼 있다).
   `git checkout <이전커밋> -- <파일들>` → Metro 리로드 12초 → 스크린샷 →
   `git checkout HEAD -- <파일들>`. **리로드해도 화면 상태(문제·빙고판)가 유지되어**
   같은 화면을 픽셀 대조할 수 있다
10. **★ 화면 이동 좌표를 미리 정해 두고 연속으로 탭하면 어긋난다.**
    「연습 끝내기」로 끝내면 **루트가 아니라** 그 종목의 시작 화면이나 **직전 탭**으로
    간다(한 번은 통계 화면까지 갔다). **한 단계마다 스크린샷으로 확인하며 갈 것**
11. **`input tap`이 가끔 씹힌다.** 스크린샷으로 현재 위치부터 다시 확인할 것
12. **우하단 톱니는 우리 게 아니다 — Expo dev-menu.** 오른쪽 버튼 위에 겹치므로
    탭 좌표를 **왼쪽으로 피할 것**. 화면 오른쪽 가장자리의 잘린 흰 카드는 삼성 엣지 패널
13. **PIL이 있다**(`/c/Python312/python`). ImageMagick은 없다
14. **`__DEV__` + `console.log` → `adb logcat -d | grep`** 로 읽을 수 있다.
    이번에 뒤로가기 핸들러 등록 여부를 이걸로 갈랐다. **끝나면 반드시 걷어낼 것**

### 화면 이동 지도 (1080x2280 @420)

```
탭 y=2018   링6 x=134 · 소리높낮이 x=403 · 단어듣기 x=672 · 떨림 x=943

「연습 끝내기」   취소 (725, 1250) · 끝내기 (893, 1250)
단어듣기 카드    한글자 y=460 · 두글자 y=714 · 빙고 y=966   (x=540)
소리높낮이 카드  높낮이비교 y=475 · 다른음찾기 y=734        (x=540)
시작 화면       「연습 시작」(540,1640) · 「뒤로 가기」(400,1804)
빙고 시작 화면   「비슷한 소리」x=220 · 「쉬운 판」x=540 · 「뒤로 가기」x=840 (y=1826)
ListeningCheck  「연습 시작」(290,1820)
연습 중         「중지」 y≈1804~1830 (x는 400으로 — 오른쪽은 dev-menu와 겹침)
떨림 탭 idle    「연습 시작」(400,1800)

링6      Ling6SessionScreen
소리높낮이 PtaSessionScreen(고르기) → ListeningCheckScreen → PitchCompareScreen / FreqSessionScreen
단어듣기  WrsTabScreen(고르기) → WrsSessionScreen / WrsTwoCharScreen / WrsBingoScreen
떨림     AmTabScreen → ListeningCheckScreen(첫 시작 전에만) → AmSessionScreen
통계     각 화면 우상단 막대그래프 버튼 → StatsScreen
```

**모드 토글**: 「귀풀기」= `practice`(**저장 안 함**) · 「연습」= `measure`(**저장함**).
이름이 반대로 느껴지니 조심할 것. **기록을 봐야 하는 시험은 「연습」으로 할 것.**

**`WrsVoiceGuideScreen`은 한국어 TTS가 없는 기기에서만 뜬다**(`WrsTabScreen:81`).
시험기에는 있어서 실기기로 진입할 방법이 없다. jest 테스트 4건은 통과한다.

## 9. 사용자에 대해

- **★ 진행이 안 보이면 죽인다.** 1절을 다시 읽어라. **이게 제일 중요하다**
- **기록에 의지한다.** 「나는 기록한 내용에 의지하는 편」이라고 직접 말했다.
  중요한 판단은 반드시 문서에 남길 것. 대화에만 두면 잃는다
- **★ 자기가 직접 실기기로 확인한다.** 「끊긴다」처럼 짧고 정확하게 알려 준다.
  **소리처럼 내가 못 보는 것은 절차를 짧게 적어 주고 부탁할 것** — 잘 해 준다
- **시각적 설명을 원한다.** 스크린샷을 PIL로 이어 붙여 나란히 보낼 것
- **깃 개념을 배우는 중이다.** 커밋·푸시·브랜치를 설명 없이 쓰지 말 것
- **판단을 물으면 선택지를 나열하지 말고 추천을 줄 것.** 다만 근거는 수치로
- **범위를 스스로 좁힌다.** 「펴는 동작까지는 필요 없다」, 「곁다리 두 건은 그냥 둔다」,
  「떨림만 고친다」처럼 잘라 준다. **잘라 준 것은 그대로 받아들이고 다시 꺼내지 마라**
- **내 질문이 헷갈리면 되묻는다.** 「링 6는 뒤로가기가 없는데 무엇을 물어보는 거지?」
  → **용어를 정확히 쓸 것**(화면 안 버튼 vs 시스템 뒤로가기)
- **세션을 자주 새로 연다.** 그래서 인계문이 이 프로젝트의 생명줄이다

## 10. 작업 후

`ui_tn수정.md` **맨 위**에 새 블록으로 기록할 것(**CRLF 보존**). 형식은 기존 블록을 따를 것 —
요청 / 판단 / 실제 변경 / **막힌 곳** / 검증 / 미결.
기능 쪽 상태는 **`docs/improvement-backlog.md`의 진행 현황 표**도 같이 갱신할 것.

**틀렸던 판단은 숨기지 말고 새 블록에서 철회한다.** 이 계보가 실제로 그렇게
운영돼 왔다(「처방은 하나」→ 둘 · 「나머지도 같은 결과」→ 빙고는 달랐다 ·
줄바꿈 표 정정 · **「저장하는 화면은 둘」→ 셋 정정** ·
**「Standby로 소리를 판정했다」→ 그 지표는 못 쓴다 정정**).
