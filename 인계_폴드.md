# 인계문 — `color_ui` 브랜치 (폴드 · 남은 대조)

> **읽는 사람**: 이 대화를 처음 보는 세션.
> **한 줄**: **`인계_반응형.md` 3절(연습 중 화면 스크롤)이 끝났다.** 남은 건
> 폴드 정지 크기 2종과, 안 끝낸 픽셀 대조 몇 개다. **급한 파손은 없다.**
> **먼저 할 일**: 이 문서를 끝까지 읽고 `ui_tn수정.md` **맨 위 블록**을 읽을 것.
> **가장 중요한 것**: 1절(★ 3시간 폭주 사고)과 6절(다시 열지 말 것).
> 이 갈래는 닫은 판단을 다시 여는 낭비를 반복해 왔다. **6절을 다시 열지 마라.**

---

## 0. 프로젝트가 뭔지

- Expo/React Native 앱. 난청 관련 **듣기 연습**(청력 검사가 아니다) — 링 6 · 소리 높낮이 ·
  단어 듣기 · 떨림 4탭
- **대상 사용자가 고령·난청자다.** 이게 판단의 기준선이다. 「세련됨」보다
  「보이는가·누를 수 있는가」가 이긴다
- **안드로이드 전용.** `ios/` 없음. RN 0.86 · newArch · 라이트 전용(다크 모드는 2026-08-11 안 함)
- 팔레트 이름은 「Clean Clinical」

## 1. ★★ **먼저 읽어라 — 이 계보가 사용자 시간을 3시간 태웠다**

**사용자가 직접 말했다(2026-08-22): 「이전에 작업 오류인지 3시간 동안 작업이 계속되어
내가 강제 종료함」.** 이번 세션에서도 **두 번** 중간에 끊겼다.

```
1차 중단   「작업이 안 끝남 그래서 강제 종료시킴」
2차 중단   「중단해 일단」
```

**원인은 버그가 아니라 작업 방식이었다.** 화면 7개를 「전부 재현 → 전부 수정 →
전부 검증」이라는 큰 덩어리로 잡아서, **중간에 사용자가 볼 결과물이 오래 안 나왔다.**
실기기 왕복(탭 눌러 들어가기 · 스크린샷 · 확인)이 화면당 3~5회씩 필요한데
그 비용을 예상에서 빼먹었다. 25분이라고 말하고 그보다 훨씬 오래 걸렸다.

### 그래서 이렇게 해라

1. **한 화면 고치면 바로 보고하고, 커밋 단위를 잘게 가져가라.** 7개를 다 끝내고
   한 번에 보고하지 마라
2. **오래 걸릴 것 같으면 시작 전에 「왜 오래 걸리는지」와 함께 「어디서 끊어 보고할지」를
   같이 말해라.** 사용자는 속도에 민감하고 **진행이 안 보이면 죽인다**
3. **실기기 왕복은 비싸다.** 화면 하나당 최소 4~6번의 `adb` 왕복이 든다고 잡아라
4. **스크린샷은 모아 두었다가 마지막에 몰아 보내지 말고 그때그때 보내라**

## 2. **실기기가 붙어 있다. 이게 이 갈래의 전제다**

```
adb 경로     /d/Android/Sdk/platform-tools/adb      ← PATH에 없다. 전체 경로로 부를 것
기기         RF8MC0XZYLK  SC-01M (갤럭시 노트10+)
규격         w411dp · 420dpi · 1080x2280
앱           com.rnhear.app (개발 빌드)
Metro        8081에서 돌고 있다 — 화면에 보이는 게 워킹 트리의 코드다
```

### 이 갈래의 핵심 도구

```bash
export MSYS_NO_PATHCONV=1
ADB=/d/Android/Sdk/platform-tools/adb

$ADB shell settings put system font_scale 1.3    # 큰 글씨 재현
$ADB shell settings put system font_scale 1.0    # 반드시 되돌릴 것
$ADB exec-out screencap -p > shot.png
$ADB shell input tap <x> <y>
```

### 화면 크기를 흉내내는 법

```bash
$ADB shell wm size 720x1280 && $ADB shell wm density 360    # 320dp x 568dp
$ADB shell wm size 720x1280 && $ADB shell wm density 320    # 360dp x 640dp
$ADB shell wm size 1200x1920 && $ADB shell wm density 213   # 901dp x 1442dp 태블릿
$ADB shell wm size 904x2316 && $ADB shell wm density 420    # 344dp x 882dp 폴드 겉화면
$ADB shell wm size 1812x2176 && $ADB shell wm density 420   # 690dp x 829dp 폴드 속화면

$ADB shell wm size 1080x2280 && $ADB shell wm density 420   # ← 되돌리기
```

> **`wm size reset` / `wm density reset`을 쓰지 마라.** 이 기기는 사용자가
> **물리 1440x3040·560dpi를 1080x2280·420dpi로 낮춰 쓰고 있다.** `reset`은 사용자의
> 그 설정까지 지우고 물리값으로 돌아가 버린다. **반드시 명시값으로 되돌릴 것.**

**끝나면 `font_scale`·`wm size`·`wm density`를 전부 되돌려라.** 사용자의 실제 폰이다.
(지금은 `1.0` · `1080x2280` · `420`으로 되돌려 놓은 상태다.)

> **화면 크기를 바꾸면 기기가 잠기거나 화면이 꺼질 수 있다.** 이번에 그래서 막혔다.
> `input keyevent 224`로 깨울 수는 있지만 **잠금이 걸렸으면 거기서 멈추고 사용자에게
> 풀어 달라고 해라.** 잠금번호는 건드리지 마라.

## 3. 저장소 상태

- 브랜치 **`color_ui`** · 워킹 트리 clean
- **`main` 대비 76 커밋 앞선다. PR 없음.**
  **사용자가 「병합은 아직 안 해도 된다」고 했다(2026-08-22).** 먼저 병합하지 마라
- ★ **`eaecb2a`(이번 작업)는 아직 푸시 안 됐다.** 원격 `origin/color_ui`는 `11b9804`다

```
eaecb2a  연습 중 화면 — 7개에 스크롤을 씌워…       ← 이번 작업 · 로컬에만 있다
11b9804  태블릿 — MaxContentWidth 800에서 560으로   ← 원격은 여기까지
f82c230  반응형 실측 — 파손 범위 정정
```

> **원격 상태를 말하기 전에 반드시 `git fetch origin`을 먼저 돌려라.**
> 안 하면 틀린 숫자를 말하게 된다(이 계보가 실제로 두 번 틀렸다).

### 문서

| 파일 | 무엇 |
|---|---|
| `ui_tn수정.md` | **작업 로그. 누적 문서** — 새 기록은 **맨 위**에. 기존 블록은 고치지 않는다. 판단이 뒤집히면 새 블록에서 닫는다. **CRLF다** |
| `인계_반응형.md` | 직전 인계문. **그 3절은 이 문서에서 끝났다.** 미해결 목록을 그대로 믿지 말 것 |
| `인계_바닥여백.md` · `인계_글씨크기.md` · `인계_color_ui.md` · `인계_컬러.md` | 더 앞 인계문. 역사용 |

## 4. **지금 할 일 — 급한 것 없음. 우선순위 순서**

### ① 폴드 정지 크기 2종 (사용자가 지목한 것)

사용자가 **「320dp면 가장 작은 폰, 폴드폰 테스트가 남았다는 건데」**라고 지적했다.
맞는 지적이라 시작했는데 **기기가 잠겨서 못 끝냈다.**

```
폴드 겉화면   904x2316 @420  →  344dp x 882dp   ← 좁지만 「길다」
폴드 속화면   1812x2176 @420 →  690dp x 829dp   ← 411dp 폰과 901dp 태블릿 사이
```

**위험은 낮다고 본다.** 파손의 지배 변수가 **높이**인데 둘 다 829dp 이상으로
사용자 기기(868dp)와 비슷하고, 폭도 이미 잰 320~901dp 범위 안이다.
**다만 재 보지 않은 것은 재 보지 않은 것이다.** 확인할 것:

- 연습 중 화면 7개에서 잘리는 곳이 없는지
- **좁은 폭(344dp)에서 글자가 이상하게 쪼개지는 곳** — 4절 ③의 두 건이 여기서 더 나빠질 수 있다
- 속화면(690dp)에서 `MaxContentWidth: 560`이 걸려 가운데 정렬되는지

> **「펴는 동작」(접었다 펴는 순간의 액티비티 재생성)은 사용자가 안 해도 된다고 했다
> (2026-08-22). 하지 마라.** 6절 참고.

### ② 안 끝낸 픽셀 대조 — 6개 화면

`eaecb2a`에서 **411dp x 868dp 회귀 대조를 `AmSessionScreen` 하나만 했다.**
나머지 6개는 안 했다. 방법은 이미 검증돼 있다:

```bash
# 1) 지금 코드로 화면을 띄우고 스크린샷
# 2) git stash → Metro가 리로드 → 같은 화면 스크린샷
# 3) git stash pop
# 4) PIL로 대조 (상태바 위쪽 120px는 시계가 달라 잘라내고 비교)
```

`Am`에서 나온 결과: **버튼 y좌표 완전 일치, 다른 픽셀 0.52%는 전부 카드 그림자가
스크롤 경계에서 잘린 것.** 나머지도 같은 결과가 나올 것으로 본다.

### ③ 곁다리 두 건 — 320dp x 568dp에서 봤지만 안 고쳤다

| 화면 | 증상 | 판단 |
|---|---|---|
| `AmTabScreen` | 「귀풀기/연습」 토글이 「연습 시작」 버튼에 **잘린다** | 7개 목록 밖이라 이번에 안 건드렸다. **누를 수는 있다** |
| `WrsBingoScreen` idle | 버튼 3개가 「비슷/한/소리」로 한 글자씩 쪼개진다 | 폭이 좁아서다. **파손은 아니다** |

**둘 다 사용자에게 물어보고 시작할 것.** 범위 밖이었다.

## 5. 이번에 한 것 — `eaecb2a`

### `running` 화면 7개에 스크롤 (`인계_반응형.md` 3절이 닫혔다)

`running` 블록만 `ScrollView` 밖의 맨 `View`라, 내용이 화면보다 길면 잘릴 뿐
스크롤할 방법이 없었다. **320dp x 568dp에서 7개 전부 실기기로 파손을 재현했다**
(직전 인계문은 1개만 눈으로 봤고 6개는 구조 추론이었다).

| 화면 | 기본 글씨(1.0)에서의 증상 |
|---|---|
| `WrsSessionScreen` · `WrsTwoCharScreen` | 「중지」 아래쪽 잘림 |
| `WrsBingoScreen` | **「중지」가 통째로 사라짐** |
| `Ling6SessionScreen` | **그림 6개 중 4개만 · 「쉬」「스」「못 들었어요」가 화면 밖.** 제일 심했다 |
| `PitchCompareScreen` | 보기 글자가 잘려 화살표만 |
| `FreqSessionScreen` · `AmSessionScreen` | 「중지」 잘림 |

### ★ 처방이 **하나가 아니라 둘**이었다 — 이게 이번의 핵심

직전 인계문은 `flex: 1` + `flexGrow: 1` 하나만 적어 뒀다.
**그대로 7개에 쓰면 3개가 사용자 기기에서 회귀한다.**

| 갈래 | 화면 | `running`의 자리 배분 | 쓴 값 |
|---|---|---|---|
| ① | `Wrs` 3개 · `Ling6` | `promptArea`에 `flex` 없음 · `actions`에 `marginTop: "auto"` | **`flexShrink: 1`** |
| ② | `Am` · `Freq` · `Pitch` | `promptArea`가 `flex: 1`로 남는 자리를 먹어 가운데 정렬 | `flex: 1` + `flexGrow: 1` |

- **①에 `flex: 1`을 쓰면**: 남는 자리를 스크롤이 다 먹어 **버튼이 아래로 내려간다**
- **②에 `flexShrink`를 쓰면**: `promptArea`의 `flex: 1`이 주저앉아 **가운데 정렬이 무너진다**

근거는 각 파일 주석에 남겼다. **이 구분을 지우지 마라.**

### 철회한 판단

`Am`·`Freq`에 있던 「**진행 중 화면은 선택지가 고정돼야 해서 감싸지 않는다**」는 주석을
철회했다. 짧은 화면에서는 선택지가 넘쳐 버튼이 밖으로 나간다. **고정이 지킬 게 없다.**

## 6. 검증 — 기준선

```
npx tsc --noEmit -p tsconfig.json    → exit 0
npx eslint src --ext .ts,.tsx        → 0 errors / 경고 14
npx jest                             → 23 suites · 230 tests 통과
```

경고 14건은 저장소 테스트 4개의 `import/first`이고 **손대지 않기로 한 것**이다.
**`eslint --fix`를 전체에 돌리지 말 것** — 그 파일들의 `jest.mock` 아래 import를 위로
끌어올려 의도를 깬다. 한 번 겪고 되돌렸다.

## 7. **다시 열지 말 것**

| 항목 | 상태 | 다시 열 조건 |
|---|---|---|
| **`running` 화면 스크롤 (7개)** | **닫힘.** `eaecb2a` · 320dp x 568dp 실기기 확인 | 없음 |
| **두 갈래 처방(`flexShrink` vs `flex:1`)** | **닫힘.** 화면마다 근거가 다르다. 5절 표 참고 | 없음 |
| **스크롤 경계의 그림자 잘림** | **안 고치기로 닫음.** 없애려면 여백을 줘야 하는데 그러면 키 큰 화면이 밀려 **진짜 회귀**가 된다. 1배에선 안 보인다 | 없음 |
| **폴드 「펴는 동작」(액티비티 재생성)** | **안 하기로 닫음.** 사용자가 「거기까지 할 필요는 없다」고 했다(2026-08-22) | 사용자가 다시 말할 때 |
| **바닥 여백 / `BottomTabInset`** | **닫힘.** 10개 화면 처리 | 없음 |
| **WRS 요약 pill 가림** | **닫힘.** 여백 회수로 저절로 풀렸다 | 없음 |
| **첫 음 뜸 0.7초** | **닫힘.** 사용자가 「적당하다」고 확정 | 사용자가 다시 말할 때 |
| **뜸 동안의 문구** | **「잠시 뒤 들려 드려요」로 닫음.** 「듣는 중」은 난청 사용자에게 거짓말이 된다 | 없음 |
| **세로 고정 · dp 치수 · `MaxContentWidth`** | **잘 돼 있다. 손대지 마라.** 8절 참고 | 없음 |
| **태블릿 폭** | **닫힘.** `MaxContentWidth` 800 → **560**. 901dp 실기기 확인 | 없음 |
| **폭에 따른 브레이크포인트(열 수 바꾸기)** | **넣지 않기로 닫음.** 분기 없는 레이아웃이 이 앱이 튼튼한 이유다 | 없음 |
| **태블릿 세로 배치**(가운데가 빔) | **안 하기로 닫음.** 폰 앱이 태블릿에 올라간 정상적인 모습 | 사용자가 태블릿을 실제로 쓰겠다고 할 때 |
| **`ListeningCheckScreen` 파손** | **닫힘.** 1.0/1.3/2.0 확인 완료 | 없음 |
| **`allowFontScaling` 상한** | **두지 않기로 닫음.** 고령 사용자가 키운 글씨를 앱이 거부할 근거가 없다 | 스크롤로도 **막는** 파손이 남을 때 |
| **`chartMuted` 2.21:1** | **현행 유지로 닫음** | 없음 |
| **안드로이드 그림자**(`Shadows.card`) | **닫힘.** `elevation: 2`/`4`가 이미 있다 | 없음 |
| **`a1a5bd7` 되돌리기** | **하지 말 것** | 없음 |
| **카드 경계 대비**(`border` 1.20) | **닫힘 — 현행 유지** | 실사용자가 「카드가 어디서 끝나는지 모르겠다」고 할 때 |
| **배경을 낮춰 카드를 띄우는 안** | **막힘.** `textMuted`가 AA 아래로 떨어진다 | 없음 |
| **다크 모드** | **안 하기로 결정**(2026-08-11) | 없음 |
| **글자 크기(px) 재조정** | **끝남** | 없음 |
| **「설정 화면」** | **그런 화면은 없다.** 우하단 톱니는 **Expo dev-menu**다 | 없음 |
| **`main` 병합** | **사용자가 미루라고 했다**(2026-08-22) | 사용자가 하자고 할 때 |

## 8. 반응형 — **이미 잘 돼 있는 것**(손대지 마라)

**실기기로 흉내내어 확인한 것들이다. 추론이 아니다.**

| 조건 | 흉내내는 법 | 결과 |
|---|---|---|
| **901dp x 1442dp** 태블릿 | `1200x1920@213` | **온전.** `MaxContentWidth` 560으로 보기 칸 248dp |
| **411dp x 868dp** 사용자 기기 | (기기 그대로) | **1.0 · 1.3 · 2.0 전부 온전** |
| **360dp x 640dp** 보급형 | `720x1280@320` | 1.0 · 1.3 온전 → 2.0은 `eaecb2a` 전엔 파손이었다 |
| **320dp x 568dp** 좁고 짧은 폰 | `720x1280@360` | **`eaecb2a`로 7개 전부 통과** |
| **344dp x 882dp / 690dp x 829dp** 폴드 | `904x2316@420` / `1812x2176@420` | **안 쟀다 — 4절 ①** |

**튼튼한 이유**:

- **세로 고정**(`app.json` `orientation: "portrait"`) — 가로 조합을 아예 없앴다
- 모든 치수가 **dp**라 화면 밀도를 따라간다. 절대 px이 박힌 곳 없음
- `MaxContentWidth: 560` + `alignSelf: "center"` — 폰(320~411dp)에서는 아무 일도 하지 않는다
- 보기 칸이 `width: "48%"`라 폭을 따라간다
- `SafeAreaView edges={["top","left","right"]}` — 노치·홈바를 피한다
- **화면 크기를 재서 분기하는 코드가 사실상 없다**(`Dimensions.get`은 `animated-icon`
  한 곳뿐). **이게 이 앱이 튼튼한 진짜 이유다. 반응형을 「개선」한다며 분기를 넣지 마라**

## 9. 이 저장소에서 걸려 넘어지는 것들

1. **★ `sed -i`와 `cat >>`가 줄바꿈을 갈아엎는다.** 파일마다 줄바꿈이 다르고
   (`.tsx`도 CRLF와 LF가 섞여 있다) `core.autocrlf=true`다.
   **`git diff`는 멀쩡해 보이는데 워킹 트리가 바뀐다.**
   → **줄바꿈을 보존하는 Node 스크립트로 고칠 것.** 치환 건수를 세서 1건이 아니면
   멈추는 형태가 좋다. 끝나면 `file -b`로 확인
   - **이번에 확인된 것**: `ui_tn수정.md`는 **CRLF**. `인계_*.md`는 **LF**.
     `Am`·`Freq`·`Pitch` 세 화면은 원래부터 **LF**, 나머지 `.tsx`는 **CRLF**
2. **큰 한글 문서를 Bash 히어독이나 `node -e`에 인라인으로 넣지 말 것.**
   **스크립트를 Write 도구로 파일에 쓰고 `node <파일>`로 실행할 것**
3. **커밋 메시지에 PowerShell 히어스트링(`@'...'@`)을 Bash에서 쓰지 말 것.**
   Bash에서는 `git commit -F - <<'EOF'`
4. **Node에서 `/c/Users/...` 경로가 안 먹는다** — `C:/Users/...`로 줄 것
5. **`adb`가 PATH에 없다.** `/d/Android/Sdk/platform-tools/adb`
6. **Git Bash가 `adb` 인자의 `/sdcard/...`를 윈도우 경로로 바꾼다.**
   `export MSYS_NO_PATHCONV=1`을 먼저 줄 것
7. **화면 오른쪽 가장자리의 잘린 흰 카드는 우리 앱이 아니다** — 삼성 엣지 패널 핸들.
   우하단 톱니(`Tools`)도 우리 게 아니다 — **Expo dev-menu**. 프로덕션엔 없다
8. **★ 720x1280 화면에서 Expo dev-menu 버튼이 「떨림」 탭과 겹친다.**
   탭을 누르려다 dev-menu가 열린다. **`x=560`**(4번째 칸의 왼쪽)으로 피할 것
9. **`font_scale`을 바꾸면 액티비티가 재시작되어 기본 탭(떨림)으로 돌아간다.**
10. **`adb shell input tap`이 가끔 씹힌다.** 스크린샷으로 현재 위치부터 다시 확인할 것
11. **연습 중에 뒤로가기를 누르면 「연습 끝내기」 확인 대화상자가 뜬다.**
    이걸 안 닫으면 다음 탭이 씹힌다. 720x1280에서 「끝내기」는 **(585, 730)**
12. **화면 크기 변경 뒤 기기가 잠길 수 있다.** 2절 마지막 경고 참고
13. **PIL이 있다**(`/c/Python312/python`). 스크린샷 픽셀 대조에 쓸 수 있다.
    ImageMagick은 없다

### 화면 이동 지도 (1080x2280 기준 · 720x1280은 x×0.667 · y×0.561)

```
탭(y=2018)  링6 x=134 · 소리높낮이 x=403 · 단어듣기 x=672 · 떨림 x=943

링6      Ling6SessionScreen
소리높낮이 PtaSessionScreen(고르기) → ListeningCheckScreen → PitchCompareScreen / FreqSessionScreen
단어듣기  WrsTabScreen(고르기) → WrsSessionScreen / WrsTwoCharScreen / WrsBingoScreen
떨림     AmTabScreen → ListeningCheckScreen → AmSessionScreen
통계     각 화면 우상단 막대그래프 버튼 → StatsScreen
```

**`WrsVoiceGuideScreen`은 한국어 TTS가 없는 기기에서만 뜬다**(`WrsTabScreen:81`).
시험기에는 있어서 **실기기로 진입할 방법이 없다.** 이 화면에는 `running`이 없어
`eaecb2a`와 무관하다. jest 테스트 4건은 통과한다.

## 10. 사용자에 대해

- **★ 진행이 안 보이면 죽인다.** 1절을 다시 읽어라. **이게 제일 중요하다**
- **기록에 의지한다.** 「나는 기록한 내용에 의지하는 편」이라고 직접 말했다.
  중요한 판단은 반드시 `ui_tn수정.md`에 남길 것. 대화에만 두면 잃는다
- **시각적 설명을 원한다.** 색·레이아웃 이야기는 터미널 텍스트로 전달되지 않는다.
  **실기기 스크린샷 수정 전/후를 나란히** 보낼 것(PIL로 이어 붙이면 된다)
- **깃 개념을 배우는 중이다.** 커밋·푸시·브랜치·업스트림을 설명 없이 쓰지 말 것
- **판단을 물으면 선택지를 나열하지 말고 추천을 줄 것.** 다만 근거는 수치로
- **속도에 민감하다.** 오래 걸릴 일이면 **왜 오래 걸리는지 먼저 말하고** 시작할 것
- **범위를 스스로 좁힌다.** 「펴는 동작까지는 필요 없다」처럼 잘라 준다.
  **잘라 준 것은 그대로 받아들이고 다시 꺼내지 마라**
- **세션을 자주 새로 연다.** 그래서 인계문이 이 프로젝트의 생명줄이다

## 11. 지금 시작한다면

1. **4절 ① 폴드 두 크기.** 대략 15~20분. **각 크기를 다 본 뒤가 아니라
   한 크기 끝날 때마다 스크린샷과 함께 보고할 것**
2. 여유가 있으면 **4절 ② 픽셀 대조 6개.** 한 화면 끝날 때마다 보고
3. **4절 ③ 곁다리 두 건은 사용자에게 물어보고 시작할 것**
4. 끝나면 **기기 설정을 명시값으로 되돌릴 것**(2절 경고)
5. `main` 병합·푸시는 **사용자가 하자고 할 때까지 하지 마라**

**급한 파손은 없다. 4절 말고는 열려 있는 항목이 없고, 7절 표가 나머지를 전부 닫았다.**

## 12. 작업 후

`ui_tn수정.md` **맨 위**에 새 블록으로 기록할 것(**CRLF 보존**). 형식은 기존 블록을 따를 것 —
요청 / 판단 / 실제 변경 / **막힌 곳** / 검증 / 미결.
**틀렸던 판단은 숨기지 말고 새 블록에서 철회한다.** 이 문서 계보가 실제로 그렇게
운영돼 왔다(3번 항목 철회 · 「47곳」→「42곳」 정정 · 「남은 9곳」→ 실제 10곳 정정 ·
**「처방은 하나」→ 실제로는 둘 정정**).
