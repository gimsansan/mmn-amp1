# 나중계획안 — 음향학 문헌 기반 자극 설계 개선

작성일: 2026-08-24
상태: **계획만. 구현하지 않음.**

## 출처

- Jürgen Meyer, *Acoustics and the Performance of Music*, 5th ed. (Springer, 2009)
  - 1장 음향학 입문 — 음압, dB, 청각 특성(역치, 등청감, 마스킹)
  - 3장 악기별 음색 특성 — 스펙트럼, 온셋/디케이
  - 4장 방사 지향 특성
  - 5~6장 실내음향 / 공연 공간
  - 7~9장 좌석 배치·편성·오페라 피트 → **앱과 접점 없음. 참고 대상 아님.**
- Plazak, Huron & Williams, "Fixed Average Spectra of Orchestral Instrument Tones",
  *Empirical Musicology Review* 5(1), 2010. SHARC DB 기반 23종 악기 1,338음 평균 스펙트럼,
  최종 132 하모닉. 연구용 표준 자극으로 공개됨.

## 현재 상태 (2026-08-24 코드 기준)

| 항목 | 현재 | 위치 |
|---|---|---|
| 파형 | sine 순음 only | `src/audio/pureTone.ts` (`osc.type = 'sine'`) |
| AM 자극 | sine 반송파 + 정현 AM, RMS 등화 있음 | `src/audio/amTone.ts` |
| 말소리 | 2포먼트 근사 (m/u/a/i + s/sh 잡음) | `src/training/ling6/ling6Synth.ts` |
| 게인 | `DEFAULT_GAIN = 0.15` 리니어 고정 | `src/audio/pureTone.ts` |
| 램프 | 30 ms 고정 / pitch2afc는 50 ms | `pureTone.ts`, `pitch2afc/constants.ts` |
| 난이도 축 | Δcents 하나 (pitch/freq), m 하나 (AM) | 각 `*Staircase.ts` |
| freq 범위 | 200 ~ 2000 Hz | `pitch2afc/constants.ts` `AUDIO.FREQ_MIN_HZ/MAX_HZ` |

---

## 1. 등청감 보정 (Meyer 1장) — 우선순위 최상

### 문제
`freq` 트랙은 200~2000 Hz를 쓰는데 게인은 주파수와 무관하게 리니어 0.15 고정이다.
등청감곡선상 200 Hz는 1 kHz보다 체감 라우드니스가 크게 낮다.
따라서 저역에서 측정된 역치 악화가 **주파수 변별력 저하인지 단순 가청도 저하인지 구분되지 않는다.**
= 지금 쌓이고 있는 역치 데이터의 해석이 흔들린다. 새 기능이 아니라 **기존 측정의 타당성 문제.**

### 계획
- `src/audio/loudness.ts` (신규) — 주파수 → 게인 배율 테이블 + 보간 함수.
  - 근거: Meyer 1장 등청감곡선 (또는 ISO 226 표). 어느 쪽을 쓸지는 착수 시 결정.
  - 40 phon 근처 곡선을 기준으로 삼는 안이 유력 (훈련 청취 레벨대).
- `playPureTone` / `playAmTone`에 `equalizeLoudness?: boolean` 옵션 추가. 기본값은
  **하위호환 위해 처음엔 false**, 파일럿 후 true 전환.
- 주의: 기존 저장된 역치 데이터와 보정 후 데이터는 **직접 비교 불가**.
  전환 시 세션 레코드에 보정 플래그를 남겨야 함 (`sessionStore.ts` 스키마 영향).

### 남은 판단
- 절대 dB SPL 캘리브레이션은 여전히 불가(기기·볼륨 미지). 어디까지나 **상대 보정**임을 명시할 것.

---

## 2. 복합음(하모닉) 자극 옵션 (Meyer 3장 + Plazak 2010)

### 근거
- 현재 sine 한 종뿐 → 생태학적 타당성 낮음. Plazak 논문이 지적한 그 딜레마 그대로.
- Plazak의 grand average spectrum은 하모닉 번호 상승에 따라 부드러운 지수 감쇠,
  에너지 대부분이 처음 몇 부분음에 집중. 말소리 평균 스펙트럼과도 유사.
- Meyer 3장은 악기군별(금관/목관/현/피아노/성악) 스펙트럼 특성을 준다 → 개별 음색 프리셋 근거.

### 계획
- `src/audio/harmonicTone.ts` (신규) — 하모닉 진폭 배열을 받아 오실레이터 스택 합성.
  - 또는 `PeriodicWave`(react-native-audio-api 지원 여부 확인 필요)로 오실레이터 1개 처리.
    **오실레이터 N개는 모바일에서 비용이 크므로 PeriodicWave 지원 여부를 먼저 확인할 것.**
- 계수 테이블 `src/audio/spectra.ts`:
  - `average`: Plazak 평균 스펙트럼 (-60 dB 컷 후 132 하모닉 → 앱에서는 가청 상위 N개만)
  - 악기 프리셋 몇 종 (Meyer 3장 근거) — 후순위
- 적용처: `pitch2afc` / `freq` 트랙의 자극 모드 선택.
  이미 pitch2afc에 `PEAK_GAIN_WAVE` / `PEAK_GAIN_VOICE` 두 모드 구분이 있으므로 그 축을 확장.

### 부수 효과 (설계 시 반드시 반영)
하모닉이 많으면 음고 단서가 늘어 **변별이 쉬워진다.** 즉 순음↔복합음은 난이도가 다르다.
- 계단식 역치를 모드별로 분리 저장해야 함. 섞으면 무의미.
- 반대로 이걸 이용해 "순음(어려움) ↔ 악기음(쉬움)" 난이도 축으로 쓸 수도 있음.

---

## 3. 온셋 시간 파라미터화 (Meyer 3장)

### 근거
- 램프가 상수(30 ms / 50 ms)로 박혀 있다.
- Meyer 3장은 악기별 실측 온셋을 준다 (금관 ~20 ms대, 활 현악 ~40–100 ms, 등).
  온셋은 음색 인지에 크게 관여하고 음고 인지에도 영향.

### 계획
- 1단계(저비용): `rampSec`를 이미 옵션으로 받고 있으므로, **상수를 자극 스펙 객체로 승격**만 해둔다.
  트랙별 자극 정의를 한곳에 모으는 리팩터. 기능 변화 없음.
- 2단계: 온셋 시간 자체를 훈련 변수로 쓰는 트랙(음색/어택 변별). 별도 기획 필요.

---

## 4. 마스킹 / SNR 난이도 축 (Meyer 1장)

### 근거
- 현재 난이도 축이 Δcents 하나뿐. 실생활 듣기 어려움의 주 원인은 **소음 속 듣기**다.
- Meyer 1장에 임계대역·마스킹 곡선이 정리돼 있어 마스커 대역/레벨 설계 근거가 된다.

### 계획
- `src/audio/masker.ts` (신규) — 광대역/대역제한 잡음 생성 + SNR 지정.
- 적용 우선순위: **WRS / Ling6 > freq / AM**.
  말소리 쪽이 전이 효과(실생활 개선)가 훨씬 크다. 보청기·CI 사용자 대상이면 특히.
- 계단식 확장: SNR을 두 번째 적응 축으로 둘지, 고정 레벨 블록으로 둘지 결정 필요.
  **동시에 두 축을 적응시키면 역치 해석이 어려워진다** — 처음엔 SNR 고정 블록 권장.

---

## 5. 잔향 (Meyer 5~6장) — 후순위

- 짧은 잔향을 섞으면 난이도가 오르고 실환경에 가까워진다.
- 구현 비용(IR 컨볼루션 또는 피드백 딜레이 네트워크) 대비 이득이 1~4번보다 작다.
- 착수한다면 IR 파일 없이 알고리즘 리버브(짧은 RT60 0.4~1.2 s)로 시작.

## 6. 지향성 / 공간 큐 (Meyer 4장) — 조건부

- 현재 모노 출력. 방향 분별 트랙을 새로 만들 때만 의미가 있다.
- 헤드폰 착용이 전제 조건이라 사용자 안내 부담이 생긴다.
- 새 트랙 기획이 생기기 전까지는 보류.

---

## 착수 순서

1. **등청감 보정** — 기존 측정값의 해석을 바로잡는 문제. 새 기능보다 먼저.
2. **복합음 옵션** — 생태학적 타당성 + 난이도 축 확보. (PeriodicWave 지원 확인이 선행)
3. **마스킹/SNR** — 말소리 트랙부터.
4. 온셋 파라미터화는 1~3 진행 중 리팩터로 묻어서 처리.
5. 잔향·지향성은 별도 기획이 생기면 재검토.

## 미결 사항

- [ ] `react-native-audio-api`의 `PeriodicWave` / `ConvolverNode` 지원 여부 확인
- [ ] 등청감 보정 근거를 Meyer 1장 곡선으로 할지 ISO 226으로 할지
- [ ] 보정 전/후 역치 데이터 호환 처리 (`sessionStore.ts` 스키마)
- [ ] Plazak 논문 부록 스펙트럼 데이터 실물 확보 (SHARC DB 접근 경로)
