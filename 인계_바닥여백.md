# 인계문 — `color_ui` 브랜치 (바닥 여백 정리 시점)

> **읽는 사람**: 이 대화를 처음 보는 세션.
> **한 줄**: 글씨 크기(`font_scale`) 파손의 **근본 원인을 찾아서 닫았다.** 남은 일은
> 그 원인을 **나머지 9개 화면에 똑같이 적용**하는 것이다.
> **먼저 할 일**: 이 문서를 끝까지 읽고 `ui_tn수정.md` **맨 위 블록**을 읽을 것.
> **가장 중요한 것**: 3절(다음 할 일)과 6절(다시 열지 말 것). 이 갈래는 닫은 판단을
> 다시 여는 낭비를 반복했다. **이번에 3개 항목이 닫혔다. 다시 열지 마라.**

---

## 0. 프로젝트가 뭔지

- Expo/React Native 앱. 난청 관련 **듣기 연습**(청력 검사가 아니다) — 링 6 · 소리 높낮이 ·
  단어 듣기 · 떨림 4탭
- **대상 사용자가 고령·난청자다.** 이게 판단의 기준선이다. 「세련됨」보다
  「보이는가·누를 수 있는가」가 이긴다
- **안드로이드 전용.** `ios/` 없음. RN 0.86 · newArch · 라이트 전용(다크 모드는 2026-08-11 안 함)
- 팔레트 이름은 「Clean Clinical」

## 1. **실기기가 붙어 있다. 이게 이 갈래의 전제다**

```
adb 경로     /d/Android/Sdk/platform-tools/adb      ← PATH에 없다. 전체 경로로 부를 것
기기         RF8MC0XZYLK  SC-01M (갤럭시 노트10+)
규격         w411dp · 420dpi · 1080x2280
앱           com.rnhear.app (개발 빌드 — 이게 중요하다. 7절 참고)
Metro        8081에서 돌고 있다 — 화면에 보이는 게 워킹 트리의 코드다
```

### 이 갈래의 핵심 도구

```bash
ADB=/d/Android/Sdk/platform-tools/adb
$ADB shell settings put system font_scale 1.3    # 파손 재현
$ADB shell settings put system font_scale 1.0    # 반드시 되돌릴 것
$ADB exec-out screencap -p > shot.png
$ADB shell input tap <x> <y>      # 하단 탭 y=2018 · x = 134/403/672/943
```

**끝나면 `font_scale`을 1.0으로 되돌려라.** 사용자의 실제 폰이다.
(지금은 1.0으로 되돌려 놓은 상태다.)

## 2. 저장소 상태

- 브랜치 **`color_ui`** · 워킹 트리 clean
- **`origin/color_ui`와 동기. 푸시 끝났다**(2026-08-22)
- **`main`에는 안 들어갔다.** `main` 대비 68 커밋 앞선다. PR 없음

```
fd23835  글씨 크기 — 듣기 준비 화면을 스크롤에 넣는다     ← 이번 작업
592679e  인계문 — 글씨 크기(font_scale) 작업
f453f28  작업 로그 — 실기기 확인 · 글씨 크기 파손
321c8c5  글씨 크기 — idle 히어로를 스크롤에 넣는다 (3개 화면)
...
a1a5bd7  가독성 — 색 4개 + 글자 42곳     ← **되돌리지 말 것.** 6절
```

> **직전 인계문의 「2 커밋 앞선다」는 낡은 정보였다.** 원격 상태를 말하기 전에
> **반드시 `git fetch origin`을 먼저 돌려라.** 안 하면 틀린 숫자를 말하게 된다.

### 문서

| 파일 | 무엇 |
|---|---|
| `ui_tn수정.md` | **작업 로그. 누적 문서** — 새 기록은 **맨 위**에. 기존 블록은 고치지 않는다. 판단이 뒤집히면 새 블록에서 닫는다. CRLF다 |
| `인계_글씨크기.md` | 직전 인계문. **①③④가 이 문서에서 닫혔다.** 그 문서의 미해결 목록을 그대로 믿지 말 것 |
| `인계_color_ui.md` · `인계_컬러.md` | 더 앞 인계문. 역사용 |

## 3. **지금 할 일 — 하나뿐이고, 원인은 이미 손에 있다**

### ★ 바닥 여백 정리 — 9개 화면에 같은 처방

**`BottomTabInset`(안드로이드 80dp)은 이 앱에서 죽은 값이다.**
하단 탭이 `NativeTabs`(네이티브 — `src/components/app-tabs.tsx`)라 **화면 영역이 이미
탭바 위에서 끝난다.** 탭바 높이를 또 더하면 그만큼 헛돈다.
`SafeAreaView`의 하단 inset(≈48dp)도 같은 이유로 탭바가 덮는 자리라 헛돈다.

**그동안 안 보였던 이유**: 내용이 상자를 **넘쳐 흐르며** 그 여백을 먹고 있었다.
`ScrollView`를 씌워 정직하게 자르기 시작하면 그제야 드러난다.

| 대상 | 수 | 회수량 |
|---|---|---|
| `BottomTabInset`을 더하는 화면 | **10곳** (`ListeningCheckScreen`은 처리 완료 → **남은 9곳**) | 80dp |
| 그중 `SafeAreaView`에 `edges`를 안 준 화면 | **5곳** — `AmSessionScreen` · `FreqSessionScreen` · `PitchCompareScreen` · `PtaSessionScreen` · `WrsTabScreen` | +48dp |

```
paddingBottom: BottomTabInset + N   →   paddingBottom: N      (BottomTabInset import 제거)
<SafeAreaView style={...}>          →   <SafeAreaView edges={["top","left","right"]} style={...}>
```

`Ling6SessionScreen` · `WrsSessionScreen` · `WrsBingoScreen` · `WrsTwoCharScreen` ·
`WrsVoiceGuideScreen` · `StatsScreen`은 **이미 `edges`가 있다.** 그쪽은 `BottomTabInset`만
빼면 된다.

**검증은 화면마다 1.0과 1.3을 둘 다 찍어 대조할 것.** 1.3만 보고 고치면 1.0이 깨진다 —
이 갈래가 두 번 겪었다. 폰이 붙어 있는 지금이 가장 싸다. 대략 30~40분짜리 일이다.

> **범위는 확정됐다(2026-08-22).** 사용자가 「9개 화면 전면 정리」를 골랐고,
> 「새 창에서 해줘」라고 해서 이 인계문으로 넘겼다.
> **다시 묻지 말고 바로 시작할 것.** 다만 화면마다 1.0/1.3 대조는 건너뛰지 마라.

### 곁다리 — WRS 요약 pill (**막지는 않는다**)

`WrsSessionScreen` 요약, 1.3: 「25개를 다 고르지 않아 기록에는 안 남겼어요」 알림이
「다시 연습」 버튼에 절반 가려진다. **버튼 3개는 항상 온전하고 스크롤하면 다 보인다.**
위 바닥 여백 정리(이 화면은 104dp가 헛돈다)를 하면 **저절로 풀릴 가능성이 높다.**
따로 손대기 전에 여백부터 정리하고 다시 볼 것.

## 4. 이번에 고친 것 — 왜 그렇게 고쳤나

`fd23835`. `ListeningCheckScreen` 한 파일.

`font_scale` 1.2부터 「연습 시작」·「뒤로 가기」가 화면 밖으로 밀려 눌리지 않았다.
소리 높낮이·떨림 두 경로가 이 화면을 거치므로 **연습을 시작할 방법이 없었다.**

```
<SafeAreaView edges={["top","left","right"]} style={styles.safeArea}>
  <ScrollView style={styles.scroll} contentContainerStyle={styles.scrollContent}>
    header · 카드 2장 · extra · 면책 · error
  </ScrollView>
  <View style={styles.actions}>   ← 스크롤 밖 고정. `marginTop:"auto"` 없음
scroll:        { flex: 1 }
scrollContent: { flexGrow: 1, gap: Spacing.three - 4 }
paddingBottom: Spacing.three      ← `BottomTabInset` 제거
```

**앞선 두 시도가 진 진짜 이유는 `marginTop:"auto"`가 아니었다.**
스크롤을 씌우자 헛돌던 ~144dp가 드러나 버튼이 250px 위로 올라왔고, 앞선 세션들은 그걸
「1.0 회귀」로 보고 **되돌렸다.** 되돌릴 게 아니라 **여백을 고쳤어야 했다.**

실기기 대조: **1.0 수정 전과 동일**(면책 문구까지 보임) · **1.3 버튼 온전** ·
**2.0(안드로이드 최대)에서도 버튼 두 개가 다 눌린다.**

## 5. 검증 — 기준선

```
npx tsc --noEmit -p tsconfig.json    → exit 0
npx eslint src --ext .ts,.tsx        → 0 errors / 경고 14
npx jest                             → 23 suites · 230 tests 통과
```

경고 14건은 저장소 테스트 4개의 `import/first`이고 **손대지 않기로 한 것**이다.
**`eslint --fix`를 전체에 돌리지 말 것** — 그 파일들의 `jest.mock` 아래 import를 위로
끌어올려 의도를 깬다. 한 번 겪고 되돌렸다.

## 6. **다시 열지 말 것**

| 항목 | 상태 | 다시 열 조건 |
|---|---|---|
| **`ListeningCheckScreen` 파손** | **닫힘.** 1.0/1.3/2.0 실기기 확인 완료 | 없음 |
| **`allowFontScaling` 상한** | **두지 않기로 닫음.** 훑은 결과 막는 곳 0 · 잘리는 곳 1. 고령 사용자가 키운 글씨를 앱이 거부하는 거래를 할 근거가 없다 | 안 본 화면에서 **막는** 파손이 새로 나올 때 |
| **`chartMuted` 2.21:1** | **현행 유지로 닫음.** 7절 방법으로 실제 렌더를 봤다. 근거는 아래 | 없음 |
| **안드로이드 그림자**(`Shadows.card`) | **닫힘.** `elevation: 2`/`4`가 이미 있다(`theme.ts:132,140`) | 없음 |
| **`SessionModeToggle`·`SessionTrendPanel` 글자 넘침** | **닫힘.** 411dp·1.0·1.3 전부 멀쩡 | 없음 |
| **`a1a5bd7` 되돌리기** | **하지 말 것.** 파손은 14px이 아니라 시스템 배율 문제였다 | 없음 |
| **카드 경계 대비**(`border` 1.20) | **닫힘 — 현행 유지** | 실사용자가 「카드가 어디서 끝나는지 모르겠다」고 말할 때 |
| **배경을 낮춰 카드를 띄우는 안** | **막힘.** `textMuted`가 AA 아래로 떨어진다 | 없음 |
| **다크 모드** | **안 하기로 결정**(2026-08-11) | 없음 |
| **글자 크기(px) 재조정** | **끝남** | 없음 |
| **「안 본 화면에도 파손이 있다」는 가정** | **틀렸다.** 6곳을 1.3으로 훑어 **막는 파손 0**. 파손 4개는 전부 같은 처방(idle 히어로 무스크롤)을 쓰던 자리였다 | 없음 |
| **「설정 화면」** | **그런 화면은 없다.** `find src -iname "*setting*"` 0건. 우하단 톱니는 **Expo dev-menu**다 | 없음 |

### `chartMuted`를 현행 유지로 닫은 근거 (수치)

```
chartMuted(#94A7BB) vs 트랙(borderSubtle #EEF3F8)  2.21:1
chartMuted          vs 카드(surface #FFFFFF)        2.47:1
highlight(#F36C1C)  vs 트랙                         2.70:1
highlight           vs chartMuted (막대끼리)        1.22:1   ← 여기가 핵심
```

- 실기기에서 회색 막대(/m/ 2회, /ʃ/ 3회)는 트랙 위에서 **또렷하게 읽힌다.** 부족하지 않다
- 직전 인계문은 「부족하면 `#8A9EB4`(2.47)까지 올릴 수 있다」고 적었다. **올리면 안 된다.**
  `chartMuted`를 더 진하게 만들면 `highlight`(2.70)와 **가까워져 둘을 구별하기 더 어려워진다**
- `highlight`와 `chartMuted`는 **광도 차이가 1.22:1뿐이고 색상(hue)으로만 갈린다.**
  다만 앱이 이미 **막대 높이 · 라벨 굵기(`text` vs `textMuted`) · 문장**
  (「요즘 /s/가 아쉬운 날이 많아요」)으로 **중복 부호화**하고 있어 색만으로 읽지 않아도 된다.
  색을 손댈 게 아니라 이 중복 부호화를 유지하는 것이 답이다

## 7. 이 저장소에서 걸려 넘어지는 것들

1. **줄바꿈이 파일마다 다르다.** 직전 인계문의 「`theme.ts`와 `ui_tn수정.md`는 CRLF,
   `.tsx`는 LF」는 **틀렸다.** `ListeningCheckScreen.tsx`도 **CRLF**다.
   **파일마다 `file <경로>`로 확인하고 줄바꿈을 보존할 것.** 안 맞으면 조용히 실패한다
2. **큰 한글 문서를 Bash 히어독이나 `node -e`에 인라인으로 넣지 말 것.** 이스케이프가
   깨진다. **스크립트를 Write 도구로 파일에 쓰고 `node <파일>`로 실행할 것**
3. **Node에서 `/c/Users/...` 경로가 안 먹는다** — `C:/Users/...`로 줄 것
4. **`adb`가 PATH에 없다.** `/d/Android/Sdk/platform-tools/adb`
5. **Git Bash가 `adb` 인자의 `/sdcard/...`·`/data/...`를 윈도우 경로로 바꿔 버린다.**
   `export MSYS_NO_PATHCONV=1`을 먼저 줄 것. 안 주면
   `failed to stat remote object 'C:/Program Files/Git/sdcard/...'`가 난다
6. **화면 오른쪽 가장자리의 잘린 흰 카드는 우리 앱이 아니다** — 삼성 엣지 패널 핸들
7. **비교 기준선을 먼저 말할 것.** 원격 이야기는 **`git fetch` 먼저**
8. **`git log --oneline --branches --not --remotes`** — 푸시 안 된 커밋을 뽑는다

### ★ 기기 데이터를 심고 보는 방법 (③을 이걸로 뚫었다)

개발 빌드라 `run-as`가 된다. 저장소는 AsyncStorage = SQLite `RKStorage`.
**기기에 `sqlite3` 바이너리는 없지만, Node 24의 내장 `node:sqlite`로 처리하면 된다.**

```bash
export MSYS_NO_PATHCONV=1
ADB=/d/Android/Sdk/platform-tools/adb

# 1) 백업 — exec-out이라야 바이너리가 안 깨진다
$ADB shell am force-stop com.rnhear.app
$ADB exec-out run-as com.rnhear.app cat databases/RKStorage > RKStorage.orig

# 2) node:sqlite로 catalystLocalStorage(key,value) 수정  ← 값은 JSON 문자열

# 3) 되돌려 넣기 — run-as는 /data/local/tmp를 읽을 수 있다
$ADB push RKStorage.new /data/local/tmp/RKStorage.new
$ADB shell "run-as com.rnhear.app cp /data/local/tmp/RKStorage.new databases/RKStorage"
$ADB shell "run-as com.rnhear.app sh -c 'rm -f databases/RKStorage-journal'"
$ADB shell monkey -p com.rnhear.app -c android.intent.category.LAUNCHER 1
```

**키**: `training.ling6Daily.v1` · `training.ling6History.v1` · `training.sessionHistory.v1` ·
`training.wrsSessions.v1` · `training.twoCharSessions.v1`

**아쉬움 패널은 `training.ling6Daily.v1`에 레코드 7건이 있어야 뜬다**
(`LING6_WEAKNESS_WINDOW = 7`, `ling6Session.ts:206`). 4회 이상이 강조색이다.

> **사용자의 실제 기록이다. 반드시 백업하고, 끝나면 되돌리고, 되돌린 값을
> 기기에서 다시 읽어 확인할 것.** 이번엔 그렇게 했고 원본 2건(8/22·8/20)이 그대로다.
> **손대기 전에 사용자에게 허락을 받을 것.**

## 8. 사용자에 대해

- **기록에 의지한다.** 「나는 기록한 내용에 의지하는 편」이라고 직접 말했다.
  중요한 판단은 반드시 `ui_tn수정.md`에 남길 것. 대화에만 두면 잃는다
- **시각적 설명을 원한다.** 색·레이아웃 이야기는 터미널 텍스트로 전달되지 않는다.
  **실기기 스크린샷 대조**가 가장 잘 통한다. 수정 전/후를 나란히 보낼 것
- **깃 개념을 배우는 중이다.** 커밋·푸시·브랜치·업스트림을 설명 없이 쓰지 말 것
- 판단을 물으면 **선택지를 나열하지 말고 추천을 줄 것.** 다만 근거는 수치로
- **속도에 민감하다.** 오래 걸릴 일이면 **왜 오래 걸리는지 먼저 말하고** 시작할 것
- **세션을 자주 새로 연다.** 그래서 인계문이 이 프로젝트의 생명줄이다

## 9. 지금 시작한다면

1. **바닥 여백 정리 9개 화면**(3절). 유일하게 남은 실질 작업이고 **범위는 이미 확정됐다.**
   묻지 말고 시작할 것. 오래 걸리는 이유(화면마다 1.0/1.3 대조)를 먼저 말하고 들어갈 것
2. 그 뒤 **WRS 요약 pill**을 다시 볼 것 — 1번으로 저절로 풀릴 수 있다
3. 그 다음이 **`main` 병합**. 68 커밋이 밀려 있다. PR:
   `https://github.com/gimsansan/mmn-amp1/pull/new/color_ui`

**3절 말고는 열려 있는 항목이 없다.** 6절 표가 나머지를 전부 닫았다.

## 10. 작업 후

`ui_tn수정.md` **맨 위**에 새 블록으로 기록할 것. 형식은 기존 블록을 따를 것 —
요청 / 판단 / 실제 변경 / **막힌 곳** / 검증 / 미결.
**틀렸던 판단은 숨기지 말고 새 블록에서 철회한다.** 이 문서 계보가 실제로 그렇게
운영돼 왔다(3번 항목 철회 · 「47곳」→「42곳」 정정 · 인계문 ③ 오진 정정 ·
이번엔 **「안 본 화면에도 파손이 있다」는 가정과 「`.tsx`는 LF」를 정정**).
