# UI 수정 로그 — 누적

> **규칙**: 이 문서는 **누적**한다. 새 기록은 **맨 위**에 추가하고, 기존 블록은 지우거나 덮어쓰지 않는다.
> **범위**: 화면·UI 변경 작업의 실행 기록(무엇을 왜 어떻게 바꿨나). 결정의 정본은 `docs/` 아래 문서다.

---

## 2026-08-22 · **링 6 · 단어 듣기 점검 — 7항목 통과 (코드 변경 없음)**

**브랜치**: `color_ui` · **코드 수정 없음** · 실기기 확인 + 코드 대조

### 왜 했나

`docs/improvement-backlog.md`는 **2026-08-07 `two_feat` 기준**이라 **4탭 이후에 들어온
링 6 · 단어 듣기는 점검 대상에 아예 없었다.** 백로그에 「안 고침」으로 남아 있던 게
아니라 **한 번도 확인받은 적이 없던 갈래**다.

### 점검 항목과 결과 — 7/7 정상 (사용자 실기기)

| # | 갈래 | 확인한 것 | 결과 |
|---|---|---|---|
| 1 | 링 6 | 하루에 두 번 하면 기록이 덮이는지 쌓이는지 | ✅ 정상 |
| 2 | 링 6 | 「못 들었어요」가 제대로 기록되는지 | ✅ 정상 |
| 3 | 링 6 | 6음을 다 안 고르고 중지했을 때 | ✅ 정상 |
| 4 | 단어 듣기 | TTS가 느리거나 실패할 때 | ✅ 정상 |
| 5 | 단어 듣기 | 25개를 다 안 채우고 끝냈을 때 | ✅ 정상 |
| 6 | 단어 듣기 | 두 글자 목록이 다음으로 넘어가는지 | ✅ 정상 |
| 7 | 단어 듣기 | 빙고에서 기회를 다 썼을 때 | ✅ 정상 |

소리는 사용자가 직접 듣고 판정했다. **`dumpsys`로는 판정되지 않는다** — 이 갈래의
점검은 앞으로도 사용자 손이 필요하다.

### 코드 대조 — 관찰과 코드가 일치했다 (링 6 세 항목)

「정상으로 보인다」와 「의도된 동작이다」는 다르다. 기록이 사라지는 경로가 없는지
링 6 세 항목만 코드로 맞춰 봤다.

| # | 근거 | 내용 |
|---|---|---|
| 1 | `ling6Store.ts:133-136` 주석 · 테스트 「같은 날은 덮어쓰고 다른 날은 쌓인다」 | **날짜 upsert가 설계다.** 같은 날 두 번 하면 덮어쓴다 — 가로축이 세션 횟수로 늘어나지 않게 한 것 |
| 2 | `byPhoneme`에 `false`로 남고 `passCount`에서 빠짐 | 「못 들었어요」도 **기록되는 답**이지 무응답이 아니다 |
| 3 | `Ling6SessionScreen.tsx:228-233` · `savedRef` | 6음 미완이면 **저장을 막고** 「6음을 다 고르지 않아 날짜 기록에는 안 남겼어요」 안내. `savedRef`로 중복 저장도 막힘 |

`upsertLing6DailyRecord`는 불완전한 맵이 들어오면 **던진다**(`ling6Store.ts:141`).
화면이 막고 스토어가 또 막는 **이중 빗장**이다.

### 결론

**기록이 조용히 사라지는 경로는 없다.** 이 갈래의 점검은 닫는다.
남은 것은 백로그의 미구현·보류 항목과 「소리 높낮이 뒤로가기 확인 없이 나가기」다.

---

## 2026-08-22 · **「중지」가 소리를 안 끊던 것 · 0문항 세션 저장 · 떨림 뒤로가기**

**브랜치**: `color_ui` · **8개 파일 수정** · **아직 커밋 안 함**(사용자가 소리를 직접
들어 보고 커밋하기로 함)

### 요청

사용자가 `docs/improvement-backlog.md`의 **P0-1**(「중지」 미작동 · 기록 중복 저장)을
직접 실기기로 확인했다. 결과: **① 남은 소리가 난다 ② 「연습 끝내기」 모달이 뜬다
③ 연습 횟수는 1인데 결과 화면은 0.**

### ★ 진단 — P0-1은 고쳐져 있었다. **나중에 붙은 모달이 같은 증상을 되살렸다**

`goSummary`의 `abortRef.current = true`(2026-08-07 수정)는 **지금도 유효하다.**
그 뒤에 오탭 방지용 `confirmEndSession`이 들어오면서 흐름이 이렇게 됐다.

```
중지 누름 → Alert 「연습 끝내기」 표시
              ↑ 여기서 멈춰 있는 동안 시행이 계속 굴러가고 소리가 난다
          → 「끝내기」 → onEndManual → goSummary → 그제야 끊긴다
```

즉 **P0-1의 수정이 무효가 된 게 아니라, 중단 신호가 걸리는 시점이 뒤로 밀린 것**이다.
7개 화면이 전부 이 모달을 쓴다. 사용자가 본 ②는 고장이 아니라 이 모달이고,
**①의 직접 원인**이다.

### 고친 것 1 — 「중지」는 소리를 **먼저** 끊는다 (7개 화면)

`confirmEndSession(onConfirm, hooks)`에 훅 두 개를 뒀다.

| 훅 | 언제 | 하는 일 |
|---|---|---|
| `onOpen` | 대화상자 **띄우기 직전** | `abortRef` + 재생 중단 |
| `onCancel` | 「취소」·바깥 눌러 닫음 | 채점 안 된 시행을 **다시** 낸다 |

취소 경로가 없으면 화면이 재생 상태에 멈춘 채 남는다. **`onOpen`을 쓰면 `onCancel`도
반드시 같이 쓸 것.**

### ★ 실기기에서 잡은 것 — 안드로이드는 **버튼으로 닫아도 `onDismiss`가 불린다**

처음 구현은 `onDismiss: hooks?.onCancel`을 그대로 걸었다. 그랬더니 「끝내기」가
`onConfirm`과 `onCancel`을 **둘 다** 실행해, **요약 화면으로 넘어간 뒤 다음 문제가
재생됐다.** 실기기에서 실제로 그렇게 났다(요약 화면인데 오디오 스트림이 살아 있었다).
→ `settled` 빗장을 걸어 먼저 정해진 쪽 하나만 실행한다.

### ★ 세대 카운터 — `abortRef` 하나로는 두 루프를 구분할 수 없다

취소로 시행을 다시 틀면, 끊긴 **이전** 재생 루프가 `abortRef`가 `false`로 돌아간 것을
보고 되살아나 소리가 겹칠 수 있다. 7개 화면 모두 `runSeqRef`를 두고
`aborted = () => abortRef.current || runSeqRef.current !== seq`로 바꿨다.

빙고만 처방이 다르다. `playCue`는 **새 단서를 뽑으며 기회를 1 소모**하므로,
취소용으로 **같은 단서를 기회 소모 없이 다시 읽는** `replayCue`를 따로 뒀다.

### 고친 것 2 — 문항 0인 세션은 기록하지 않는다 (저장하는 3개 화면)

사용자가 남긴 화면이 그대로 증거였다. **「연습 0 · 정답 0 · 전환 0」인데
「기기에 기록했어요」.** 통계는 「떨림 기록 4회 · 푼 문항 35」.

- `goSummary`에 `trialCount <= 0` 가드를 넣고 「연습이 짧아서 기록에는 안 남겼어요」로
  바꿨다. **단어 듣기가 이미 쓰던 방식**(「25개를 다 고르지 않아…」)을 따랐다
- **평균 정답률은 원래도 안 흔들렸다** — `총 정답 / 총 문항`이라 0문항은 분모에 안 들어간다.
  부풀려지던 것은 **연습 횟수**뿐
- **링 6는 이미 막혀 있었다**(「6음을 다 고르지 않아…」). 손대지 않았다

> **정정**: 처음에 「저장하는 화면은 떨림·다른 음 찾기 둘」이라고 말했는데 **틀렸다.**
> `appendPitch2SessionSummary`가 이름에 숫자가 들어가 내 검색에서 빠졌다.
> **높낮이 비교까지 셋**이다.

### 고친 것 3 — 떨림 탭의 막힌 길 (사용자가 지목)

사용자: 「떨림탭에서 뒤로가기가 있어야 연습모드 토글이 선택 가능하니까 필요하지」.
맞는 지적이고 **실제로는 더 나빴다.**

- 「귀풀기/연습」 토글은 **idle에만** 있다(`AmSessionScreen:362`)
- 요약 버튼이 **「다시 연습」 하나뿐**이었다 — 「뒤로 가기」는 `onBack`이 올 때만
  그려지는데 `AmTabScreen`이 안 넘긴다
- 「다시 연습」은 **직전 모드 그대로** 시작한다 · 듣기 준비는 첫 시작 전에만 뜬다
- 시스템 뒤로가기는 **무반응**이었다(`AmTabScreen`은 통계·듣기 준비일 때만 잡는다)

→ **앱을 껐다 켜기 전까지 떨림의 모드가 잠겼다.** 귀풀기로 한 번 시작하면
기록이 남는 「연습」으로 못 바꾼다.

고친 방식(사용자가 **「떨림만 고친다」**로 범위를 잘라 줬다):

- 요약에 **「처음으로」** 버튼(→ idle). 단어 듣기가 쓰는 방식과 같다
- 시스템 뒤로가기 처리 추가 — **연습 중이면 「중지」와 같게**(확인 → 요약, 기록 보존),
  **요약이면 idle로**. 링6·단어처럼 버리게 하지 않은 이유는, 떨림의 「중지」가 이미
  기록을 남기고 있어 실수로 뒤로가기를 눌렀을 때만 연습이 사라지는 게 이상하기 때문

### 갈래마다 다른 뒤로가기 (지금 상태 · 통일은 **안 하기로** 했다)

| 갈래 | 연습 중 시스템 뒤로가기 |
|---|---|
| 소리 높낮이(높낮이 비교·다른 음 찾기) | **확인 없이** 고르기 화면으로. 세션이 버려진다 |
| 링 6 · 단어 듣기 3종 | 확인 모달 → `resetRun`(버림) |
| 떨림 | 확인 모달 → 요약(**기록 보존**) · 요약에서 한 번 더 → idle |

사용자가 **「떨림만 고친다」**고 정했다(2026-08-22). 소리 높낮이의 「확인 없이 나가기」는
**열려 있는 항목이다.**

### 막힌 곳

1. **소리가 실제로 끊기는지 나는 확인할 수 없다.** `dumpsys media.audio_flinger`의
   `Standby`를 지표로 쓰려 했는데 **못 쓴다** — 앱이 `AudioMmapOut` 스트림을 계속
   열어 두어, 소리를 내든 무음을 쓰든 똑같이 활성으로 보인다.
   **판정은 사용자가 귀로 해야 한다.** 내가 확인한 것은 로직·화면·기록뿐이다
2. **떨림 뒤로가기가 콜드 스타트 직후 한 번 무반응이었다.** 같은 순서로 두 번째
   콜드 스타트에서는 정상이었고, `__DEV__` 로그로 보면
   `effect run: idle → playing → choose` 뒤 `pressed`까지 정상이다.
   **재현되지 않아 원인을 못 박았다.** JS가 덥혀지기 전에 키가 들어간 것으로 **추정**한다
3. 화면 이동 좌표를 미리 정해 두고 연속 탭하면 어긋난다(직전 블록 2번과 같다).
   한 단계마다 스크린샷으로 확인하며 갔다

### 검증

```
npx tsc --noEmit          → exit 0
npx eslint src            → 0 errors / 경고 14 (기준선 그대로)
npx jest                  → 23 suites · 230 tests 통과
```

실기기(411dp x 868dp) 확인:

| 화면 | 중지 → 취소 | 0문항 저장 |
|---|---|---|
| 다른 음 찾기 | 다시 재생 ✓ | 안 남김 · **기록 11회 유지** ✓ |
| 떨림 | 다시 재생 ✓ | 안 남김 · **기록 4회 유지** ✓ |
| 높낮이 비교 | 다시 재생 ✓ | 안 남김 ✓ |
| 링 6 | 다시 들려줌 ✓ | (원래 막혀 있음) |
| 한 글자 · 두 글자 | 다시 읽어 줌 ✓ | (원래 막혀 있음) |
| 단어 빙고 | 다시 읽어 줌 · **기회 17번 그대로** ✓ | (기록 안 남기는 화면) |

떨림 뒤로가기: 연습 중 → 확인 모달 ✓ · 요약 → idle로 가고 **토글이 다시 보인다** ✓

### 미결

- **소리가 즉시 끊기는지 귀로 확인** → 확인되면 커밋한다(사용자 결정)
- 소리 높낮이 갈래의 「확인 없이 나가기」 — 통일할지는 사용자가 정할 때
- `docs/improvement-backlog.md`의 나머지(P0-4 테스트 없음 · P2-3 실기기 미확인 ·
  P2-4/P2-5/P2-7 · P3-1 스플래시). **링6·단어 듣기는 그 백로그에 거의 안 들어 있다** —
  백로그가 2026-08-07 `two_feat`(3트랙 단일 홈) 기준이라 4탭 이후가 반영돼 있지 않다

---

## 2026-08-22 · **폴드 두 크기 실측 + 연습 중 7개 픽셀 대조 마무리**

**브랜치**: `color_ui` · **코드 변경 없음**(실기기 확인만) · 워킹 트리 clean

### 요청

「`인계_폴드.md` 읽어서 이어서」 — 그 문서 4절의 ① 폴드 정지 크기 2종,
② 안 끝낸 픽셀 대조 6개.

### ① 폴드 두 크기 — **파손 없음**

| 크기 | 흉내내는 법 | 결과 |
|---|---|---|
| **344dp x 882dp** 겉화면 | `904x2316@420` | 연습 중 7개 **전부 온전**. 잘림 없음 |
| **690dp x 829dp** 속화면 | `1812x2176@420` | 7개 **전부 도달 가능**. 빙고만 판이 넘치는데 **스크롤로 해결** |

**속화면 빙고가 이번 실측의 수확이다.** 폭이 690dp로 넓어지면 3×3 칸도 같이
커지는데 높이는 829dp로 줄어서, 판의 셋째 줄이 화면 아래로 넘어간다.
**`eaecb2a` 이전이었다면 거기에 닿을 방법이 아예 없었다.** 그 커밋이 「짧은
화면」뿐 아니라 **「넓고 짧은 화면」에서도 필요했다는 실증**이다.

곁다리로 본 것 두 가지(둘 다 **파손 아님**):

- `AmTabScreen`의 「귀풀기/연습」 토글은 **344dp에서는 안 잘린다.** 320dp에서만 잘린다
- `WrsBingoScreen` 시작 화면의 「비슷한 소리」는 344dp에서 **2줄**(320dp에서는 3줄),
  690dp에서는 **1줄**

`ListeningCheckScreen`도 690dp x 829dp에서 내용이 넘치지만 스크롤로 전부 닿는다.

### ② 픽셀 대조 — **7개 전부 끝났다. 예상은 6개 맞고 하나 틀렸다**

사용자 기기 규격 **411dp x 868dp · font_scale 1.0**에서, 같은 화면을 지금 코드와
`eaecb2a` 직전(`11b9804`) 코드로 각각 찍어 비교했다.

| 화면 | 다른 픽셀 | 카드·버튼 y 위치 |
|---|---|---|
| `AmSessionScreen` | 0.52% (직전 세션) | 완전 일치 |
| `Ling6SessionScreen` | 0.45% | 일치 (「못 들었어요」만 **1px**) |
| `PitchCompareScreen` | 0.30% | 완전 일치 |
| `FreqSessionScreen` | 0.26% | 완전 일치 |
| `WrsSessionScreen` | 0.35% | 완전 일치 |
| `WrsTwoCharScreen` | 0.45% | 완전 일치 |
| **`WrsBingoScreen`** | **12.73%** | **다르다 — 판이 커지고 가운데로 왔다** |

### ★ 철회 — 「나머지도 같은 결과가 나올 것」은 **틀렸다**

`인계_폴드.md` 4절 ②는 「`Am`에서 나온 결과와 나머지도 같을 것으로 본다」고 적었다.
**빙고는 달랐다.** 같은 판(산·삼·손)인데 흰 칸 영역이

```
이전  x 123..843  (폭 720px · 중심 483)   ← 화면 중심 540에서 왼쪽으로 57 치우침
지금  x 123..957  (폭 834px · 중심 540)   ← 가운데
```

**원인은 `tile`의 `flex: 1` + `aspectRatio: 1`이다**(`WrsBingoScreen.tsx` `tile`).
스크롤이 없던 이전에는 판이 남은 세로 공간에 눌렸고, 정사각형을 유지하려고
**가로도 같이 줄어들면서** `tileOuter: flex: 1`의 행이 왼쪽으로 몰렸다.
스크롤이 생긴 지금은 판이 제 높이를 쓸 수 있어 폭도 제자리를 찾는다.

**이것은 회귀가 아니라 눌려 있던 것이 펴진 것이다.** 되돌리지 마라 —
고령·난청 사용자에게 누를 칸이 커진 쪽이 낫고, 「중지」 위치는 그대로다
(이전·지금 모두 y 1762~1898).

### ★ 정정 — 줄바꿈 표

`인계_폴드.md` 9절 1번은 「`Am`·`Freq`·`Pitch` 세 화면은 원래부터 **LF**」라고 적어
뒀는데, **지금 `HEAD`의 blob은 7개 화면 전부 CRLF다.** `eaecb2a`가 CRLF로
커밋했다(`git show HEAD:<파일> | grep -c $'\r$'` 로 확인). 그래서
`git checkout HEAD -- <파일>`로 되돌려도 줄바꿈이 어긋나지 않는다.

### 막힌 곳

1. **`git stash`가 안 통한다.** 직전 세션은 변경이 워킹 트리에 있어서 `stash`로
   껐다 켤 수 있었지만, 지금은 `eaecb2a`로 커밋돼 있어 `stash`가 아무 일도 안 한다.
   → `git checkout 11b9804 -- <7개 파일>` 로 되돌리고, 끝나면
   `git checkout HEAD -- <7개 파일>` 로 원복했다. Metro가 12초 안에 리로드한다
2. **화면 이동 좌표를 미리 정해 두고 연속으로 탭하면 어긋난다.**
   「연습 끝내기」로 끝내면 **루트가 아니라** 그 종목의 시작 화면이나 **직전 탭**으로
   간다. 한 번은 통계 화면(`StatsScreen`)까지 갔다.
   → 한 단계마다 스크린샷으로 확인하며 갔다
3. **`Ling6`의 1차 대조가 무효였다.** 리로드 직후 화면이 「정답 보여 주는」 상태라
   지금 코드 쪽(문제 상태)과 상태 자체가 달랐다. 「다음」을 눌러 같은 상태로
   맞춘 뒤 다시 쟀다
4. 리로드해도 **문제·빙고판이 그대로 유지된다.** 덕분에 `Wrs` 3개도 글자가 같아
   순수 픽셀 대조가 됐다(`한 글자`만 예외라 y 위치로 봤다)

### 좌표 (다음 세션용)

```
1080x2280@420   탭 y=2018 · 링6 x=134 · 소리높낮이 x=403 · 단어듣기 x=672 · 떨림 x=943
                「연습 끝내기」의 「끝내기」  (893, 1253)
                단어듣기 카드  한글자 y=460 · 두글자 y=714 · 빙고 y=966  (x=540)
                시작 화면의 「연습 시작」 (540, 1640) · 「뒤로 가기」 (400, 1804)
                빙고 시작 화면  「비슷한 소리」 x=220 · 「쉬운 판」 x=540 · 「뒤로 가기」 x=840  (y=1826)
                ListeningCheck의 「연습 시작」 (290, 1820)

904x2316@420    탭 y=2073 · x = 111 / 336 / 563 / 789
1812x2176@420   탭 y=1937 · x = 242 / 682 / 1126 / 1570 · 「끝내기」 (1473, 1231)
```

### 검증

```
git status                 → clean (7개 파일 전부 HEAD와 동일)
file -b <7개 .tsx>         → 전부 CRLF
기기 설정 되돌림            → 1080x2280 · 420 · font_scale 1.0
```

코드를 안 고쳤으므로 `tsc`·`eslint`·`jest`는 `eaecb2a`의 결과가 그대로 유효하다.

### 미결 — **없다**

`인계_폴드.md` 4절 ③ 곁다리 두 건(`AmTabScreen` 토글 잘림 · 빙고 버튼 글자 쪼개짐)은
**사용자가 「둘 다 그냥 둔다」고 확정했다(2026-08-22).** 둘 다 **320dp x 568dp에서만**
나타나고, 토글은 잘려도 누를 수 있으며 버튼 글자 쪼개짐은 좁은 폭의 정상 줄바꿈이다.
고치려면 분기나 글자 크기 조정이 들어가는데, 그것이 이 앱을 튼튼하게 만든
「분기 없는 레이아웃」을 깎는다. **다시 열지 마라.**

---

## 2026-08-22 · **연습 중 화면 7개에 스크롤 — 짧은 화면에서 「중지」가 잘리던 것**

**브랜치**: `color_ui` · **7개 파일** · 실기기 320dp x 568dp 재현·수정·확인 완료

### 요청

「3절 시작해줘」 — `인계_반응형.md` 3절이 유일하게 열려 있던 항목.
그 전에 사용자가 **「반응형이 이제 다 끝난 것 같은데 또 긴 작업이 남았다고?」**라고
물었다. **「잰 것은 끝났고 고친 것은 아직」**이라고 구분해서 답했고,
사용자 기기(411dp x 868dp)에서는 안 깨지지만 **깨지는 조합(보급형 폰 + 큰 글씨)이
하필 이 앱의 대상 사용자 그대로**라서 하는 쪽을 추천했다.

### 파손 — **7개 전부 실기기에서 직접 봤다**

인계문은 `WrsSessionScreen` 하나만 눈으로 봤고 나머지 6개는 「같은 구조라서
같은 처방이 필요하다고 판단한 것」이라고 정직하게 적어 뒀다.
**이번에 7개를 전부 320dp x 568dp · font_scale 1.0에서 재현했다.**

| 화면 | 기본 글씨(1.0)에서의 증상 |
|---|---|
| `WrsSessionScreen` | 「중지」 아래쪽 잘림 |
| `WrsTwoCharScreen` | 「중지」 아래쪽 잘림 |
| `WrsBingoScreen` | **「중지」가 통째로 사라짐** |
| `Ling6SessionScreen` | **그림 6개 중 4개만 보이고 「쉬」·「스」·「못 들었어요」가 전부 화면 밖.** 고를 수조차 없다 |
| `PitchCompareScreen` | 보기 버튼 글자(「더 낮아요」/「더 높아요」)가 잘려 화살표만 보임 |
| `FreqSessionScreen` | 「중지」 잘림 |
| `AmSessionScreen` | 「중지」 잘림 |

**제일 심한 것은 `Ling6SessionScreen`이었다** — 인계문이 「제일 위험」이라 지목한
곳은 `PitchCompareScreen`이었는데, 실제로는 링6가 더 나빴다. 보기 자체를 못 고른다.

### 판단 — **한 처방이 아니라 두 처방이다**

인계문의 처방은 `ScrollView` `flex: 1` + `contentContainer` `flexGrow: 1`
(`ListeningCheckScreen`이 `fd23835`에서 쓴 것)이었다.
**그대로 7개에 쓰면 3개가 사용자 기기에서 회귀한다.** 두 갈래로 나뉜다.

| 갈래 | 화면 | `running`의 자리 배분 | 쓴 값 |
|---|---|---|---|
| ① | `Wrs` 3개 · `Ling6` | `promptArea`에 `flex` 없음 · `actions`에 `marginTop: "auto"` | **`flexShrink: 1`** |
| ② | `Am` · `Freq` · `Pitch` | `promptArea`가 `flex: 1`로 남는 자리를 먹어 가운데 정렬 · `actions`는 `marginTop: Spacing.two` | `flex: 1` + `flexGrow: 1` |

**①에 `flex: 1`을 쓰면 안 되는 이유**: 남는 자리를 스크롤이 다 먹어 버튼이
화면 아래로 내려간다. `flexShrink: 1`은 **자리가 남을 때 내용 높이 그대로 두고
모자랄 때만 줄어들어** 키 큰 화면의 배치를 건드리지 않는다.

**②에 `flexShrink`를 쓰면 안 되는 이유**: 스크롤이 자리를 넘겨주지 않으면
`promptArea`의 `flex: 1`이 자연 높이로 주저앉아 가운데 정렬이 무너진다.

즉 **「스크롤을 씌운다」는 한 문장이 화면마다 다른 값을 요구했다.**
근거는 각 파일 주석에 남겼다.

### 실제 변경

- 7개 화면의 `running` 블록(따로 흩어져 있던 것들)을 **`ScrollView` 하나로 묶었다.**
  버튼(`actions`/`SessionActions`)은 **스크롤 밖에 그대로** 뒀다
- `Ling6`는 `running` 블록이 3개였고 **「못 들었어요」도 보기의 하나라 스크롤 안에** 넣었다
- `Bingo`는 `promptArea` · `hud` · 3×3 판 · 오류문을 한 스크롤에 넣었다
- `contentContainer`의 `gap`은 각 화면의 컨테이너 `gap`과 **같은 값**으로 맞췄다
  (`Wrs`·`Ling6`는 `Spacing.three`, `Am`·`Freq`·`Pitch`는 `Spacing.two + 2`).
  감싸기 전후로 요소 사이가 벌어지지 않게 하려는 것

### **철회 — `Am`·`Freq`에 적혀 있던 주석**

두 파일에 이렇게 적혀 있었다:

> 요약만 스크롤한다 … **진행 중 화면은 선택지가 고정돼야 해서 감싸지 않는다.**

**이 판단을 철회했다.** 짧은 화면에서는 기본 글씨에서도 선택지가 넘쳐 버튼이
화면 밖으로 나간다. **고정이 지킬 게 없다.** 주석을 그 자리에서 고쳐 적었다.

### 막힌 곳 / 걸려 넘어진 것

- **오토 모드 분류기가 한 번 응답하지 않아** 기기 조작이 몇 분 막혔다.
  그동안 읽기 작업(나머지 화면 구조 파악)으로 돌렸다
- **720x1280 화면에서 Expo dev-menu 버튼이 「떨림」 탭과 겹친다.** 탭을 누르려다
  dev-menu를 열었다. `x=560`(탭 4번째 칸의 왼쪽)으로 피했다
- **작업이 길어져 사용자가 중간에 강제 종료했다.** 25분이라고 예상했는데
  실제로는 더 걸렸다. **화면마다 탭을 눌러 들어가는 왕복이 예상보다 비쌌다** —
  다음에 비슷한 일을 잡을 때 이 왕복 비용을 빼먹지 말 것

### 검증

```
npx tsc --noEmit -p tsconfig.json    → exit 0
npx eslint src --ext .ts,.tsx        → 0 errors / 경고 14   (기준선과 동일)
npx jest                             → exit 0 · 23 suites · 230 tests
```

실기기:

- **320dp x 568dp · 1.0**(`720x1280@360`) — **7개 화면 전부 파손 재현 → 수정 후 전부 통과.**
  스와이프로 실제로 스크롤되는 것과, 버튼이 스크롤과 무관하게 고정되는 것을 확인했다
- **411dp x 868dp · 1.0**(사용자 기기) — `AmSessionScreen`을 **수정 전/후 픽셀 대조**했다
  (`git stash`로 되돌려 같은 화면을 찍고 PIL로 비교).
  **보기 버튼의 위·아래 y가 1368/1660으로 완전히 일치**하고, 「중지」도 1735/1872로 같다.
  다른 픽셀은 0.52%인데 **전부 카드 그림자(`Shadows.card`)가 스크롤 경계에서
  잘린 것**이었다(20배 증폭해야 보인다). **배치 회귀 없음**
- 기기 설정 원복 확인: `wm size 1080x2280` · `wm density 420` · `font_scale 1.0`

### 남긴 것 — **그림자 잘림은 안 고친다**

`ScrollView`가 경계에서 자식의 그림자를 자른다. 없애려면 `contentContainer`에
여백을 줘야 하는데 **그러면 키 큰 화면에서 내용이 그만큼 밀려 진짜 회귀가 된다.**
1배에서 보이지 않는 차이를 없애자고 보이는 배치를 흔들 이유가 없다.

### 미결

- **나머지 6개 화면의 411dp 픽셀 대조** — `Am` 하나만 했다.
  같은 두 갈래 중 하나씩이라 위험은 낮지만 **안 한 것은 안 한 것이다**
- `WrsVoiceGuideScreen`은 이번에도 못 봤다(한국어 TTS가 있는 기기에선 안 뜬다).
  이 화면에는 `running`이 없어 이번 변경과 무관하다
- **곁다리로 본 것 두 가지** — 320dp x 568dp에서
  ① `AmTabScreen`의 「귀풀기/연습」 토글이 「연습 시작」 버튼에 잘린다
  ② `WrsBingoScreen` idle의 버튼 3개가 「비슷/한/소리」로 쪼개진다
  **둘 다 7개 목록 밖이고 이번에 안 고쳤다.** 누를 수는 있어 파손은 아니다
- `main` 병합 — 사용자가 미루라고 했다(2026-08-22)

---

## 2026-08-22 · **태블릿 — `MaxContentWidth` 800 → 560. 상수 한 줄, 분기 0**

**브랜치**: `color_ui` · **1개 파일**(`theme.ts`) · 실기기 태블릿·폰 대조 완료

### 요청

「태블릿은 안 쓰지만 대략적으로 태블릿에서 동작하게 해줄 수는 있을까?
내가 쓰지는 않지만 모바일 앱을 누군가 태블릿에서 사용할 가능성도 있기 때문이다.」

### 먼저 짚은 것 — **이미 「동작」은 했다**

901dp 태블릿에서 깨지거나 잘리거나 못 누르는 곳은 **없었다.** 문제는 *보기가 어색한 것*
이었다. 보기 칸이 `width: "48%"`라 384dp까지 늘어나 한 글자에 과하게 넓고, 목록 카드도
글씨가 넓은 카드 왼쪽에 덩그러니 놓였다. **파손이 아니라 인상의 문제**라고 구분해서 답했다.

### 판단 — 분기를 넣지 않고 상수 하나로 끝낸다

「태블릿 대응」의 흔한 처방은 폭에 따라 열 수를 바꾸는 브레이크포인트다.
**이 앱에는 넣으면 안 된다.** 여러 화면 크기에서 튼튼한 이유가 바로
**「분기 없이 흐르는 레이아웃」**이기 때문이다(직전 블록 7절). 분기를 넣는 순간
조합이 늘고 오늘 같은 실측을 화면마다 다시 해야 한다.

**대신 이미 있는 상한을 낮췄다.**

```
theme.ts   MaxContentWidth   800  →  560
```

**핵심은 폰이 320~411dp라는 것이다.** 411dp 이상 어떤 값으로 낮춰도
**폰에서는 이 값이 아무 일도 하지 않는다.** `maxWidth`는 그보다 넓을 때만 걸린다.
그래서 이 한 줄은 **태블릿만 바꾸고 폰은 수학적으로 무변화**다. 위험이 없다.

12개 화면이 전부 `maxWidth: MaxContentWidth`를 똑같이 쓰고 있어 한 줄이 전부에 닿는다.

### 값을 고른 근거 (실기기 901dp에서 세 값 대조)

| 상한 | 보기 칸 폭 | 인상 |
|---|---|---|
| 800(기존) | 384dp | 한 글자에 과하게 넓다 |
| **560(채택)** | **248dp** | 폰 비율에 가깝고 가운데 정렬이 또렷하다 |
| 480 | 209dp | 폰에 제일 가깝지만 큰 화면에서 살짝 옹색하다 |

**560으로 뒀다.** 고령 사용자에게는 터치 영역이 큰 쪽이 유리하므로, 늘어진 느낌만
걷어내고 크기는 남기는 선택이다. (참고: 411dp 폰의 보기 칸은 176dp다.)

근거를 `theme.ts` 주석으로 남겼다 — **「폭에 따라 열 수를 바꾸는 분기는 넣지 마라」**도
같이 적어 뒀다.

### 남긴 것 — 세로 여백은 손대지 않는다

태블릿에서는 여전히 가운데가 세로로 크게 빈다(내용 위, 버튼 아래라는 폰 배치 그대로).
**이건 태블릿에 올라간 폰 앱의 정상적인 모습이고 고치려면 배치를 새로 짜야 한다.**
사용자가 태블릿을 실제로 쓰겠다고 하기 전에는 하지 않는다.

### 검증

```
npx tsc --noEmit -p tsconfig.json    → exit 0
npx eslint src --ext .ts,.tsx        → 0 errors / 경고 14   (기준선과 동일)
npx jest                             → exit 0 · 23 suites · 230 tests
```

실기기:

- **901dp x 1442dp 태블릿** — 보기 칸 248dp, 목록 카드 정돈됨. 연습 중 화면 온전
- **411dp x 868dp 사용자 기기** — **수정 전과 픽셀 단위로 동일**(예상대로 무변화)
- 기기 설정 원복: `wm size 1080x2280` · `wm density 420` · `font_scale 1.0`

### 미결

- **`running` 화면 7개 스크롤**(`인계_반응형.md` 3절) — 여전히 열려 있다.
  **이 변경과 무관하다**(폭 상한은 세로 넘침과 관계없다). 짧은 화면에서는 기본 글씨에서도
  깨지므로 이게 더 급하다
- 태블릿 세로 배치 — 위 이유로 안 함
- `main` 병합 — 사용자가 미루라고 했다(2026-08-22)

---

## 2026-08-22 · **반응형 나머지 크기 실측 — 「360dp + 2.0에서만 깨진다」는 판정을 철회한다**

**브랜치**: `color_ui` · **코드 변경 없음**(실측과 문서만)

### 요청

「너는 내가 말한 반응형을 여러 크기의 화면 모바일 디바이스로 이해한 거 맞지?」
→ 맞다고 답하면서 **태블릿과 320dp는 안 재고 추론으로 적었다**고 밝혔다.
「지금 마저 재줘.」

### 잰 것

`wm size` + `wm density`로 네 가지 화면을 흉내냈다.

| 화면 | 흉내내는 법 | 결과 |
|---|---|---|
| **901dp x 1442dp** 태블릿 | `1200x1920@213` | **온전** |
| **411dp x 868dp** 사용자 기기 | 기기 그대로 | **1.0·1.3·2.0 전부 온전** |
| **360dp x 640dp** 보급형 | `720x1280@320` | 1.0·1.3 온전 → **2.0 파손** |
| **320dp x 568dp** 좁고 짧은 폰 | `720x1280@360` | **1.0에서 이미 잘림** → 1.3에서 「중지」 사라짐 |

### **철회 — 직전 블록의 파손 범위가 틀렸다**

직전 블록에 이렇게 적었다:

> 360dp x 640dp(보급형) · 1.0 / 1.3 → **전부 온전. 여유까지 있다**
> 360dp x 640dp · **2.0** → 파손

**「보급형 화면에서는 큰 글씨일 때만 깨진다」는 읽음이 틀렸다.**
320dp x 568dp에서는 **기본 글씨(`font_scale` 1.0)에서 이미 「중지」가 잘린다.**
1.3이면 아예 사라져 세션을 끝낼 수가 없다.

**지배 변수는 화면 «폭»이 아니라 «높이»(dp)였다.**

```
868dp 높이   1.0 · 1.3 · 2.0  전부 온전
640dp 높이   1.0 · 1.3 온전  →  2.0 파손
568dp 높이   1.0 이미 잘림   →  1.3 사라짐
```

폭은 320·360·411·901dp 넷 다 이 파손과 **무관했다.** 글씨 배율도 유일한 원인이 아니라
**「내용 높이 > 화면 높이」를 만드는 여러 입력 중 하나**일 뿐이었다.
`running` 블록에 스크롤이 없다는 사실이 원인이고, 그게 언제 드러나느냐의 문제다.

**처방은 안 바뀐다** — `running`을 `ScrollView`로 감싸는 그대로다. 다만
**「작은 화면 + 2.0이라는 드문 조합」이 아니라 「짧은 화면이면 기본 설정에서도」**
나는 파손이므로 **급한 정도가 올라간다.**

### 태블릿 — 추론을 실측으로 바꿨다

직전 블록에서 `MaxContentWidth: 800`을 코드만 보고 「태블릿에서 줄이 길어지지 않는다」고
적었다. **901dp 화면에서 실제로 확인했다** — 버튼 폭이 ≈757dp(= 800dp 상한 − 좌우 22dp
패딩)로 잡히고 화면 가운데 정렬된다. **상한이 실제로 돈다.**

다만 **태블릿용 배치는 아니다.** 보기 칸이 `width: "48%"`라 384dp까지 늘어나 한 글자에
과하게 넓고, 세로로는 가운데가 크게 빈다. **깨지지는 않으므로 지금 손대지 않는다.**
(고치려면 폭에 따라 열 수를 바꿔야 하는데, 그건 이 앱이 일부러 피해 온 「분기」다.
사용자가 태블릿을 쓰겠다고 하기 전에는 하지 마라.)

### 막힌 곳

- 기기 복구 명령이 앱 재기동을 기다리다 **2분 타임아웃**에 걸렸다.
  설정 자체는 먼저 적용돼서 별도 확인으로 복구를 검증했다 —
  `1080x2280` · `420` · `font_scale 1.0`. **화면으로도 확인했다.**
- `wm size reset`/`wm density reset`은 여전히 쓰면 안 된다(사용자가 해상도를 낮춰 쓴다).
  이번에도 명시값으로만 되돌렸다.

### 문서 반영

`인계_반응형.md` 5곳을 고쳤다 — 3절 증상(높이가 지배 변수라는 실측 표) ·
3절 검증 조합(320dp x 568dp · 1.0 추가) · 7절 표(네 크기 + 흉내내는 법) ·
7절 도입 · `MaxContentWidth` 줄을 「추론」에서 「실측」으로.

### 검증

코드 변경 없음. tsc/eslint/jest는 직전 기준선 그대로
(tsc 0 · eslint 0 errors/경고 14 · 23 suites · 230 tests).
기기 설정 복구 확인: `wm size` Override `1080x2280` · density `420` · `font_scale 1.0`.

### 미결

- **`running` 화면 7개 스크롤** — `인계_반응형.md` 3절. **급한 정도가 올라갔다**
- 태블릿 배치(보기 칸이 과하게 넓음) — **깨지지 않으므로 안 함.** 사용자가 말할 때만
- `main` 병합 — 사용자가 미루라고 했다(2026-08-22)

---

## 2026-08-22 · **인계문 — 반응형 구멍을 새 세션으로 넘긴다**

**브랜치**: `color_ui` · **코드 변경 없음**(문서만) · 새 파일 `인계_반응형.md`

### 요청

「0.7초 적당하다. 구멍은 "새 창에서 인계문 이어서" 방식으로 하겠다.」

### 한 것

`인계_반응형.md`를 썼다. 직전 `인계_바닥여백.md`의 구조를 따랐다 —
0 프로젝트 / 1 실기기 / 2 저장소 / **3 할 일** / 4 이번에 한 것 / 5 검증 기준선 /
**6 다시 열지 말 것** / 7 반응형 현황 / 8 걸려 넘어지는 것 / 9 사용자 / 10 시작 / 11 작업 후.

### 판단 — 「7개 화면」을 짐작으로 적지 않았다

인계문에 대상 화면을 적기 전에 7개 전부 `awk`로 `<SafeAreaView>` 블록을 훑어
**`running` 블록이 정말 `ScrollView` 밖의 맨 `View`인지 확인했다.**

```
WrsSessionScreen · WrsTwoCharScreen · WrsBingoScreen · AmSessionScreen ·
FreqSessionScreen · PitchCompareScreen · Ling6SessionScreen
```

`PtaSessionScreen`·`WrsTabScreen`은 고르기 목록뿐이라 `running`이 없다 — 대상 아님.
`PitchCompareScreen`은 `idle`이 아예 없고(`ListeningCheckScreen`이 대신한다)
보기 칸이 `minHeight: 132`로 제일 크다 — **가장 위험한 화면이라고 인계문에 표시했다.**

### 정직하게 적은 것

**실기기에서 직접 깨지는 걸 본 것은 `WrsSessionScreen` 하나뿐이다.**
나머지 6개는 구조가 같아서 같은 처방이 필요하다고 **판단한 것**이다.
인계문에 그대로 적고 **「화면마다 재현부터 해 보고 고칠 것」**을 못 박았다.
이 계보는 「안 본 화면에도 파손이 있다」는 가정이 틀렸던 적이 있다(2026-08-22).
같은 실수를 반복하지 않으려고 근거의 등급을 구분해 뒀다.

### 인계문에 새로 넣은 함정

1. **`wm size reset` / `wm density reset`을 쓰지 마라.** 이 기기는 사용자가
   물리 1440x3040·560dpi를 **1080x2280·420dpi로 낮춰 쓰고 있다.**
   `reset`은 사용자의 그 설정까지 지운다. **명시값으로 되돌릴 것.**
   (이번에 실제로 `reset`을 눌렀다가 `wm size 1080x2280` · `wm density 420`으로 복구했다.)
2. **`sed -i`·`cat >>`가 줄바꿈을 갈아엎는다** — 오늘 하루에 두 번 밟았다.
   1순위 함정으로 올렸고 `replaceOnce` 방식(치환 건수를 세서 1건이 아니면 멈춤)을 적어 뒀다.
3. **커밋 메시지에 PowerShell 히어스트링(`@'...'@`)을 Bash에서 쓰지 마라** —
   `@`가 메시지에 박힌다(오늘 겪고 amend했다). Bash에서는 `git commit -F - <<'EOF'`.
4. **`font_scale`을 바꾸면 액티비티가 재시작되어 기본 탭(떨림)으로 돌아간다.**
5. **`adb shell input tap`이 가끔 씹힌다** — 좌표를 의심하기 전에 스크린샷으로
   현재 위치부터 다시 볼 것.
6. **화면 이동 지도**를 표로 넣었다(탭 좌표 · 각 탭에서 어느 화면으로 가는지).

### 미결 / 넘긴 것

- **`running` 화면 7개 스크롤** — `인계_반응형.md` 3절. 유일하게 열린 작업
- **`main` 병합** — 사용자가 **미루라고 했다**(2026-08-22). 72 커밋 앞섬
- `WrsVoiceGuideScreen`은 한국어 TTS 없는 기기에서만 떠서 **여전히 실기기 미검증**

### 검증

코드 변경이 없어 tsc/eslint/jest는 직전 블록의 기준선 그대로다
(tsc 0 · eslint 0 errors/경고 14 · 230 tests).
인계문이 주장하는 수치는 전부 명령으로 다시 확인했다 —
`BottomTabInset` 사용 화면 **0곳** · `theme.ts:144` 정의 · `WRS_FIRST_WORD_LEAD_MS = 700` ·
`hasKoreanVoice` **`WrsTabScreen:81`**(처음에 82로 적었다가 정정) ·
`PitchCompareScreen:770 minHeight: 132` · `orientation: "portrait"` ·
`MaxContentWidth = 800` · `Dimensions.get`은 `animated-icon.tsx` 한 곳 · `core.autocrlf=true` ·
`main` 대비 **72 커밋**.

---

## 2026-08-22 · **단어 듣기 — 첫 음에 뜸을 들인다** · 반응형 점검(작은 화면에서 파손 발견)

**브랜치**: `color_ui` · **3개 파일 + 테스트 1개**

### 요청

「한 글자·두 글자, 첫 음이 터치하자마자 나서 어색하다. 첫 음만 조금 늦추면 좋겠다.」
「그리고 안드로이드 앱으로서 반응형이 지금도 잘 구현돼 있는지 묻고 싶다.」

### 고친 것 — 첫 단어 앞 0.7초

카드를 누르면 `onStart` → `playCurrent(0)`이 **곧바로** `speakWrsWord`를 불렀다.
화면 전환과 소리가 겹쳐 들을 준비를 할 새가 없었다.

```
wrsTts.ts            WRS_FIRST_WORD_LEAD_MS = 700
                     waitFirstWordLeadIn()      ← 함수로 뺐다(아래 이유)
playCurrent(index)   index === 0 일 때만 뜸을 들인다
```

**두 번째 단어부터는 두지 않았다.** 「고르기 → 다음」 사이에 이미 사이가 있어
거기까지 늦추면 25번이 늘어져 답답해진다.

### 판단 — 뜸 동안 「듣는 중」이라고 하지 않는다

가장 쉬운 길은 `phase === "playing"`을 그대로 두고 소리만 늦추는 것이었다.
**그러면 소리가 나기 전 0.7초 동안 화면이 「듣는 중… 소리가 끝난 뒤 고르세요」라고 말한다.**
난청 사용자가 이걸 보면 **자기가 못 들은 줄 안다.** 이 앱에서 그 오해는 값이 비싸다.

그래서 `leadIn` 상태를 하나 두고 그 동안은

- 문구를 **「잠시 뒤 들려 드려요」**로 바꾸고
- 이퀄라이저를 **멈춰** 둔다(`playing={phase === "playing" && !leadIn}`)

실기기에서 뜸 → 재생 전환을 두 화면 다 확인했다.

### `waitFirstWordLeadIn`을 함수로 뺀 이유

`WrsSessionScreen.test.tsx`가 `wrsTts` 모듈을 통째로 대신한다. 뜸을 함수로 빼 두면
그 mock에 한 줄 더하는 것만으로 테스트에서 0.7초가 사라진다. 상수로만 뒀으면
테스트마다 실제 0.7초가 붙거나 `waitFor` 기본 1초와 아슬아슬해진다.

### 막힌 곳 — **`sed -i`가 또 줄바꿈을 갈아엎었다**

바로 앞 블록에서 겪고 적어 뒀는데 **같은 자리에서 또 밟았다.** `wrsTts.ts`는
`cat >>`로 붙이자 CRLF/LF가 섞였고, 두 화면은 `sed -i` 한 줄에 LF로 통일돼 버렸다.
매번 `sed -i 's/\r$//' && sed -i 's/$/\r/'`로 되돌렸다.

> **다음 세션에: 이 저장소에서 `.tsx`를 고칠 때 `sed -i`와 `cat >>`를 쓰지 마라.**
> 줄바꿈을 보존하는 Node 스크립트로 고치고, 끝나면 `file -b`로 확인할 것.
> 이번엔 치환 건수를 세는 Node 스크립트(`replaceOnce`)로 6곳씩 고쳐서
> 「조용히 안 맞는」 치환은 없었다.

### 반응형 질문에 대한 답 — **기기에서 재 봤다**

말로 답하지 않고 `adb`로 화면 크기·글씨 배율을 바꿔 실제로 확인했다.

| 조건 | 결과 |
|---|---|
| 411dp × 870dp(사용자 기기) · 1.0 / 1.3 | **전부 온전** |
| 411dp × 870dp · **2.0**(안드로이드 최대) | **연습 중 화면 온전.** 탭 목록은 잘리지만 스크롤됨 |
| **360dp × 640dp**(보급형) · 1.0 / 1.3 | **전부 온전.** 여유까지 있다 |
| **360dp × 640dp · 2.0** | **파손.** 아래 |

**튼튼한 이유**(설계가 실제로 하고 있는 것)

- 세로 고정(`app.json` `orientation: "portrait"`) — 가로 조합을 아예 없앴다
- 모든 치수가 dp라 화면 밀도에 따라간다. 절대 px이 박힌 곳 없음
- `MaxContentWidth: 800` + `alignSelf: "center"`로 태블릿에서 줄이 길어지지 않는다
- 보기 칸이 `width: "48%"`라 폭에 따라간다
- `SafeAreaView edges={["top","left","right"]}` — 노치·홈바를 피한다
- 화면 크기를 재는 코드가 사실상 없다(`Dimensions.get`은 `animated-icon` 한 곳뿐).
  **분기 없이 흐르는 레이아웃**이라 조합 폭발이 없다

**구멍 — `연습 중`(running) 화면만 스크롤이 없다**

`idle`과 `summary`는 `ScrollView` 안이지만 **`running`은 맨 `View`다.**
360dp × 640dp · 2.0에서 `WrsSessionScreen`이 **보기 4개 중 2개만 보이고
「중지」가 화면 밖으로 나간다.** 스와이프해도 안 움직인다(스크롤이 없으므로).
하드웨어 뒤로가기는 살아 있어 갇히지는 않지만 **연습을 할 수 없다.**

같은 조합에서 `WrsBingoScreen` idle의 버튼 3개는 「비/슷/한/소/리」처럼
한 글자씩 세로로 쪼개진다. 막지는 않지만 보기 나쁘다.

**아직 안 고쳤다.** 사용자 기기(411dp)에서는 2.0까지 멀쩡하므로 급하지 않다.
고친다면 처방은 이미 알려져 있다 — **`running` 블록을 `ScrollView`로 감싸고
「중지」는 스크롤 밖에 고정**(`ListeningCheckScreen`이 쓴 그 방법, `fd23835`).

### 검증

```
npx tsc --noEmit -p tsconfig.json    → exit 0
npx eslint src --ext .ts,.tsx        → 0 errors / 경고 14   (기준선과 동일)
npx jest                             → exit 0 · 23 suites · 230 tests
```

실기기: 한 글자·두 글자 모두 **「잠시 뒤 들려 드려요」(이퀄라이저 정지) → 「듣는 중…」(재생)**
전환 확인. 두 번째 단어부터는 뜸이 없는 것도 확인.

기기 설정은 **원래대로 되돌려 놓았다** — `wm size 1080x2280` · `wm density 420` ·
`font_scale 1.0`.
(`wm size reset`/`wm density reset`은 **물리값 1440x3040·560dpi로 돌아가 버린다.**
이 기기는 사용자가 1080x2280·420dpi로 낮춰 쓰고 있다. **reset 말고 명시값으로 되돌릴 것.**)

### 미결

- **작은 화면(360dp) + 글씨 2.0에서 `running` 화면 파손.** 위 처방대로 고칠 수 있다.
  대상: `WrsSessionScreen` · `WrsTwoCharScreen` · `WrsBingoScreen` ·
  `AmSessionScreen` · `FreqSessionScreen` · `PitchCompareScreen` · `Ling6SessionScreen`
- 뜸 길이 700ms는 `wrsTts.ts` 한 줄(`WRS_FIRST_WORD_LEAD_MS`)에서 바꿀 수 있다
- `main` 병합 — 사용자가 **아직 하지 말라고 했다**(2026-08-22)

---

## 2026-08-22 · **바닥 여백 정리 — 10개 화면에서 `BottomTabInset`을 걷어냈다**

**브랜치**: `color_ui` · **10개 파일** · 실기기 1.0/1.3 대조 완료

### 요청

「인계문 보고 작업해줘. 아마 하단 작업일 것이다.」
→ `인계_바닥여백.md` 3절에 **범위가 이미 확정돼 있어** 묻지 않고 바로 실행했다.

### 판단 — 인계문의 처방을 그대로 따랐다

`BottomTabInset`(안드로이드 80dp)은 이 앱에서 죽은 값이다. 하단 탭이 `NativeTabs`(네이티브)라
**화면 영역이 이미 탭바 위에서 끝난다.** `SafeAreaView`의 하단 inset(≈48dp)도 탭바가 덮는
자리라 헛돈다. 둘을 합쳐 화면당 최대 128dp(≈336px)가 헛돌고 있었다.

### **인계문 정정 — 「남은 9곳」이 아니라 10곳이었다**

`grep -rn BottomTabInset src` 결과 쓰는 화면은 **10개**다. 인계문 3절의
「10곳 − `ListeningCheckScreen` = 남은 9곳」은 셈이 어긋났다 —
`ListeningCheckScreen`은 이미 처리돼 그 10곳에 애초에 들어 있지 않았다.
또 인계문이 손댈 대상으로 적은 `StatsScreen`은 **`BottomTabInset`을 쓰지 않고 `edges`도
이미 있어** 손댈 게 없었다.

### 실제 변경

| 파일 | `paddingBottom` | `edges` 추가 |
|---|---|---|
| `AmSessionScreen` | `BottomTabInset + 26` → `26` | ✔ |
| `FreqSessionScreen` | `BottomTabInset + 26` → `26` | ✔ |
| `PitchCompareScreen` | `BottomTabInset + 26` → `26` | ✔ |
| `PtaSessionScreen` | `BottomTabInset + Spacing.three` → `Spacing.three` | ✔ |
| `WrsTabScreen` | `BottomTabInset + Spacing.three` → `Spacing.three` | ✔ |
| `Ling6SessionScreen` | `BottomTabInset + Spacing.four` → `Spacing.four` | 이미 있음 |
| `WrsSessionScreen` | `BottomTabInset + Spacing.four` → `Spacing.four` | 이미 있음 |
| `WrsTwoCharScreen` | `BottomTabInset + Spacing.four` → `Spacing.four` | 이미 있음 |
| `WrsBingoScreen` | `BottomTabInset` → **`Spacing.three`** | 이미 있음 |
| `WrsVoiceGuideScreen` | `BottomTabInset` → **`Spacing.three`** | 이미 있음 |

**`BottomTabInset`을 쓰는 화면은 이제 0곳이다.** `theme.ts:144`의 정의만 남았다
(iOS 값이 들어 있고 이 앱은 안드로이드 전용이라 정의 자체는 건드리지 않았다).

> **마지막 두 줄만 처방에서 벗어났다.** `WrsBingoScreen`·`WrsVoiceGuideScreen`은
> `paddingBottom: BottomTabInset` 단독이라 인계문 공식(`+ N → N`)을 그대로 쓰면 **0**이 되어
> 버튼이 탭바에 붙는다. 이웃 화면과 맞춰 `Spacing.three`(16dp)를 넣었다.
> 실기기에서 두 화면 다 버튼이 붙지 않는다.

### 막힌 곳 — **`sed -i`가 줄바꿈을 갈아엎었다**

인계문 7절 1번이 경고한 그 함정에 그대로 빠졌다. `sed -i`가 CRLF였던 7개 파일
(`Ling6` · `Pta` · `WrsBingo` · `WrsSession` · `WrsTab` · `WrsTwoChar` · `WrsVoiceGuide`)을
**LF로 바꿔 버렸다.**

- `core.autocrlf=true`라 **`git diff`는 오염되지 않았다** — 의도한 15줄만 잡혔다.
  하지만 워킹 트리 파일은 실제로 바뀌었다
- `sed -i 's/$/\r/'`로 **원래 CRLF였던 7개만** 되돌렸다.
  `Am`·`Freq`·`PitchCompare`는 원래부터 LF라 그대로 뒀다
- **다음 세션에**: 이 저장소에서 `sed -i`는 줄바꿈을 통일해 버린다. 고치기 전에
  `file <경로>`로 확인하고, 고친 뒤 다시 확인할 것

### **곁다리 「WRS 요약 pill」 — 저절로 풀렸다. 닫는다**

인계문 3절 곁다리: 1.3에서 「25개를 다 고르지 않아 기록에는 안 남겼어요」 알림이
「다시 연습」 버튼에 절반 가려지던 문제.
**여백 104dp를 회수하자 그대로 해소됐다.** 1.3 실기기에서 pill이 온전히 보이고
버튼 3개(다시 연습 · 처음으로 · 뒤로 가기)도 전부 보인다. 인계문의 예측이 맞았다.

### 검증

```
npx tsc --noEmit -p tsconfig.json    → exit 0
npx eslint src --ext .ts,.tsx        → 0 errors / 경고 14   (기준선과 동일)
npx jest                             → exit 0 · 23 suites · 230 tests
```

**실기기 대조 (SC-01M · w411dp · 1080x2280).** 화면마다 `font_scale` 1.0과 1.3을 둘 다 찍었다.

```
1.0  링6 idle · Am idle/듣기준비/진행/요약 · Pta 고르기 · PitchCompare ·
     Freq · WrsTab · Wrs 한글자/요약 · 두글자 · 빙고 idle
1.3  같은 경로 전체 + Wrs 한글자 idle · 빙고 판(3×3)
```

- **1.0에서 회귀 없음.** 어느 화면도 내용이 탭바에 붙지 않는다
- **1.3에서 잘리거나 가려지는 곳 없음.** 모든 주 버튼이 온전하고 누를 수 있다
- 눈에 보이는 변화: 링 6 idle의 「연습 시작」이 **210px 아래로 내려왔다**(헛돌던 80dp 회수)
- 끝나고 `font_scale`은 **1.0으로 되돌려 놓았다**

### 미결

- **`WrsVoiceGuideScreen`만 실기기 미검증.** 이 화면은 **한국어 TTS가 없을 때만** 뜬다
  (`WrsTabScreen:82` `hasKoreanVoice()`). 시험기에는 있어서 진입할 방법이 없었다.
  변경은 나머지 9곳과 같은 기계적 수정이고 jest 테스트 4건은 통과한다
- `main` 병합. `main` 대비 69 커밋 앞선다. PR 없음

---

## 2026-08-22 · **③ `chartMuted` 판정 — 기기에 기록을 심어 실제로 봤다** · 푸시 · 인계문

**브랜치**: `color_ui` · **코드 변경 없음**(판정과 문서만)

### 요청

「푸시하고, 남은 문제 어떻게 해결?」 → 「(기록을 건드려도) 허락할게. 그 다음 인계문 작성해」

### 푸시

`git fetch` 먼저 돌렸다. **직전 인계문의 「2 커밋 앞선다」는 낡은 정보였고 실제로는 1개**
(`fd23835`)만 안 올라가 있었다. `592679e..fd23835` 푸시 완료. `origin/color_ui`와 동기.
**원격 상태를 말하기 전에는 반드시 `git fetch`부터.**

### ③ — 「기록이 없어 못 본다」는 벽을 뚫었다

이 항목은 인계문 3대에 걸쳐 「링 6 기록이 2건뿐이라 패널이 안 뜬다」는 이유로 미결이었다.
**개발 빌드라 `run-as`가 된다는 걸 쓰면 심을 수 있다.**

```
저장소   AsyncStorage = SQLite  /data/data/com.rnhear.app/databases/RKStorage
기기에   sqlite3 바이너리 없음  → Node 24 내장 `node:sqlite`로 처리
조건     training.ling6Daily.v1 에 레코드 7건 (LING6_WEAKNESS_WINDOW=7)
```

절차는 인계문 7절에 재사용 가능한 형태로 적어 뒀다. **사용자 실제 기록이라 백업 2중
(DB 파일 + 값 JSON) → 시드 → 판정 → 복구 → 기기에서 다시 읽어 확인**까지 했다.
원본 2건(8/22 `passCount:5` · 8/20 `passCount:1`) 그대로 돌아왔다.

시드는 강조색과 muted가 **한 화면에 같이 나오도록** 아쉬움 횟수를 흩뿌렸다
(`/s/` 6회 → 강조, `/ʃ/` 3회 · `/m/` 2회 · `/a/` 1회 → muted, `/u/`·`/i/` 0회 → 스텁).

### 판정 — **현행 유지로 닫는다**

```
chartMuted(#94A7BB) vs 트랙(borderSubtle #EEF3F8)   2.21:1
chartMuted          vs 카드(surface #FFFFFF)         2.47:1
highlight(#F36C1C)  vs 트랙                          2.70:1
highlight           vs chartMuted (막대끼리)         1.22:1
```

- 실기기에서 회색 막대(`/ʃ/` 3회, `/m/` 2회)는 트랙 위에서 **또렷하게 읽힌다.** 부족하지 않다
- **직전 인계문의 「부족하면 `#8A9EB4`(2.47)까지 올릴 수 있다」는 잘못된 처방이다.**
  `chartMuted`를 진하게 만들면 `highlight`(2.70)와 **가까워져 둘을 구별하기 더 어려워진다.**
  올릴 게 아니라 그대로 두는 게 맞다
- 진짜 눈여겨볼 값은 따로 있었다: **`highlight` vs `chartMuted`가 광도로는 1.22:1뿐이고
  색상(hue)으로만 갈린다.** 다만 앱이 이미 **막대 높이 · 라벨 굵기(`text` vs `textMuted`) ·
  문장**(「요즘 /s/가 아쉬운 날이 많아요」)으로 **중복 부호화**하고 있어 색만으로 읽지
  않아도 된다. **색을 손댈 게 아니라 이 중복 부호화를 유지하는 것이 답이다**

### 막힌 곳

- **Git Bash가 `adb` 인자의 `/sdcard/...`를 윈도우 경로로 바꿔 버린다.**
  `failed to stat remote object 'C:/Program Files/Git/sdcard/...'`.
  `export MSYS_NO_PATHCONV=1`로 막았다
- `adb shell "... > /sdcard/파일"` 경유는 0바이트가 나왔다.
  **`adb exec-out run-as ... cat`이 바이너리를 안 깨뜨린다**(screencap과 같은 방식)

### 검증

```
기기에서 복구값 재확인   원본 2건 그대로 (8/22 · 8/20)
font_scale               1.0 (되돌려 놓음)
코드 변경 없음           tsc/eslint/jest 재실행 불필요
```

### 남은 것

**바닥 여백 정리 9개 화면 하나뿐이다.** `BottomTabInset`(80dp)이 죽은 값이고,
그중 5곳은 `SafeAreaView` 하단 edge(≈48dp)까지 헛돈다. 자세한 건 `인계_바닥여백.md` 3절.
WRS 요약 pill은 그 정리로 저절로 풀릴 가능성이 높아 따로 손대지 않았다.

---

## 2026-08-22 · **듣기 준비 화면 파손 수정**(세 번째 시도에서 통과) · 안 본 화면 6곳 훑기

**브랜치**: `color_ui` · **변경**: `ListeningCheckScreen` 한 파일

### 요청

`인계_글씨크기.md`를 읽고 이어서. 그 문서 9절 순서(① 수정 → ② 훑기 → ④ 상한 결정)를 그대로 따랐다.

### ① `ListeningCheckScreen` — 고쳤다. 앞선 두 번이 진 이유가 인계문의 짐작과 달랐다

재현부터 했다. `font_scale 1.3`에서 「연습 시작」·「뒤로 가기」가 **아예 화면 밖**이고
면책 문구도 잘린다. 인계문 3절 ①이 적은 그대로였다.

처방은 인계문 권고대로 `marginTop: "auto"`를 먼저 걷어내는 것으로 시작했다.

```
<SafeAreaView edges={["top","left","right"]} style={styles.safeArea}>
  <ScrollView style={styles.scroll} contentContainerStyle={styles.scrollContent}>
    header · 카드 2장 · extra · 면책 · error
  </ScrollView>
  <View style={styles.actions}>  ← 스크롤 밖 고정. `marginTop:"auto"` 없음
scroll:        { flex: 1 }
scrollContent: { flexGrow: 1, gap: Spacing.three - 4 }
```

**그런데 이것만으로는 1·2차와 같은 자리에서 다시 졌다.** 1.3에서 버튼은 살아났지만
버튼 아래에 **250px짜리 빈 공간**이 생겼다 — 인계문이 「버튼이 250px 위로 딸려 온다」고
적은 바로 그 증상이다.

#### 진짜 원인 — `marginTop:"auto"`가 아니라 **바닥 여백이 과했다**

```
paddingBottom: BottomTabInset + Spacing.three   = 80 + 16 = 96dp
+ SafeAreaView 하단 inset(내비바)               ≈ 48dp
                                              = ~144dp
```

**`BottomTabInset`(안드로이드 80dp)은 이 앱에서 죽은 값이다.** 하단 탭이
`NativeTabs`(네이티브 — `src/components/app-tabs.tsx`)라 **화면 영역이 이미 탭바 위에서
끝난다.** 탭바 높이를 또 더하면 그만큼 헛돈다. `SafeAreaView`의 하단 inset도 같은 이유로
탭바가 덮는 자리라 헛돈다.

그동안은 **내용이 상자를 넘쳐 흐르며 그 ~144dp를 먹고 있었다.** 그래서 1.0에서 버튼이
바닥에 붙어 보였던 것이다. `ScrollView`가 정직하게 자르기 시작하자 그 빚이 드러났다.
**321c8c5의 히어로(「1.0에서도 이미 상자보다 ~13dp 컸다」)와 똑같은 구조다.**

앞선 두 시도는 이 여백을 그대로 둔 채 스크롤만 씌웠고, 드러난 빈 공간을 「1.0 회귀」로
보고 되돌렸다. **되돌릴 게 아니라 여백을 고쳤어야 했다.**

- `paddingBottom` → `Spacing.three`만 남김(`BottomTabInset` import 제거)
- `SafeAreaView` → `edges={["top", "left", "right"]}`

#### 결과 — 실기기 대조(스크린샷 9장)

| `font_scale` | 결과 |
|---|---|
| **1.0** | **수정 전과 동일.** 카드 2장·토글·면책 문구·버튼 전부 첫 화면에 보인다. 회귀 없음 |
| **1.3** | 버튼 두 개 온전. 스크롤로 토글·면책 문구까지 도달 |
| **2.0**(안드로이드 최대) | **버튼 두 개가 다 눌린다.** 글자는 두 줄로 감기지만 잘리지 않는다 |

### ② 안 본 화면들을 1.3으로 훑었다 — **사용자를 막는 파손 0**

| 화면 | 결과 |
|---|---|
| 단어 빙고 — idle · 3×3 격자 · 요약 | **정상.** 격자는 오히려 여유가 많다. 인계문의 「격자라 특히 의심」은 빗나갔다 |
| 한 글자 진행 | 정상, 여유 많음 |
| 두 글자 진행 | 정상 |
| 링 6 진행(그림 6장) | 정상, 여유 있음 |
| 링 6 요약 | 정상 |
| WRS 요약(한 글자) | **잘려 보인다** — 아래에 따로 적는다 |
| **설정 화면** | **그런 화면은 없다.** `find src -iname "*setting*"` 0건, `src/app`은 `index`·`ling6`·`pta`·`wrs`·`_layout`뿐. 화면 우하단 톱니는 **Expo dev-menu**다(개발 빌드라 뜬다). 인계문 ② 목록의 이 항목은 지운다 |

인계문은 「확인한 4개 중 4개에서 파손이 나왔으니 안 본 곳에도 있다고 가정하는 편이 맞다」고
했다. **그 가정은 틀렸다.** 파손 4개는 전부 같은 처방(idle 히어로에 스크롤 없음)을
공유하던 자리였고, 그 처방이 애초에 없던 화면들은 멀쩡했다.

### 새로 찾은 것 — WRS 요약의 알림 pill (**미결. 막지는 않는다**)

`WrsSessionScreen` 요약, 1.3: 「25개를 다 고르지 않아 기록에는 안 남겼어요」 알림이
「다시 연습」 버튼에 **절반 가려진다.**

**다만 ①과 급이 다르다** — 이 화면은 이미 스크롤 + 버튼 고정 구조라 **버튼 3개는 항상
온전하고**, 살짝 스크롤하면 알림도 다 보인다. 사용자를 막지 않는다. 급하지 않다.

### ④ `allowFontScaling` 상한 — **두지 않는다**

인계문 3절 ④가 세운 기준은 「넘치는 곳이 두어 곳이면 개별 수정, 열 곳 넘으면 상한」이었다.
훑은 결과 **막는 곳 0 · 잘리는 곳 1**. 기준상 개별 수정이 답이다.
**고령·난청 사용자가 키운 글씨를 앱이 거부하는 거래를 할 근거가 없다.** 이 항목은 닫는다.

### 막힌 곳 · 이 저장소에서 걸려 넘어진 것

- **인계문 7절 1번이 틀렸다.** 「`.tsx`는 LF」가 아니다. `ListeningCheckScreen.tsx`는
  **CRLF**다. 노드 스크립트가 `\n` 마커를 못 찾아 한 번 실패했다.
  **파일마다 `file <경로>`로 확인하고 줄바꿈을 보존할 것.** 저장소가 섞여 있다
- 큰 한글 문서를 Bash 히어독/`node -e`에 인라인으로 넣으면 이스케이프가 깨진다(한 번 겪음).
  **스크립트를 Write 도구로 파일에 쓰고 `node <파일>`로 실행할 것**

### 검증

```
npx tsc --noEmit -p tsconfig.json    → exit 0
npx eslint src --ext .ts,.tsx        → 0 errors / 경고 14  (기준선 그대로)
npx jest                             → 23 suites · 230 tests 통과
adb  실기기 RF8MC0XZYLK · font_scale 1.0 / 1.3 / 2.0 대조 · 스크린샷 15장
```

**`font_scale`은 1.0으로 되돌려 놓았다**(사용자의 실제 폰).

### 미결

- **WRS 요약 pill**(위) — 잘려 보이지만 막지는 않는다
- **③ `chartMuted` 2.21:1** — 여전히 못 본다. 링 6 기록이 서로 다른 7일치가 있어야
  패널이 렌더된다. 기록이 쌓이기 전엔 불가. 그대로 둔다

---

## 2026-08-22 · 실기기 연결 — 인계문 ①③ 답함 · **글씨 크기 파손 수정** (3개 화면)

**브랜치**: `color_ui` · **변경**: `AmSessionScreen` · `FreqSessionScreen` · `PitchCompareScreen`

### 요청

`인계_color_ui.md`를 읽고 이어서. 그 문서 8절이 「남은 셋이 전부 실기기 의존이므로
`/run`으로 앱을 띄우는 것이 유일한 다음 단계」라고 못박고 있었다.

### 기기가 이미 붙어 있었다 — 띄울 필요가 없었다

```
adb devices  →  RF8MC0XZYLK  SC-01M (Galaxy Note10+, docomo)
포그라운드   →  com.rnhear.app/.MainActivity  (개발 빌드, 2026-08-21 23:04 갱신)
Metro        →  8081 LISTENING (PID 23280), 기기와 연결됨
```

**즉 화면에 보이던 것이 워킹 트리의 코드였다.** 기기 규격: `w411dp` · `420dpi` ·
`font_scale 1.0`. 411dp는 좁은 축이 아니다(요즘 좁은 기기는 360dp).

### ③ **인계문이 틀렸다** — 안드로이드 그림자는 이미 먹고 있다

인계문 3절 ③은 「`Shadows.card`가 iOS 모양 값인데 안드로이드에서 뭐가 적용되는지
확인 안 됨」이라고 적었다. **코드를 보면 답이 이미 있다.**

```
theme.ts:132   Shadows.card    elevation: 2
theme.ts:140   Shadows.accent  elevation: 4
theme.ts:123   주석: "iOS는 shadow*, 안드로이드는 elevation으로 근사한다"
```

화면에서도 「연습 시작」 버튼 아래 파란 번짐이 그 `elevation: 4`다. **③은 닫는다.**
파일을 열어보지 않고 값의 생김새만으로 진단한 것이 원인이다.

### ① **진짜 파손을 찾았다 — 다만 원인이 인계문의 짐작과 달랐다**

인계문은 「좁은 화면에서 14px이 넘치는가」를 물었고 1순위 의심 지점으로
`SessionModeToggle`(10.5→14)을 지목했다. **틀렸다.** 411dp·1.0에서도, 1.3에서도
`SessionModeToggle`은 멀쩡하다. `SessionTrendPanel`(8곳)도 멀쩡하다.

고령 사용자의 진짜 위험은 **화면 폭이 아니라 시스템 글씨 크기 설정**이었다.

```
adb shell settings put system font_scale <값>     ← 재현 방법
```

떨림 화면 idle의 「시작을 누르면 난이도가 맞춰지는 / 연습이 이어집니다」:

| `font_scale` | 결과 |
|---|---|
| 1.0 | 여유 있음 |
| **1.1** | 두 줄 다 보이나 **간격 0** — 버튼이 글자에 닿는다 |
| **1.2** | 둘째 줄이 「연습 시작」 버튼에 **완전히 가려짐** |
| **1.3** | 첫 줄까지 잘림 |

**기본에서 한두 단계만 올리면 깨진다.** 대상 사용자가 고령·난청자인 앱에서 가볍지 않다.

#### 원인 — 배율이 두 번 곱해지는데 스크롤이 없다

```
TEXT_SCALE = 1.2                      앱 내부 배수 (4개 화면)
× 시스템 font_scale                    상한 없음. 안드로이드 최대 2.0
= 1.3에서 실효 1.56배 · 2.0에서 2.4배
```

- **`allowFontScaling` · `maxFontSizeMultiplier`가 저장소 어디에도 없다.** 전수 확인함
- `hero`는 `flex: 1` + `justifyContent: center`인데 **스크롤이 없다.**
  내용이 커지면 상자를 넘쳐 흐르고, 나중에 그려지는 버튼이 그 위를 덮는다
- 같은 파일의 **요약(summary) 단계는 이미 `ScrollView`로 감싸져 있었고**,
  그 주석이 이 문제를 정확히 예견하고 있었다 —
  「글자 크기를 키우면(시스템 설정 최대 200%) 카드가 화면을 넘겨 버튼에 닿을 수 없게 되므로」.
  **idle에만 그 처방을 안 넣은 것이다**

#### `a1a5bd7`을 되돌리지 않는다

인계문은 「넘치면 `a1a5bd7`만 되돌린다」고 적어 뒀다. **되돌려도 안 고쳐진다.**
파손은 14px 때문이 아니라 시스템 배율 × 무스크롤 때문이고, 글자를 다시 줄이면
1.3에서 여전히 깨지면서 **가독성만 잃는다.** 인계문 5절의 「글자 크기 재조정 — 끝남」은
그대로 유지한다.

### 실제 변경

세 화면의 **idle 히어로를 `ScrollView`로 감쌌다.** 요약 단계와 같은 처방이다.

```
<ScrollView style={styles.heroScroll} contentContainerStyle={styles.hero}>
heroScroll: { flex: 1, alignSelf: "stretch", marginBottom: -(Spacing.two + 2) }
hero:       { flexGrow: 1, ... }          ← flex: 1 에서 바뀜
```

버튼은 스크롤 **밖**에 남아 항상 보인다. 자리가 남으면 예전처럼 가운데 정렬,
모자랄 때만 스크롤된다.

**음수 마진이 왜 필요했나** — 처음엔 없이 넣었더니 **기본 배율 1.0에서 둘째 줄이
잘리는 회귀**가 났다. 재 보니 히어로 내용은 **1.0에서도 이미 상자보다 ~13dp 컸고**,
그 넘침이 `safeArea`의 `gap`으로 흘러들어 가 안 보였을 뿐이었다. `ScrollView`가
그 경계에서 자르기 시작하자 드러난 것이다. 음수 마진은 그 `gap`을 이 자식에서만
상쇄해 **예전에 빌려 쓰던 높이를 그대로 돌려준다.** 버튼과의 간격은 `actions`의
`marginTop`이 따로 준다. 1.0 화면은 수정 전과 사실상 동일하다(실기기 대조 확인).

### 막힌 곳 — `ListeningCheckScreen`(듣기 준비)은 **고치지 못했다. 되돌렸다**

**같은 계열의 파손이 있고, 셋 중 가장 심각하다.** `font_scale` 1.3에서 안내 카드가
늘어나 **「연습 시작」 버튼이 화면 밖으로 밀려 눌리지 않는다.** 스크롤이 아예 없어서
(`ScrollView` 0개) 도달할 방법이 없다 — 그 경로로는 연습을 시작할 수 없다.

두 가지를 시도했고 **둘 다 기본 배율 1.0에서 회귀를 냈다.**

1. **세션 화면과 같은 처방**(안내만 스크롤, 버튼은 밖에 고정) → 1.0에서 버튼이
   250px 위로 딸려 올라오고 귀풀기/연습 토글이 잘렸다. 이 화면은 `actions`가
   `marginTop: "auto"`로 바닥에 붙는 구조인데, 스크롤이 `flex: 1`로 남은 자리를
   다 먹으면 그 `auto`가 죽는다
2. **버튼까지 통째로 감싸기**(`flexGrow: 1` + `marginTop: "auto"` 유지) → 1.0에서
   면책 문구와 버튼이 **아예 화면에서 사라졌다.** `auto` 마진이 뷰포트보다 큰
   컨텐트 높이를 기준으로 계산되는 것으로 보인다

세 번째 시도로 넘어가는 대신 **`git checkout`으로 되돌렸다.** 한 화면에 계속 매달리는
것이 이 갈래가 반복해 온 낭비 패턴이고, 되돌리면 최소한 **오늘 동작이 그대로 보존된다.**
고칠 때는 `marginTop: "auto"`를 먼저 걷어내고 명시적인 바닥 배치로 바꾸는 쪽을 볼 것.

### ② `chartMuted`는 여전히 답할 수 없다 — 인계문의 함정이 실제였다

통계 화면을 열었다. **링 6 기록이 2회뿐이다**(8/20, 8/22). 아쉬움 막대 패널은
서로 다른 7일치가 있어야 렌더되므로(`ling6Session.ts:206`) **화면에 아예 없다.**
「안 보인다」가 아니라 「없다」다. 기록을 7일 쌓거나 시드를 넣기 전에는 못 본다.

### 곁가지 — 오탐 하나

화면 오른쪽 가장자리에 잘린 흰 카드가 보여 가로 넘침인 줄 알았다.
**삼성 엣지 패널 핸들**(`com.samsung.android.app.cocktailbarservice`)이었다. 우리 앱이 아니다.

### 검증

```
npx tsc --noEmit          → exit 0
npx eslint src            → 0 errors / 14 warnings   (기준선 그대로. 그 4개 테스트 파일)
npx jest                  → 23 suites · 230 tests 통과
실기기                     → 1.0 / 1.1 / 1.2 / 1.3 대조 촬영
```

기기 설정은 **원래대로 되돌려 놨다**(`font_scale` 1.0, 미디어 볼륨 복구,
해상도·밀도는 애초에 손대지 않음).

### 미결

1. **`ListeningCheckScreen` 글씨 크기 파손** — 위 「막힌 곳」 참고. **새로 생긴 최우선 항목**
2. **`chartMuted` 2.21이 충분한가** — 링 6 기록 7일치 필요. 변동 없음
3. **`allowFontScaling` 상한을 둘 것인가** — 지금은 전무하다. 안드로이드 2.0까지
   그대로 곱해진다. 상한(`maxFontSizeMultiplier`)을 두면 넘침은 줄지만 **고령 사용자가
   키운 글씨를 앱이 거부하는 것**이라 접근성과 맞바꾸는 거래다. 결정한 적 없다

**①③은 닫는다.** ①은 원인을 바꿔 고쳤고, ③은 오진이었다.

---

## 2026-08-22 · 푸시 · 팔레트 이력 전수 · 「색이 안 변했다」의 원인 (코드 변경 없음)

**브랜치**: `color_ui` → **`origin/color_ui` 생성 (이 갈래의 첫 원격 반영)**
**코드 변경 없음.** 앞 블록 이후 벌어진 일과, 대화로만 오갔던 조사 결과를 여기 남긴다.

### 푸시했다 — 이 갈래가 처음 원격에 올라갔다

```
git push -u origin color_ui   → * [new branch]  color_ui -> color_ui
```

- 커밋 9개 · 파일 24개 · +1416 / −374 (문서 2개 853줄 포함)
- `color_ui`에 업스트림이 없었으므로 **새 원격 브랜치가 생겼다.** 기존 브랜치는 건드리지 않았다
- **`ui_chips_suisei`는 여전히 업스트림이 없지만 안전하다** — `color_ui`가 그 위에 쌓기만 했으므로
  그 커밋은 전부 `origin/color_ui`에 포함돼 있다. `git merge-base --is-ancestor`로 확인
- `git log --oneline --branches --not --remotes` → 비어 있음. 푸시 안 된 커밋 없음
- 곁가지: `two_feat`이 **behind 5**다. 원격이 앞서 있다는 뜻이고 이 작업과 무관하다. 손대지 않았다

### 변곡점 — 「컬러적으로 변한 게 없는 것 같다」

작업을 마치고 나온 지적인데, **맞는 말이었다.** 원인은 코드가 아니라 **비교 기준선**이었다.

| 무엇과 비교하나 | 색 차이 |
|---|---|
| 부모 `ui_chips_suisei` | **`chartMuted` 하나뿐** — 사실상 같다 |
| `d37a654` | 색 4개 교정 + 신규 1개 + 글자 40곳 — **뚜렷하다** |
| `main` | 팔레트가 통째로 다르다 |

`color_ui`는 `ui_chips_suisei`에서 갈라졌고, **큰 색 변경(`a1a5bd7`)은 이미 그 아래에 있었다.**
그래서 부모 기준으로 보면 아무것도 안 변한 게 맞다. 이 브랜치가 팔레트에 더한 것은
`chartMuted` 하나이고, 그마저 **링 6 기록 7일치가 있어야 보이는 패널**의 막대 색이다.

**교훈으로 남긴다**: 「변했나」를 물을 때는 **무엇과 비교하는지를 먼저 말해야 한다.**
브랜치가 겹겹이 쌓인 상태에서는 같은 작업이 「전부 바뀜」도 되고 「그대로」도 된다.

### `theme.ts` 팔레트 전체 이력 — 이번에 처음 전수 조사했다

| 커밋 | 무엇을 했나 |
|---|---|
| `a9916a7` Initial | Expo 기본 템플릿 — `#000000` / `#ffffff` / `#F0F0F3`, **다크 모드 있음**, 토큰 5개 |
| `a91dc8e` | **「Clean Clinical」 전면 교체** — 지금 앱 인상의 뼈대. **다크 모드는 여기서 사라졌다** |
| `c05fac1` | `highlight` `#F5833F` · `positive` `#1F9D6B` · `positiveTint` 추가 |
| `592620c` | `positiveTint` 제거 |
| `79a35bb` | `danger` `#9B3B3B` 추가 |
| `a1a5bd7` | **대비 교정 4개** (앞 블록 참고) |
| `95d50ac` · `fd2e693` | 이 브랜치 — 주석 규칙 + `chartMuted` |

**`a91dc8e`가 가장 큰 변곡점이다.** 「흰 바탕에 회색 면」이 **「옅은 파란 바탕에 흰 카드」**로
뒤집혔다. 지금 우리가 「Clean Clinical 톤을 깨지 말 것」이라고 부르는 제약이 여기서 생겼다.

토큰별 사용 규모(팔레트를 흔들 때 영향 범위):

```
textMuted      47곳     ← a1a5bd7의 체감 변화가 거의 전부 여기서 나온다 (2.85 → 4.82, 대비 1.7배)
textSecondary  40곳     ← 4.40 → 4.78. 거의 티가 안 난다
highlight       8곳     ← 전부 비텍스트
positive        2곳
chartMuted      1곳     ← 7일치 기록 전엔 화면에 안 나온다
```

### 정정 — 앞 블록의 「글자 크기 47곳」은 **42곳**이다

`a1a5bd7`을 실측했다.

```
a1a5bd7:  +fontSize:14  42줄
          −10.5×1  −11×5  −11.5×5  −12×14  −12.5×8  −13×8  −13.5×1  =  42줄
```

즉 **`a1a5bd7`이 14px로 올린 곳은 42곳**이다. 「47」이 나온 건 뒤 커밋까지 합산했기 때문으로 보인다 —
`26fe5df`(ScreenHeader)가 죽은 스타일을 지우면서 `fontSize: 12` **5줄을 통째로 삭제**했다.
42 + 5 = 47. **올린 것 42 + 지운 것 5**이지, 47곳을 올린 게 아니다.

`d37a654` 기준 누적으로 보면 `+fontSize: 14`가 **40줄**이다(42에서 뒤 커밋이 2줄을 지웠다).
가장 작던 값은 **10.5px**(`SessionModeToggle` 힌트)였다.

### 만든 것 — 검토 페이지와 학습 문서

- **푸시 전 검토 페이지**(아티팩트): https://claude.ai/code/artifact/60d9ace1-b0da-47c4-8ab6-af21884e54b0
  색 5개의 이전/지금을 **실제 카드 견본**으로 재현했다(흰 카드 · `#F6F9FD` 바탕 · 테두리 1px ·
  반경 18 · 아이콘 타일 44 — 앱 지오메트리 그대로). 대비비를 터미널에서 볼 수 없어서 만든 것.
  같은 파일 경로로 다시 게시하면 같은 URL로 갱신된다
- **`깃_학습_질문지.md`** (저장소 루트, 아직 커밋 안 함) — 커밋 · 푸시 · 브랜치 · 업스트림 ·
  분기 · 동기화 개념을 순서대로 익히는 프롬프트 22개 + 부록 5개.
  이 저장소의 실제 브랜치·커밋 이름을 예시로 박아 뒀다

### 미결 — 변동 없음

앞 두 블록에서 넘어온 셋이 그대로다.

1. **14px 상향이 좁은 화면에서 넘치는지** — 실기기 미확인
2. **`chartMuted` 2.21이 충분한가** — 링 6 기록 7일치 필요(`ling6Session.ts:206`)
3. **안드로이드 그림자** — `Shadows.card`가 iOS 모양 값인데 이 저장소엔 `ios/`가 없다

② 카드 경계는 앞 블록에서 **닫았다.** 다시 열지 말 것 — 다시 열 조건은 그 블록에 적어 뒀다.

---

## 2026-08-22 · ② 카드 경계 — **현행 유지로 닫는다** (결정만, 변경 없음)

**브랜치**: `color_ui` · **코드 변경 없음**

인계문 `인계_컬러.md` ②와 바로 앞 블록의 「미결 1」을 **닫는다.** 카드 경계는 지금 값으로 간다 —
`border` `#E4EBF3`(흰 카드 1.20:1) · 카드 vs 배경 1.06:1 · `Shadows.card`.

### 닫은 근거 — 올릴 수 있는 폭이 애초에 좁다

「실기기로 봐야 안다」고 미뤄 뒀던 항목인데, 보기 전에 **천장부터 재 봤더니 낮았다.**

| 테두리 | 흰 카드 대비 | |
|---|---|---|
| `#E4EBF3` | 1.20 | 현재 — **이대로 간다** |
| `#D3DEEA` | **1.36** | 인계문의 중간 안 |
| `#C7D5E5` | 1.49 | |
| `#BACBDF` | 1.65 | 톤 한계쯤 |
| `#7397C1` | 3.03 | WCAG 1.4.11 통과. 「Clean Clinical」이 죽는다 |

즉 톤을 지키면서 갈 수 있는 범위는 **1.20 → 1.36~1.65**다. 사람 눈으로 겨우 갈릴 차이 하나를
얻자고 카드가 있는 전 화면을 흔드는 거래다. 값어치가 안 맞는다.

**인계문 수치 하나를 정정한다.** ②에 `#D3DEEA`를 「≈1.5」로 적어 뒀는데 실제로는 **1.36**이다.
1.5를 원하면 `#C7D5E5`까지 가야 한다.

### 반대쪽 레버는 **막혀 있다** — 이번에 확인한 것

「테두리 대신 페이지 배경을 낮춰 흰 카드를 띄우면 되지 않나」를 재 봤다. **못 쓴다.**

| 배경 | 흰 카드 대비 | `textMuted` 대비 |
|---|---|---|
| `#F6F9FD` | 1.06 | 4.56 (현재) |
| `#E7EEF6` | 1.17 | **4.12** |
| `#DCE6F2` | 1.26 | **3.82** |

`themed-view.tsx:15`가 `background`를 화면 바탕으로 깔고 그 위에 캡션·라벨이 얹힌다.
배경을 낮추면 카드는 1.06 → 1.26밖에 안 오르는데 `textMuted`가 **AA 아래로 떨어진다** —
`a1a5bd7`이 올려놓은 것을 도로 무너뜨린다. 카드 대비를 올리는 레버는 **테두리 하나뿐**이고,
그 하나의 천장이 위 표다.

### 그래서 이건 「미확인」이 아니라 「결정」이다

앞 두 블록과 인계문이 ②를 **「실기기 없이는 결정할 수 없다」**로 넘겨 왔는데,
그 전제가 틀렸다. 실기기가 답할 수 있는 건 「1.36이 1.20보다 나은가」뿐이고,
**어느 쪽이든 앱이 달라지지 않는다.** 재는 비용이 얻는 것보다 크다. 닫는다.

다시 열려면 조건이 필요하다 — **실사용자가 「카드가 어디서 끝나는지 모르겠다」고 말할 때.**
그때는 테두리가 아니라 카드 간 간격·그림자를 먼저 볼 것.

### 곁가지 — 안드로이드 그림자

이 저장소엔 `ios/`가 없다(안드로이드 전용, RN 0.86 · newArch). `Shadows.card`는
`shadowOpacity 0.08` / `shadowRadius 18`처럼 **iOS 모양의 값**인데 안드로이드에서 실제로
무엇이 먹는지는 확인하지 않았다. 카드 윤곽을 다시 볼 일이 생기면 **여기부터** 볼 것 —
테두리보다 이쪽이 헐거울 수 있다.

### 남은 미결 (②를 뺀 나머지)

1. **`chartMuted` 2.21이 충분한가** — 실기기 필요. 링 6 기록이 **7일치** 쌓여야 패널이
   렌더된다(`ling6Session.ts:206`)는 점도 같이 기억할 것. 그전엔 볼 방법이 없다
2. **14px 상향이 좁은 화면에서 넘치는지** — 계속 열려 있다

---

## 2026-08-22 · 컬러 인계문 이어받기 — 옅은 파란 면 규칙 · 링 6 막대 토큰화

**브랜치**: `color_ui` (`ui_chips_suisei`에서 분기)
**커밋**: `95d50ac` 규칙+세그먼트 힌트 / `fd2e693` 링 6 막대
**검증**: `eslint` 0 errors(기존 경고 14 그대로) · `tsc` exit 0 · `jest` 23 suites / 230 tests 통과

`인계_컬러.md`의 남은 과제 5건 중 **①③⑤를 닫았다.** ②는 실기기가 필요해 손대지 않았고
④는 감시 항목이라 손댈 것이 없었다.

### ① 세그먼트 힌트 — 활성일 때만 깨지던 곳

`SessionModeToggle.tsx`의 힌트는 활성 세그먼트 안에 있는데, 활성이면 바닥이 `accentTint`가 된다.
흰 면에서 4.82로 통과하던 `textMuted`가 거기서 **4.28**이 된다.

인계문의 선택지 (a)~(d)를 다 쓰지 않고 **다섯 번째 안**을 골랐다 — 활성일 때 힌트도
**라벨과 같은 `accent`(4.52)**로 둔다.

| 안 | 왜 안 골랐나 |
|---|---|
| (a) `textSecondary` | 4.24라 더 나쁘다 |
| (b) 활성 배경을 더 옅게 | `accentTint`는 17곳이 공유한다. 한 곳 때문에 전부 흔든다 |
| (c) 틴트 전용 토큰 신설 | 「면을 알아야 색을 고른다」는 부담이 남는다. 이 버그가 그렇게 생겼다 |
| (d) 활성일 때 `text` | 14.08로 안전하지만 힌트가 라벨보다 진해져 위계가 뒤집힌다 |
| **(e) 라벨과 같은 `accent`** | **새 토큰 0개 · 팔레트 무변경 · 활성 세그먼트가 통째로 파랗게 물들어 선택 표시로도 읽힌다** |

위계가 죽지 않는 이유: 라벨은 `smallBold`, 힌트는 `small`이라 굵기가 층을 이미 지고 있다.
게다가 비활성 쪽의 `textSecondary`(4.78) / `textMuted`(4.82)는 대비가 사실상 같아서
**색으로 층을 나눈 적이 원래 없었다.**

### ③ 규칙을 주석으로 박았다

`accentTint` 주석에 **「이 면 위 글자색은 `accent`와 `text`뿐」**을 미달 수치와 함께 적었다
(`textMuted` 4.28 · `textSecondary` 4.24 · `positive` 4.05). `backgroundSelected`에도 포인터를 달았다.

**17곳을 전수 확인했다.** ① 말고는 문제없다:

- 아이콘 타일(`stats-entry-button`·`StatsScreen`·`WrsVoiceGuideScreen`·`ListeningCheckScreen`·
  히어로 마크 3종) → 내용물이 `accent`
- 카드형(`WrsTabScreen`·`PtaSessionScreen`) → 글자가 틴트 **밖**에 있다(`cardIcon` / `cardText` 분리)
- `PitchCompareScreen:146` → `pressed` 순간만 틴트, 글자는 `text`
- `WrsBingoScreen:570` 트레이 → 타일이 각자 흰 면/`accent` 면을 깔고 그 위에 글자가 얹힌다
- `app-tabs.tsx:15` 인디케이터 → 선택 라벨이 `accent`(4.52), 비선택은 흰 면의 `textMuted`(4.82)

### ⑤ 하드코딩 hex — 옮기다가 실제 문제를 하나 찾았다

`Ling6ProgressPanel.tsx`의 두 상수를 없앴다.

- `BAR_TRACK_BG "#EEF3F8"` = `borderSubtle`과 **같은 값**이라 그냥 토큰으로 바꿨다
- `BAR_MUTED "#D5DDE6"` → 새 토큰 **`chartMuted #94A7BB`**

옮기기 전에 재 봤더니 `#D5DDE6`는 트랙 대비가 **1.23:1**이었다. 강조 안 된 막대가
**사실상 안 보인다**는 뜻이다. 6개 소리의 아쉬움 횟수를 막대 높이로 견주는 패널인데
견줄 대상이 보이지 않으면 패널의 기능이 죽는다. **2.21:1**로 올렸다.

3:1까지 올리지 **않은** 이유: `#7F94AC`(2.79) 이상이면 `highlight`(트랙 대비 **2.70**)를 넘어서
「강조 안 된 막대가 강조된 막대보다 세다」가 된다. 색이 유일한 단서도 아니다 —
높이 + 라벨 색 전환 + `accessibilityLabel`의 횟수가 이미 있다(인계문 ④에서 확인된 사항).

**인계문 ⑤의 괄호는 파일 경로가 틀렸다.** `animated-icon.tsx:143`은
`src/components/ui/`가 아니라 `src/components/`에 있다. 판단은 맞다 —
`#208AEF`는 `AnimatedSplashOverlay` 전용이고 **`app.json`의 네이티브 splash `backgroundColor`와
같은 값이어야 한다.** 토큰으로 옮기면 네이티브 설정과 끊어진다. 그대로 둔다.

이로써 `src/` 안에서 팔레트를 우회하는 hex는 **splash 3개(`#208AEF` + 그라디언트 2)뿐**이고,
그건 팔레트가 아니라 네이티브 설정의 사본이다.

### 왜 두 커밋인가

앞 커밋들과 같은 기준 — **되돌릴 이유가 다르다.**
`95d50ac`는 수치가 강제한 교정이라 되돌릴 이유가 없다.
`fd2e693`은 막대 색이 눈에 띄게 진해지는 **보이는 변경**이라 취향으로 거부당할 수 있다.
붙여 두면 막대를 되돌리려다 규칙까지 딸려 나간다.

### 막힌 곳

**여전히 실기기·에뮬레이터로 본 적이 없다.** 이 블록의 수치도 전부 계산값이다.
특히 `chartMuted`는 「보이는가」를 눈으로 확인해야 값이 맞는지 안다.

### 미결

1. **② 카드 경계** — `border` 1.20:1 · 카드 vs 배경 1.06:1. 인계문대로 **위반이 아니라 판단 사항**이고
   실기기 없이는 못 정한다. 손대지 않았다
2. **`chartMuted 2.21`이 충분한가** — 실기기에서 봐야 한다. 부족하면 `#8A9EB4`(2.47)까지가
   `highlight`를 넘지 않는 한계다
3. **14px 상향이 좁은 화면에서 넘치는지** — 앞 블록에서 넘어온 미결. 그대로 열려 있다

---

## 2026-08-21 · 커밋 3분할 — 가독성 / ScreenHeader / 동작

**브랜치**: `ui_chips_suisei`
앞 블록 「남은 결정 1」을 실행했다. 22개 파일 한 덩어리를 성격별 3커밋으로 나눴다.

### 왜 나눴나

셋은 되돌릴 이유가 서로 다르다.

| 커밋 | 내용 | 되돌릴 상황 |
|---|---|---|
| ① 가독성 | 색 4개 + 글자 크기 47곳 | 실기기에서 글자가 넘칠 때 — **이것만** 되돌리면 된다 |
| ② `ScreenHeader` | 공용 컴포넌트 + 7개 화면 + 죽은 스타일 15블록 | 헤더 짜임을 다시 갈 때 |
| ③ 동작 | `BackHandler` · 터치 타깃 · 기록 지우기 위치 · jest 리졸버 | 거의 없음(버그 수정) |

①이 가장 되돌릴 가능성이 높고 ③이 가장 낮다. 붙여 두면 ①을 빼려다 ③까지 딸려 나간다.

### 나누기가 까다로웠던 이유

**파일이 그룹 간에 겹친다.** 6개 파일이 두세 그룹에 걸쳐 있다
(`StatsScreen`은 셋 다, `Ling6SessionScreen`·`WrsSessionScreen`·`WrsTwoCharScreen`도 셋 다).
그래서 파일 단위가 아니라 **헝크 단위**로 갈랐다 — 전체 diff 67헝크를 분류.

**순서가 강제된다.** ①을 먼저 넣어야 한다. ②가 지우는 `caption` 스타일 블록의 내용이
①에서 `fontSize: 12` → `14`로 바뀌기 때문에, ②를 먼저 넣으면 문맥이 안 맞아 적용에 실패한다.
→ ① 스테이지·커밋 → **남은 diff를 다시 뽑아** ② 분류 → 커밋 → 나머지 ③.

**헝크 하나는 손으로 쪼갰다.** `StatsScreen`의 스타일 헝크에 `otherValue`·`clearKindLabel`의
글자 크기(①)와 `footer`·`clearKind` 재배치(③)가 한 덩어리로 들어 있었다.
`git apply --cached --recount`로 줄 수 재계산을 맡기고 둘로 갈랐다.

### 딸려 들어간 것 하나

`SessionModeToggle.tsx`의 `ReadonlyArray<{...}>` → `readonly {...}[]`.
`eslint --fix`가 바꾼 것으로, 내가 의도한 변경이 아니다. 되돌릴 만큼의 문제가 아니라
같은 파일의 글자 크기 변경과 함께 **①에 담았다**. 기능 변화 없음.

### 검증

세 커밋 각각이 성립하는 상태다(중간 커밋도 컴파일된다).
최종 상태에서 `eslint` 0 errors · `tsc` exit 0 · `jest` 23 suites / 230 tests 통과.
**실기기 확인은 여전히 안 했다.**

---

## 2026-08-21 · 현재 상태 스냅샷 — 커밋 전 정리

**브랜치**: `ui_chips_suisei` (`feat_wrs_voice_guide`에서 분기)
**커밋된 것**: `0b2c535` 탭바 작업 1개뿐 — `StatsScreen.tsx` + 이 문서
**커밋 안 된 것**: 그 뒤 작업 **전부**. 워킹 트리 23개 항목(수정 22 + 신규 1).

### 문서 정리 내역

이 문서는 이제 최신 순 4블록이다. 누적 규칙상 옛 블록은 고치지 않고, 뒤집힌 판단은
**새 블록에서 닫는** 방식으로 처리했다.

| 블록 | 성격 | 뒤에서 뒤집힌 것 |
|---|---|---|
| 추천 항목 실행 | 실행 | — |
| 색 대비 조사 | 조사 | 추천 순서를 이걸로 대체 |
| UI 개선 추천 6건 | 조사 | **3번(AM 헤더) 철회** · 6번 「추정」 → 확정 · 색 축 누락 |
| 통계 화면 칩 → 탭바 | 실행 | `@testing-library` 미결 항목이 닫힘 |

즉 **오래된 블록일수록 뒤에 정정이 붙어 있다.** 나중에 읽을 때 맨 위부터 읽어야 한다.

### 지금 남은 결정 두 가지

1. **커밋 여부** — 22개 파일 변경이 한 덩어리다. 성격이 셋으로 갈려서 나눠 담을 수도 있다:
   가독성(색+크기) / `ScreenHeader` 도입 / `BackHandler`·터치 타깃·기록 지우기 위치
2. **실기기 확인** — 글자 상향이 좁은 화면에서 넘치는지. 이걸 먼저 하면 커밋 내용이 바뀔 수 있다

### 이 블록의 성격

코드 변경 없음. 문서·상태 정리만.

---

## 2026-08-21 · 추천 항목 실행 — 가독성(1+7) · ScreenHeader(4) · 8 · 2 · 5 · 6

**브랜치**: `ui_chips_suisei`
**변경**: 21개 파일 수정 + 1개 신규(`src/components/ui/screen-header.tsx`)
**검증**: `eslint` 0 errors · `tsc` exit 0 · **`jest` 23 suites / 230 tests 전부 통과**
**실기기 확인은 여전히 안 했다.** 레이아웃이 커진 곳(글자 14px 상향)은 눈으로 봐야 한다.

### 먼저 — 3번(AM 헤더)은 **틀린 진단이었다. 실행하지 않았다**

`AmSessionScreen`을 고치려고 열어 보니 제목이 **없는 게 아니라 다른 자리에 있었다.**

- `AmSessionScreen.tsx:341` — idle 화면 한가운데 히어로에 `type="heading"` **"떨림 찾기"** + 캡션이 이미 있다
- 같은 짜임을 `FreqSessionScreen`·`PitchCompareScreen`도 쓴다(`heroCaption`·`heroPromptScaled` 스타일이 셋 다 있음)
- 즉 화면은 두 부류다:
  - **목록·선택 화면**(WrsTab·Pta) + **WRS/링6 세션** → 왼쪽 위 `headerRow` 제목
  - **히어로 세션**(떨림·다른 음 찾기·높낮이 비교) → 가운데 큰 아이콘 + `heading` 제목
- AM이 `statsRow`(제목 없이 버튼만)인 이유도 있다 — AM 탭은 **피커 화면이 없어서**
  (탭 = 곧 연습) 통계 버튼이 세션 화면에 얹힐 자리가 거기뿐이다

→ 여기에 `ScreenHeader`를 넣으면 **한 화면에 제목이 둘**이 된다. 손대지 않았다.
앞 블록의 3번 항목은 **철회한다.**

### 1+7. 가독성 — 크기와 색을 같이

**색 (`src/constants/theme.ts`)** — 색조 유지, 명도만 낮춤. 각 값 옆에 대비비를 주석으로 박아 뒀다.

| 토큰 | 이전 | 이후 | 흰 카드 대비 |
|---|---|---|---|
| `textMuted` | `#8A9BAD` | `#607489` | 2.85 → **4.82** |
| `textSecondary` | `#6B7A8A` | `#667484` | 4.40 → **4.78** |
| `positive` | `#1F9D6B` | `#1A865B` | 3.45 → **4.56** |
| `highlight` | `#F5833F` | `#F36C1C` | 2.56 → **3.01** (8번) |

- 옛 hex를 하드코딩한 곳은 없었다(grep 확인) → 토큰만 바꾸면 전부 따라온다
- `highlight` 주석에 **「글자색으로 쓰지 말 것」**을 명시했다 — 3:1은 비텍스트 기준이다

**크기 — 47곳 일괄 상향**

- 스크립트로 `fontSize < 14`를 전부 14/20으로. `* TEXT_SCALE`(1.2)은 **실효 크기로 판정**해서
  이미 14 이상인 것(12*1.2 = 14.4 등)은 건드리지 않았다 — 바뀐 건 `ListeningCheckScreen` 1곳뿐
- `themed-text.tsx`의 `mono`·`code` 타입 자체가 12px였다 → 14/20. 이걸 쓰는 곳이 전부 따라 올라감
- `pill.tsx`: `MONO_BASE` 12/16 → 14/20. mono와 small이 같아지면서 **`surface` 전용 크기(13/18) 분기가 죽어** 같이 지웠다
- **SVG 차트 내부 눈금 라벨 `fontSize={9}` 3곳은 그대로 뒀다**
  (`TrendChart:129` · `Ling6ProgressPanel:217` · `WrsProgressPanel:137`).
  차트 기하(`PAD_BOTTOM` 등)를 같이 손봐야 하는데 **눈으로 확인 못 하는 상태에서 건드릴 곳이 아니다.** 미결로 남긴다

### 4. 공용 `ScreenHeader` — 신규 `src/components/ui/screen-header.tsx`

`{ title, caption?, action? }`. 6개 화면이 각자 갖고 있던 같은 마크업을 흡수했다.

| 적용 | action |
|---|---|
| `WrsSessionScreen` · `WrsTwoCharScreen` · `Ling6SessionScreen` | `StatsEntryButton`(idle·summary에서만) |
| `PtaSessionScreen` | `StatsEntryButton` |
| `WrsTabScreen` · `WrsBingoScreen` · `StatsScreen` | 없음 |

- **죽은 스타일 15개 블록 삭제** — `header`·`headerRow`·`caption`·`screenHeader`
- 캡션이 14/20이 되면서 `small` 타입 기본값과 같아져 **인라인 `style={styles.caption}`이 통째로 필요 없어졌다.**
  이게 원래 노렸던 "타입으로 흡수"다
- `StatsScreen` 캡션의 `numberOfLines={1}`도 뺐다 — 14px로 커지면 잘린다. 이제 줄바꿈된다
- 컴포넌트 주석에 **히어로 화면은 이걸 쓰지 않는다**고 못 박아 뒀다(위 3번 참고)
- `WrsVoiceGuideScreen`은 아이콘 마크가 제목 위에 오는 별도 짜임이라 제외

### 2. 터치 타깃 — 이번 탭 마무리

`StatsScreen`의 `styles.tab`에 `minHeight: 44` + 좌우 여백 `Spacing.three`. 32px → 44px.

### 5. 「기록 지우기」를 본문 맨 아래로

- 하단 고정 `footer`에서 빼내 **스크롤 본문 맨 끝**(「다른 연습」 카드 아래)으로 옮김
- `footer`에는 「뒤로 가기」만 남고 `fill`로 가로를 채운다
- 오른쪽 정렬 → **가운데 정렬**, 기록이 없으면 아예 렌더하지 않는다(전엔 흐리게 남아 있었다)

### 6. `BackHandler` — 추정이 아니라 **실제 문제였다. 고쳤다**

`src/components/app-tabs.tsx`가 **`NativeTabs`**(expo-router unstable-native-tabs)를 쓴다
→ 탭 화면이 마운트된 채 남는다 → 앞 블록의 우려가 성립한다.

- **재현 경로**: 링 6 연습을 시작(phase = playing)한 채 다른 탭으로 이동 → 그 탭에서 뒤로가기
  → 링 6의 `confirmEndSession` 알림이 뜬다
- 평범한 `useEffect`였던 4곳을 `useFocusEffect`로 바꿨다:
  `Ling6SessionScreen` · `WrsSessionScreen` · `WrsTwoCharScreen` · `WrsBingoScreen`
- 이유를 주석으로 남김 — 「`BackHandler`는 등록 역순으로 먼저 true를 문다」
- 이미 맞게 돼 있던 `AmTabScreen`·`PtaSessionScreen`·`WrsTabScreen`과 이제 전부 같은 모양

### 막힌 곳과 처리

**테스트가 깨졌다 — 내 변경 때문이다.**

`StatsScreen`에 넣은 `react-native-reanimated` import가 `WrsSessionScreen.test.tsx`를 통째로 넘어뜨렸다
(`TypeError: Cannot read properties of undefined (reading 'loadUnpackers')`).
import만으로 worklets 네이티브 모듈을 건드린다.

1. `jest.mock("react-native-reanimated", () => require("react-native-reanimated/mock"))` → **실패**.
   공식 목(`mock.ts`)이 내부에서 진짜 `index`를 import해서 같은 자리에서 넘어진다
2. `jest.config.js`에 **`resolver: 'react-native-worklets/jest/resolver.js'`** → **해결**.
   worklets 안에서만 `.native` 확장자를 빼 주는 패키지 제공 리졸버다
3. 1번 목은 **불필요해서 도로 뺐다** — 리졸버만으로 통과하는 걸 확인했고, 같은 일을 두 곳에서 하지 않는다

**`eslint --fix`가 범위 밖 파일을 건드렸다.**
저장소 테스트 4개에서 `jest.mock` 아래 두었던 import를 위로 끌어올렸다(`import/first`).
의도적으로 그렇게 둔 배치이고 이번 작업과 무관해서 **`git checkout`으로 되돌렸다.**
그 4개 파일의 `import/first` 경고 14건은 그대로 남아 있다.

### 결과 로그 — 변경 규모

```
 jest.config.js                                |   3 +
 src/components/themed-text.tsx                |   8 +-
 src/components/ui/pill.tsx                    |  11 +-
 src/constants/theme.ts                        |  23 +-
 src/training/ListeningCheckScreen.tsx         |   4 +-
 src/training/SessionModeToggle.tsx            |   8 +-
 src/training/SessionTrendPanel.tsx            |  30 +--
 src/training/StatsScreen.tsx                  |  61 +++---
 src/training/SummaryCard.tsx                  |   4 +-
 src/training/TrendChart.tsx                   |   4 +-
 src/training/am/AmSessionScreen.tsx           |  12 +-
 src/training/freq/FreqSessionScreen.tsx       |  12 +-
 src/training/ling6/Ling6ProgressPanel.tsx     |  20 +-
 src/training/ling6/Ling6SessionScreen.tsx     |  74 +++----
 src/training/pitch2afc/PitchCompareScreen.tsx |   8 +-
 src/training/pta/PtaSessionScreen.tsx         |  30 +--
 src/training/wrs/WrsBingoScreen.tsx           |  45 ++--
 src/training/wrs/WrsProgressPanel.tsx         |  12 +-
 src/training/wrs/WrsSessionScreen.tsx         |  84 +++----
 src/training/wrs/WrsTabScreen.tsx             |  28 +--
 src/training/wrs/WrsTwoCharScreen.tsx         |  80 +++----
 22 files changed, 546 insertions(+), 316 deletions(-)
```

신규(추적 전): `src/components/ui/screen-header.tsx` — 50줄.
`ui_tn수정.md`는 위 통계에서 제외했다(문서).

### 결과 로그 — 검증 명령 출력

```
$ npx eslint src --ext .ts,.tsx
✖ 14 problems (0 errors, 14 warnings)
eslint exit: 0
  └ 경고 14건은 전부 저장소 테스트 4개의 import/first — 손대지 않기로 한 것

$ npx tsc --noEmit -p tsconfig.json
tsc exit: 0

$ npx jest --silent
Test Suites: 23 passed, 23 total
Tests:       230 passed, 230 total
jest exit: 0
```

`jest`가 처음 통과했다. **앞 블록에서 「미결」로 적었던 `@testing-library/react-native` 미설치가
해소돼 있었다** — `node_modules/@testing-library/react-native`가 생겼고, 그래서
`tsc` 오류 4건도 함께 사라졌다. 그 항목은 닫힌다.

테스트 수가 228 → 230으로 는 것은 새 테스트를 쓴 게 아니라,
넘어지던 `WrsSessionScreen.test.tsx`(2건)가 이제 실제로 돌기 때문이다.

### 미결

- **SVG 차트 눈금 라벨 9px 3곳** — 차트 기하를 같이 봐야 해서 남김
- **카드 경계 판단**(앞 블록 「판단 사항」) — 실기기 확인 필요
- **실기기 확인 전체** — 글자가 커지면서 좁은 화면에서 넘치는 곳이 있을 수 있다.
  특히 `SessionModeToggle`(10.5 → 14, 배율 33%)과 `SessionTrendPanel`(8곳)
- **커밋 안 함** — 워킹 트리에 그대로 있다

---

## 2026-08-21 · 색 대비(contrast) 조사 — 앞 6건에서 빠졌던 축 (조사만, 변경 없음)

**브랜치**: `ui_chips_suisei`
**상태**: 조사·제안 단계. **코드 변경 없음.** 실기기 확인도 안 함.
**계기**: "컬러에 대한 추천은 안 보이는 거 같아 왜지?" — 정당한 지적.

### 왜 빠졌나

앞 블록(UI 개선 추천 6건)의 스캔 축이 **구조와 타이포**였다 — `fontSize` grep, 터치 타깃,
헤더 일관성, `BackHandler`. **대비비를 계산하는 패스를 아예 돌리지 않았다.**
색은 눈으로 보면 "깔끔한 의료기기 톤"이라 문제없어 보이고, 실제 문제는 **숫자를 뽑아야만**
드러나는데 그 단계를 건너뛴 것. 놓친 축이 맞다.

### 계산 결과 — `Colors.light` 전체 (WCAG 2.1 상대휘도)

| 색 | 배경 | 비율 | 판정 |
|---|---|---|---|
| `text` #10233A | 흰 카드 | 15.87 | AA ✓ |
| `text` #10233A | 배경 #F6F9FD | 15.03 | AA ✓ |
| `accent` #1668E3 | 흰 카드 | 5.09 | AA ✓ |
| `onAccent` #FFFFFF | `accent` | 5.09 | AA ✓ — **이번 탭바 안전** |
| `danger` #9B3B3B | 흰 카드 | 6.81 | AA ✓ |
| `textSecondary` #6B7A8A | 배경 | **4.16** | 미달 (4.5 필요) |
| `textSecondary` #6B7A8A | 흰 카드 | **4.40** | 미달 (간발) |
| `positive` #1F9D6B | 흰 카드 | **3.45** | 미달 |
| `textMuted` #8A9BAD | 흰 카드 | **2.85** | **실패** |
| `highlight` #F5833F | 흰 카드 | **2.56** | **실패** |
| `border` #E4EBF3 | 흰 카드 | 1.20 | 아래 「판단 사항」 참고 |

### 7. `textMuted` 2.85:1 — 47곳에서 쓰이고, 14px 문제와 **곱해진다**

앞 블록 1번(14px 하한)과 독립된 문제가 아니라 **겹쳐서 악화된다.**

- 최악: `SessionModeToggle.tsx:109` — `textMuted` + **10.5px**. 작은 데다 흐리다
- **법적으로 중요한 문구가 전부 `textMuted`다**:
  `PitchCompareScreen.tsx:236`의 disclaimer,
  `Ling6SessionScreen.tsx:373` *"연습 기록이에요. 청력 검사·진단 결과가 아니에요."*
- → 1번을 「가독성」 항목으로 재편할 때 **크기와 색을 같이** 봐야 한다

### 8. `highlight` 2.56:1 — 차트에서 가장 봐야 할 점이 가장 안 보인다

세 추이 그래프 모두 **최근 1회 점**을 `highlight`로 칠한다.

- `TrendChart.tsx:153-154` · `WrsProgressPanel.tsx:160-161` · `Ling6ProgressPanel.tsx:240-241`
- `Ling6ProgressPanel.tsx:128` — 약한 소리 막대 채움색
- 비텍스트 요소 기준 **3:1에도 못 미친다**

### 색 단독 인코딩 — 확인해 보니 문제없음

방어가 이미 돼 있다. 이 축은 손댈 것 없다.

| 위치 | 색 외의 단서 |
|---|---|
| `Ling6ProgressPanel:128` 약한 소리 막대 | 막대 **높이** + 라벨 색 전환(`text`/`textMuted`) + `accessibilityLabel`에 횟수 |
| 추이 그래프 최근 점 | **채움 원 vs 빈 원**(`fill={isLast ? highlight : surface}`) |
| `WrsBingoScreen:476` 3색 | 컨페티 — 의미 없음(장식) |

### 제안값 — 색조 유지, 명도만 낮춤

```
textMuted      #8A9BAD → #607489   2.85 → 4.56
textSecondary  #6B7A8A → #667484   4.16 → 4.52
positive       #1F9D6B → #1A865B   3.45 → 4.56
highlight      #F5833F → #F36C1C   2.56 → 3.01  (비텍스트 3:1 충족) ← 권장
                       → #C8510A   2.56 → 4.53  (텍스트로도 쓸 경우)
```

`highlight`는 **오렌지 특성상 4.5:1을 맞추면 벽돌색**이 되어 "따뜻한 대비색"이라는
`theme.ts:38`의 원래 의도가 죽는다. 차트 마크로만 쓰이므로 3:1인 `#F36C1C`를 권한다.

`positive`는 `Ling6SessionScreen.tsx:368`에서 **`smallBold` 14px 글자색**으로 쓰인다.
WCAG 대형 텍스트 기준은 18.66px 굵은 글씨부터라 여기엔 해당 없음 → 4.5:1이 필요하다.

### 판단 사항 — 카드 경계 (위반 아님, 트레이드오프)

- `border` #E4EBF3가 흰 카드 위에서 **1.20:1**
- 카드(흰색)와 배경(#F6F9FD)의 차이도 **1.06:1**
- 즉 카드 윤곽이 거의 테두리 + `Shadows.card`(opacity 0.08)에만 의존한다
- 3:1을 맞추려면 `#7397C1` 수준 → **「Clean Clinical」 톤이 무너진다**
- → 규칙 위반으로 처리하지 말고, **실기기에서 노안 조건으로 카드 구분이 되는지** 확인할 것

### 다크 모드

`theme.ts:14-16`에 **의도적으로 안 하기로 한 결정**(2026-08-11)이 기록돼 있다. 재론하지 않는다.

### 추천 순서 (앞 블록 것을 이걸로 대체)

**1+7 「가독성」으로 묶기(크기 + 색 동시) → 4 → 3 → 8 → 2 → 5 → 6 확인 → 카드 경계 판단**

### 조사 근거(재현용)

WCAG 2.1 상대휘도 공식으로 직접 계산했다. 재현 스크립트:

```js
const lin = v => { v /= 255; return v <= 0.03928 ? v / 12.92 : Math.pow((v + 0.055) / 1.055, 2.4); };
const L = h => { const r = parseInt(h.slice(1,3),16), g = parseInt(h.slice(3,5),16), b = parseInt(h.slice(5,7),16);
  return 0.2126*lin(r) + 0.7152*lin(g) + 0.0722*lin(b); };
const CR = (a,b) => { const l1 = L(a), l2 = L(b); return (Math.max(l1,l2)+0.05)/(Math.min(l1,l2)+0.05); };
// 기준: 본문 4.5:1 · 대형 텍스트(18.66px 굵게 / 24px) 3:1 · 비텍스트 UI 3:1
```

- `textMuted` 사용처: `grep -rn "textMuted" src --include=*.tsx` → **47건**
- `highlight` 사용처: `grep -rn "highlight" src --include=*.tsx`

---

## 2026-08-21 · UI 개선 추천 6건 — 코드 훑기 결과 (조사만, 변경 없음)

**브랜치**: `ui_chips_suisei`
**상태**: 조사·제안 단계. **코드 변경 없음.** 실기기 확인도 안 함.
**계기**: 탭바 작업 뒤 "그 외에 UI 개선 등을 이유로 추천할 것이 있을까?" 질문.

### 추천 순서

**1 + 4를 묶어 `ScreenHeader` + `ThemedText` 타입 정리 → 3이 딸려 옴 → 2 → 5 → 6 확인.**

---

### 1. 14px 하한 위반 50여 곳 — 가장 큰 건

`themed-text.tsx:62`에 프로젝트가 스스로 못 박아 둔 규칙:

> *"화면에서 fontSize를 11~13으로 인라인으로 줄이지 말 것 — 이 타입을 쓴다.
> (고령·난청 사용자 가독성: 14px 미만 금지.)"*

**18개 파일 50여 곳이 이 규칙을 어기고 있다.** 차트 축 라벨(`TrendChart.tsx:187` 등 SVG 텍스트)을
예외로 쳐도 대부분은 본문·캡션이다.

| 위치 | 크기 |
|---|---|
| `SessionModeToggle.tsx:141` | **10.5px** |
| `Ling6ProgressPanel.tsx:306` · `AmSessionScreen.tsx:713` · `FreqSessionScreen.tsx:687` | 11px |
| 5개 훈련 화면의 `caption` | 전부 12px |
| `themed-text.tsx:111,128` (`mono` 타입 자체) | 12px — 이걸 쓰는 곳이 전부 딸려 들어감 |

- `allowFontScaling`을 끈 곳은 **없다** → OS 글자 크기 설정은 살아 있음
- 다만 기본 배율에서 10.5~12px는 이 앱 대상 사용자에게 작다
- **화면별로 고치지 말고 `ThemedText` 타입으로 흡수**하는 게 맞다.
  `caption` 14px가 이미 그 용도로 있는데 안 쓰이고 있음

### 2. 터치 타깃 44px 미만 — 이번에 만든 탭도 해당

- `hitSlop`이 앱 전체에서 `stats-entry-button.tsx:19` **한 곳뿐**
- 이번에 만든 `styles.tab` — `paddingVertical: 6` + `lineHeight: 20` = **약 32px**.
  기존 칩과 같은 높이라 회귀는 아니지만, **이번에 같이 올렸어야 했다**
- `ClearKindButton`은 `minHeight: 44`가 이미 있음 → 이 패턴을 탭에도 적용하면 된다

### 3. 떨림(AM) 화면만 헤더 구조가 다름

| 화면 | 구조 |
|---|---|
| WRS 한 글자·두 글자, 링 6, PTA | `headerRow` — 제목 + 통계 버튼(`space-between`) |
| **`AmSessionScreen.tsx:325`** | `statsRow` — **제목 없이** 버튼만 오른쪽 정렬 |

떨림 탭만 화면 제목이 없어 "여기가 어디인지"가 안 보인다. 다른 셋에 맞출 것.

### 4. `caption` 스타일이 5개 화면에 복붙

`fontSize: 12, lineHeight: 18`이 WRS 2개·PTA·링6·StatsScreen에 **각각 따로** 정의돼 있다.
1번을 고칠 때 **공용 `ScreenHeader` 컴포넌트**로 묶으면 3번(AM 헤더)도 같이 해결된다.

### 5. 「기록 지우기」가 「뒤로 가기」와 같은 줄

`StatsScreen` 하단 `footer`에 파괴적 동작이 주 동작과 나란히 상시 노출.
`Alert` 확인이 있어 오작동 위험은 낮지만 배치상 눈에 너무 잘 띈다.
→ 스크롤 본문 맨 아래로 내리는 쪽을 권함.

### 6. UI는 아니지만 — `BackHandler` 훅이 화면마다 다름

| `useFocusEffect` (맞음) | 평범한 `useEffect` (의심) |
|---|---|
| `AmTabScreen:61` · `PtaSessionScreen:96` · `WrsTabScreen:107` | `Ling6SessionScreen:155` · `WrsSessionScreen:116,123` · `WrsTwoCharScreen:130,137` · `WrsBingoScreen:111,118` |

탭 화면들이 동시에 마운트된 채로 남으면, 포커스 없는 화면의 핸들러가 등록된 채로 남아
뒤로가기를 가로챌 수 있다. RN `BackHandler`는 **등록 역순(LIFO)** 으로 호출하고 먼저 `true`를
반환한 핸들러에서 멈추기 때문.

**단, 이건 추정이다** — 탭 마운트 유지 여부에 달려 있어 실기기로 확인해야 확정된다.

### 조사 근거(재현용 명령)

- 14px 위반: `grep -rn "fontSize: 1[0-3]\b\|fontSize: 1[0-3]\." src --include=*.tsx`
- 글자 배율: `grep -rn "allowFontScaling\|maxFontSizeMultiplier" src` → **0건**
- 터치 타깃: `grep -rn "hitSlop" src --include=*.tsx` → **1건**
- 뒤로가기: `grep -rn "BackHandler" src` 후 각 파일에서 감싼 훅 확인

---

## 2026-08-21 · 통계 화면 종목 칩 → 탭바 (`StatsScreen.tsx`)

**브랜치**: `feat_wrs_voice_guide`
**변경 파일**: `src/training/StatsScreen.tsx` 1개 (+242 / −52)

### 요청

> "칩들이 각 탭을 나타내는 통계의 진입점인데, 지금보다 **누르면 통계 화면이 나온다**는 느낌의 UI를 줄 수 있을까?"

### 사전 확인 — 헤더 구조 정정

질문이 "헤더에 통계 버튼들이 나열되어 있지?"로 시작했는데, 실제 구조는 그렇지 않았다.

| 위치 | 실제 |
|---|---|
| 각 연습 화면 헤더 | 통계 버튼 **1개뿐** (`StatsEntryButton`, 차트 아이콘 60×40). `phase === "idle" \|\| "summary"`일 때만 렌더 |
| 호출 5곳 | `WrsSessionScreen:285` · `WrsTwoCharScreen:301` · `Ling6SessionScreen:294` · `PtaSessionScreen:176` · `AmSessionScreen:326`(여기만 제목 없이 `statsRow` 단독 줄) |
| 통계 화면(`StatsScreen`) | **여기가 "나열"** — `screenHeader` **바깥**의 형제 요소로 `KindChips` 6개가 가로 스크롤 |

→ "나열"된 것은 헤더의 버튼들이 아니라 통계 화면의 **종목 칩 줄**이었다.

### 방향 결정

세 안을 제시하고 A안 채택.

- **A안 (채택)** 탭바 + 패널 제목줄 + 등장 애니메이션 — 한 화면 구조 유지, 되돌리기 쉬움
- B안 드릴다운 — 목록 카드 → 상세 화면 push. 가장 확실하지만 헤더 차트 버튼으로 들어온 사용자에게 한 단계가 더 생김
- C안 칩 강조만 — 코드 변경 최소, "화면이 열린다" 느낌은 가장 약함

### 실제 변경

**1. `KindChips` → `KindTabs`**

- 활성 탭: 옅은 틴트(`accentTint` 면 + `accent` 글씨) → **진한 파란 면 + 흰 글씨**(`accent` / `onAccent`)
- 활성 탭 아래 **삼각 꼬리** 추가 — RN에 도형이 없어 테두리 트릭(`borderTopWidth` 6 + 좌우 transparent).
  비활성은 `opacity: 0`으로 **자리만 유지** → 줄 높이가 흔들리지 않음
- **고른 탭이 잘리면 가운데로 스크롤**(`revealTab`). 종목이 6개라 한 화면에 안 들어오는데,
  기존엔 헤더 차트 버튼으로 「떨림」(마지막 탭) 통계를 열면 칩이 화면 밖에 있었다.
  각 슬롯 `onLayout`으로 x/width를 ref에 모으고, ScrollView `onLayout`·슬롯 `onLayout`·누를 때 각각 호출
- 라벨은 활성/비활성 **모두 `smallBold`**로 통일 — 굵기가 바뀌면 칩 너비가 변해 줄이 덜컹거린다
- 접근성 역할 `button` → `tab`, 컨테이너에 `tablist`

**2. `PanelHeading` 신규**

그래프 위에 **종목 아이콘 타일(44×44) + 이름(18px/700) + `기록 N회`**. 지금 보는 게 뭔지 그림과 글로 못 박는다.
아이콘은 각 종목이 훈련 화면에서 쓰는 것 그대로 재사용(`KIND_ICON`):

| kind | icon |
|---|---|
| `ling6` | `headphones` |
| `pitch2` | `bars` |
| `freq` | `findTone` |
| `wrs1` / `wrs2` | `oneChar` / `twoChar` |
| `am` | `vibrate` |

`panelTitle`은 18px — 화면 제목(`screenTitle` 23px) 한 단 아래이면서 **14px 하한 위**라
프로젝트의 "인라인으로 fontSize를 11~13으로 줄이지 말 것" 규칙에 걸리지 않는다.

**3. 등장 애니메이션**

- `Animated.View key={kind}` + `FadeInRight` / `FadeInLeft`, 220ms(`ENTER_MS`)
- 탭 줄에서 **오른쪽으로 가면 오른쪽에서** 들어온다
- 제목줄·그래프·「다른 연습」을 **한 덩어리로 묶어** 같이 밀려 들어오게 함 → "화면이 교체됐다"로 읽힘
- key가 `kind`라 **기록 삭제 후 `feed`만 갱신될 때는 움직이지 않는다**
- 종목 변경 시 본문 스크롤을 맨 위로 되돌림(`bodyRef.scrollTo({ y: 0 })`)

### 막힌 곳과 처리

- **`react-hooks/refs` 린트 위반**: 방향(`forward`)을 `useRef`에 담고 렌더 중 `.current`를 읽었더니
  *"Cannot access refs during render"* 에러. `kind`와 방향을 **한 상태로 묶어**(`TabView = { kind, forward }`) 해결.
  방향은 "어디서 어디로 갔나"라서 별도 state로 두면 한 프레임 어긋난다
- **Bash 히어독이 이 환경에서 깨짐**: `cat > file <<'TSX'`가 `unexpected EOF while looking for matching`으로
  실패(한글·백틱 섞인 대용량 내용). Write 도구로 전환해서 해결

### 검증

- `npx eslint src/training/StatsScreen.tsx` → **통과**
- `npx tsc --noEmit` → **StatsScreen 관련 오류 없음**
- **실기기/에뮬레이터 실행은 안 함** — 눈으로 확인하지 않았다

### 미결 / 기존 문제

- `@testing-library/react-native`가 `devDependencies`에는 있으나 `node_modules`에 **실제로 없음**
  (`node_modules/@testing-library`에 dom·jest-dom·user-event만 존재).
  이 때문에 `tsc`에 테스트 파일 오류 4건이 남는다 — **이번 변경과 무관한 기존 문제**, `npm i` 한 번 필요
- 이번에 손대지 않은 기존 14px 하한 위반: `caption`(11.5) · `otherValue`(12.5) · `clearKindLabel`(12.5)
