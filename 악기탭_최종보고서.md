# 악기 소리 탭 — 계획부터 완성까지 최종 보고서

> **성격**: 「악기 소리 듣고 고르기」 탭의 **음원 조달과 재생 방식**이 어떻게 정해졌는지에
> 대한 결산. 무엇을 골랐는지보다 **무엇을 버렸고 왜 버렸는지**에 무게를 뒀다.
>
> **날짜**: 2026-08-29 · **브랜치**: `new-content` · **완료 커밋**: `e15f0cb` · `9a1e53f`
>
> **대화 원본은 따로 있다** — `wav음원생성.md`(996줄), `wav음원생성1.md`(677줄).
> 이 문서는 그 둘의 결론만 추린 것이고, 판단의 세부 근거는 원본에 있다.
>
> **정본은 코드다** — `src/training/inst/`, `scripts/inst-sources.mjs`.
> 「지금 어떻게 동작하는가」를 이 문서에서 찾지 말 것.

---

## 목차

- [한 줄 결론](#한-줄-결론)
- [1. 무엇이 들어갔나](#1-무엇이-들어갔나)
- [2. 왜 이 길인가 — 여섯 갈래 비교](#2-왜-이-길인가--여섯-갈래-비교)
- [3. 계획이 바뀐 네 지점](#3-계획이-바뀐-네-지점)
- [4. 세부 결정과 이유](#4-세부-결정과-이유)
- [5. 틀렸던 것](#5-틀렸던-것)
- [6. 라이선스](#6-라이선스)
- [7. 검증](#7-검증)
- [8. 남은 것](#8-남은-것)

---

# 한 줄 결론

**하모닉 합성음을 걷어내고, University of Iowa 무향실 녹음에서 뽑은 실물 연주 wav 16개로 갈아 끼웠다.** 사운드폰트(FluidR3_GM)를 거치는 계획이 중간까지 서 있었으나, 라이선스·용량·잔향·작업량 넷 다 CC0 성격의 실물 샘플이 이겨서 **한 단계 통째로 건너뛰었다.**

---

# 1. 무엇이 들어갔나

| | |
|---|---|
| `assets/inst/*.wav` | **16개**(악기 4 × 음 4). 모노 44.1kHz · 1.6초 · 크기 정렬 · 2.2MB |
| `scripts/inst-sources.mjs` | 출처표 — URL, 고른 이유, 어느 파일에서 어느 음을 뽑는지 |
| `scripts/fetch-inst-src.mjs` | 원본 10개 → `.inst-src/` (83MB, **커밋 안 함**) |
| `scripts/prep-inst-wav.mjs` | 분절 · 기음 검증 · 길이·크기 정렬 → `assets/inst/` |
| `docs/licenses/iowa-mis.txt` | 이용 허락 문장 원문, 녹음 조건, 원본 목록 |
| `src/training/inst/instSounds.ts` | 음원 16개 `require` 표 + 기음 목록 |

**지운 것**: `src/audio/instrumentTone.ts` · `instrumentSpectra.ts` (+ 테스트). 합성 경로가 없어졌다.

**안 건드린 것**: `src/app/inst.tsx`, `InstSessionScreen`의 과제 흐름. 예고대로 교체는 `instPlay.ts` 아래에서 끝났다 — 설계 당시 주석에 *"실물 wav로 갈아탈 때 손대는 곳은 `instPlay.ts` 하나"* 라고 박아 둔 것이 실제로 맞았다.

---

# 2. 왜 이 길인가 — 여섯 갈래 비교

「악기 소리를 어디서 가져오나」에 답이 여섯 개 있었다. 순서대로 검토했고 다섯을 버렸다.

## 갈래 ① 합성음을 계속 쓰고 품질을 올린다 — **버림**

하모닉 스펙트럼 + 포락선으로 만든 소리다(`instrumentTone.ts`). 오실레이터 하나에 `PeriodicWave`를 물려 모바일에서도 가벼웠다.

**버린 이유**: 「피아노 같은 소리」까지는 가도 **「피아노」가 안 된다.** 링 6가 먼저 같은 벽을 넘었다 — 사인 2개와 흰잡음 근사로 음소를 만들었다가 *"정상 청력에도 맞추기 어려운 품질 문제"* 로 실물 녹음에 자리를 내줬다(`ling6Play.ts` 주석). 악기 음색은 음소보다 더 어렵다.

**더 중요한 이유**: 이 탭의 미결 1번이 「음색이 실제로 구분되는가」인데, 합성음으로 "구분이 안 된다"는 결과가 나오면 **과제 설계 문제인지 합성 품질 문제인지 못 가른다.** 판정 자체가 불가능해진다.

## 갈래 ② 앱 안에서 실시간으로 MIDI를 합성한다 (사운드폰트 + 네이티브 브리지) — **버림**

`.sf2`를 앱에 넣고 iOS는 `AVMIDIPlayer`, Android는 FluidSynth를 JNI로 심는 안. 외부에서 받은 분석 글이 이걸 권했다.

**버린 이유 넷**:

1. **문제가 다르다.** 그 방식은 *"임의의 `.mid`를 재생한다"* 를 푼다. 이 탭에 필요한 소리는 **고정 16개**고 사용자가 연주하지 않는다.
2. **용량**: sf2 141MB, sf3로 줄여도 30MB대. 지금 wav 16개는 **2.2MB**다.
3. **못 붙는다.** 추천된 브리지 라이브러리 셋(`react-native-midi-playback` 0.1.4, `react-native-piano-sampler` 0.3.2, `react-native-soundfont` 0.0.9)은 전부 2022년 이후 갱신이 없고 구 아키텍처용이다. 이 앱은 **RN 0.86 · React 19.2 · 신아키텍처**다.
4. **구조를 깬다.** `git ls-files android` → 0개. 이 저장소는 Expo CNG라 `android/`·`ios/`가 없다. C++를 넣으려면 prebuild 산출물을 커밋하거나 config plugin을 새로 짜야 한다.

> 요약하면 **16개 고정 음을 트는 데 신디사이저 엔진을 앱에 넣을 이유가 없다.**

## 갈래 ③ 사운드폰트를 **빌드 시점에** 렌더해 wav로 굽는다 — **한때 채택, 뒤에 폐기**

FluidSynth로 `FluidR3_GM.sf2`를 오프라인 렌더(`-F`)해서 16개를 뽑는 안. 셸 명령이 아니라 Node가 만든 SMF를 먹이고, 리버브·코러스를 끄는 것까지 설계가 끝나 있었다.

**한동안 이게 1순위였던 이유**: 네 악기가 **한 출처**에서 나와 녹음 조건이 같다. 출처가 섞이면 잔향·마이크가 달라져 *"음색이 아니라 울림으로 고르기"* 가 된다 — 이 탭의 첫 번째 금기다.

**폐기한 이유**: 갈래 ⑤가 그 장점을 그대로 가지면서 나머지가 다 낫다(바로 아래 비교표). 그리고 사운드폰트 wav는 어차피 **임시본**이었다 — 합의된 종착지는 처음부터 「실물 녹음」이었으므로, 갈래 ⑤로 가면 **교체를 두 번 할 일이 한 번으로 준다.**

## 갈래 ④ 직접 녹음한다 — **보류(종착지 후보로 남김)**

**가장 안전하다**(라이선스 문제 자체가 없다). 그러나 **바이올린·플루트 연주자가 필요하다.** 피아노·기타는 몰라도 이 둘이 실질적 장벽이라 지금 단계에서 붙잡을 수 없다.

## 갈래 ⑤ CC0·퍼블릭 도메인 **실물 단음 샘플** — **채택**

University of Iowa Electronic Music Studios, Musical Instrument Samples.

| | 갈래 ③ 사운드폰트 렌더 | **갈래 ⑤ Iowa 실물 샘플** |
|---|---|---|
| 합의된 종착지(실물 녹음) | 임시본 → 나중에 또 교체 | **바로 충족. 교체 단계가 없어진다** |
| 네 악기 한 출처 | ○ | **○** (피아노·기타·바이올린·플루트 다 있음) |
| 잔향 | 렌더 때 리버브를 꺼서 관리 | **무향실 녹음 = 0** |
| 고지 의무 | MIT → **크레딧 화면 필요** | **없음** |
| 선행 작업 | FluidSynth 설치 + 141MB 다운로드 + SMF 생성기 | **없음.** ffmpeg는 이미 있었다 |
| 음질 | 루프·압축된 샘플 | 원본 녹음 |

**고지 의무가 없다는 점이 특히 컸다.** 사운드폰트(MIT)로 가면 크레딧 화면을 새로 만들어야 하는데, 이 앱엔 설정·크레딧 화면이 하나도 없고 **탭이 6개라 더 못 늘린다**(`app-tabs.tsx` 주석의 경고). 갈래 ⑤는 그 문제를 아예 만들지 않는다.

## 갈래 ⑥ 유료 샘플 라이브러리 — **버림**

돈 문제가 아니라 **약관 문제**다. 샘플 라이선스는 보통 「음악 제작(허용)」과 「샘플 재배포(제한)」를 가르는데, 우리가 앱에 넣는 건 **1.6초짜리 단음 16개**라 후자에 가깝다. 무료 CC0로 해결되는 일에 회색 지대를 밟을 이유가 없다.

---

# 3. 계획이 바뀐 네 지점

## ① 합성음 → wav (이전 세션에서 이미 합의)

처음부터 *"새로 필요한 건 악기 음원(wav)뿐"* 이 전제였고, 합성음은 **wav가 준비될 때까지의 임시본**으로 승인된 것이었다. 코드 주석 세 군데가 그 말을 반복하고 있었다. 이번 작업은 그 예고를 실행한 것이다.

## ② 사운드폰트 경유 → 실물 샘플 직행 (**이번 작업의 핵심 전환**)

계획은 「사운드폰트 임시본 → 나중에 실물 녹음」이었다. 사용자가 *"CC0 단음 샘플도 문제될 것이 없고 권고할 만한 것이 아닌가"* 라고 물었고, **그 지적이 맞았다.**

바뀐 계기는 사실 확인 하나였다 — **네 악기가 한 출처에 다 있느냐.** CC0 라이브러리는 대개 오케스트라 편중이라(VSCO 2 CE에는 기타·피아노가 없다) 출처를 섞어야 할 줄 알았고, 섞이면 잔향이 어긋나 갈래 ③이 이긴다고 봤다. 실제로 열어 보니 **Iowa 한 곳에 넷이 다 있었다.** 그 순간 갈래 ③의 유일한 장점이 사라졌다.

> 교훈: 「출처를 섞어야 할 것」이라는 **추정** 위에 계획이 서 있었다. 목록을 여는 데 10분이 걸렸고 계획 한 단계가 통째로 없어졌다.

## ③ 크기 정렬 기준: 「앞 300ms」 → 「가장 큰 300ms」

앞선 설계는 *"앞 300ms RMS 정렬"* 이었다. 합성음에서는 맞았지만 **실물 녹음에서 무너졌다** — 바이올린은 활이라 어택이 느려 소리가 뒤에서 커진다. 앞을 기준 삼으니 몸통 RMS가 **0.30**까지 올라가 다른 악기(0.10)의 세 배가 됐다.

크기가 단서가 되면 음색 과제가 아니게 되므로 기준을 **파일 안에서 가장 큰 300ms 구간**으로 바꿨다. 16개 전부 0.100으로 맞았고, 포락선 성격(피아노·기타 감쇠 / 바이올린·플루트 지속)은 그대로 남았다.

`가정`: 지속음이 감쇠음보다 여전히 조금 크게 들릴 수 있다. 그건 악기의 성질이라 지우면 음색이 상한다. 더 정밀하게는 EBU R128(LUFS)이 맞지만 1.6초짜리에는 게이팅이 불안정하다.

## ④ 「문항당 한 번만」 → 다시 듣기 허용

실기기 청취에서 사용자가 **정상 청력으로 몇 번 들어야** 구별했다고 보고했다. 헷갈린 쌍은 예상대로 **같은 포락선끼리**였다:

```
감쇠음  피아노 · 기타      ← 이 안에서 헷갈림
지속음  바이올린 · 플루트   ← 이 안에서 헷갈림
```

두 무리 **사이**는 쉽고 무리 **안**은 어렵다. 무리 안을 가르려면 배음 구조 — 즉 진짜 음색을 들어야 하는데, **그게 이 과제가 훈련하려는 바로 그것**이다. 과제가 고장 난 게 아니라 어려운 절반이 어려운 것이다.

문제는 화면이 한 번만 들려준다는 점이었다. 못 들으면 찍는 수밖에 없고 그건 훈련이 아니다. 그래서 **「고르기」 단계에 다시 듣기를 열었다**(횟수 제한 없음).

**링 6에는 넣지 않는다.** 이유가 분명하다 — `ling6Store`는 **날짜 키로 하루 한 줄**을 쌓고 전날과 비교한다. 즉 링 6는 **측정**이다. 반복 청취를 허용하면 그 측정이 무의미해진다. 반면 `instStore`는 세션마다 시각으로 쌓는 **훈련 기록**이라 반복이 오히려 맞다.

`주의`: 그 대신 악기탭 정답률은 이제 **「몇 번이든 들은 뒤의 정답률」**이다. 한 번에 맞혔는지를 재려면 다시 들은 횟수를 따로 세야 하는데 지금은 안 센다.

---

# 4. 세부 결정과 이유

| 결정 | 값 | 이유 |
|---|---|---|
| 기음 넷 | **G4 · A4 · C5 · E5** | 한 옥타브 내리면 **플루트 최저음**이라 바람 소리가 섞여 「쉬익거리면 플루트」라는 음색 아닌 단서가 생긴다. 한 옥타브 올리면 **기타가 높은 프렛**이라 얇고 빨리 죽는다. 넷 다 편한 가운데 자리 |
| 악기마다 음을 다르게? | **안 한다** | 한 악기만 낮추면 「낮은 소리 = 그 악기」가 되어 높이로 고를 수 있다 |
| 세기 | **넷 다 `mf`** | 크기는 후처리로 맞추지만 **연주 세기가 다르면 음색 자체가 달라진다** |
| 플루트 비브라토 | **있는 쪽(vib)** | 바이올린(활)에 비브라토가 있는데 플루트만 없으면 「떨리면 바이올린」이 되어 음색이 아니라 떨림으로 고를 수 있다 |
| 기타 줄 | **1번 줄(높은 E)** | G4·A4가 3·5프렛, C5·E5가 8·12프렛. 낮은 포지션이라 소리가 얇아지지 않는다 |
| 바이올린 줄 | **D선 · A선** | 이웃한 두 줄이라 음색이 튀지 않는다 |
| 길이 | **1.6초 고정** | 길이가 다르면 음색을 안 듣고 길이로 고를 수 있다(링 6의 「여섯 파일 길이를 맞춘다」와 같은 규칙) |
| 꼬리 | **맞추지 않는다** | 피아노·기타는 1.6초 안에서 여운이 남고 바이올린·플루트는 끝까지 이어진다. **그 차이가 이 과제가 구별하려는 것** |
| 분절 방식 | **자기상관으로 기음을 재서 찾는다** | 아래 참조 |

## 분절을 「순서로 세지 않는」 이유

피아노만 음별 파일이고 나머지 셋은 **한 파일에 반음계 여러 음**이 들어 있다. 무음으로 토막 내어 순서대로 세는 방법이 자연스러워 보이지만, **감쇠가 무음 문턱을 스치면 한 음이 둘로 쪼개져 순서가 통째로 밀린다.** 실제로 8음짜리 기타 파일이 13토막으로 잡혔다.

그래서 토막마다 **자기상관으로 기음을 재고 목표 주파수에 가장 가까운 것을 고른다**(허용 오차 45센트 — 반음이 100센트라 이웃 음과 헷갈리지 않는다). 못 찾으면 **에러로 멈춘다.** 틀린 음이 조용히 `assets/`에 들어가는 게 제일 나쁜 결과다.

---

# 5. 틀렸던 것

> 이 절을 남기는 이유: 같은 실수를 다시 하지 않기 위해서고, 이 문서의 다른 주장도 그만큼 의심하며 읽으라는 뜻이다.

| 무엇 | 어떻게 드러났나 |
|---|---|
| **경로 A/B 라벨을 뒤집어 적었다** | 원본 문서의 표(A=합성음 유지, B=사운드폰트 임시본)와 그 문서 끝의 미결 표가 **서로 순서가 반대**였고, 뒤엣것을 그대로 받았다. 사용자가 "B안은 내가 녹음을 제공해야 하나"라고 물어서 드러났다 — 하마터면 **반대 답을 할 뻔했다.** 이후 A/B 글자를 버리고 「합성음 유지」·「사운드폰트 임시본」으로 부르기로 했다 |
| **라이선스를 CC-BY 3.0으로 적은 외부 글을 검증 없이 넘길 뻔했다** | 데비안 copyright을 직접 열어 **MIT**(Frank Wen)임을 확인. 그 글의 크레딧 문안을 그대로 썼으면 **없는 라이선스를 표기**할 뻔했다 |
| **CC0 경로를 낮게 평가했다** | 사용자가 되물어서 다시 봤고, 확인해 보니 더 나은 선택이었다. 「출처를 섞어야 할 것」이라는 확인 안 한 추정이 원인 |
| **자기상관 옥타브 방지 로직 버그** | 「최댓값의 90%를 처음 넘는 지연」을 쓰면 상관 곡선이 완만해서 **최소 지연(1200Hz 언저리)에 붙는다.** 16개 중 대부분이 1208Hz로 잡혀 드러났다. **봉우리(국소 최댓값) 중에서** 골라야 맞다 |
| **크기 기준을 앞 300ms로 잡았다** | 계획 단계에서 정한 값이 실물 녹음에서 무너졌다(3-③) |

---

# 6. 라이선스

**출처**: University of Iowa Electronic Music Studios — Musical Instrument Samples
https://theremin.music.uiowa.edu/MIS.html (확인 2026-08-29)

**이용 조건 원문**:

```
these recordings have been freely available on this website and may be
downloaded and used for any projects, without restrictions.
```

- 정식 CC0 문서가 아니라 **배포 페이지에 적힌 이용 허락 문장**이다. 그래서 문장을 그대로 `docs/licenses/iowa-mis.txt`에 옮겨 뒀다. 페이지가 바뀌면 그 파일이 근거다.
- **저작자 표시 의무가 명시돼 있지 않다** → 크레딧 화면을 만들지 않아도 된다.
- 원본 10개는 `.inst-src/`에 받고 **커밋하지 않는다**(83MB). `node scripts/fetch-inst-src.mjs`로 다시 받는다.

---

# 7. 검증

| | |
|---|---|
| 타입 | `npx tsc --noEmit` 통과 |
| 테스트 | `npx jest src/training/inst` — 2 suites / 21 tests 통과 |
| 전체 | `npx jest` 28/29. 실패한 `WrsSessionScreen.test.tsx`는 **이 작업과 무관한 기존 실패**(`react-native-audio-api` 네이티브 모듈 없음). HEAD로 되돌려 동일 실패를 확인 |
| 린트 | `eslint` 깨끗 |
| 음원 | 16개 전부 1.6초 · 최대 300ms RMS **0.100** · 피크 0.21~0.59(클리핑 없음) · 기음 오차 ±35센트 이내 |
| 실기기 | 소리 정상 재생 확인. 넷 구별 가능(반복 청취 시) — 3-④ |

---

# 8. 남은 것

| | |
|---|---|
| **다시 듣기 횟수 기록** | 지금은 안 센다. 「한 번에 맞혔는가」를 보고 싶어지면 그때 붙인다 |
| **배포 전 오디오 출처 정리** | `assets/ling6/`·`assets/9_sent/`는 여전히 *"웹에서 받은 개인 확인용 임시본"*(`ling6Play.ts` 주석)이다. **악기 음원은 끝났다** |
| **더 나은 종착지** | 직접 녹음(갈래 ④). 지금 음원이 충분하면 굳이 갈 이유는 없다 |
| **문서 통합** | `wav음원생성.md` · `wav음원생성1.md` · 이 문서를 `answer_1.md`에 합칠지 |
