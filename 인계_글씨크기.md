# 인계문 — `color_ui` 브랜치 (글씨 크기 작업 시점)

> **읽는 사람**: 이 대화를 처음 보는 세션.
> **한 줄**: 컬러 작업은 끝났다. 지금 갈래는 **시스템 글씨 크기(font_scale)에서 레이아웃이 깨지는 문제**다.
> 세 화면은 고쳤고 **한 화면이 남았는데, 그게 사용자를 실제로 막는다.**
> **먼저 할 일**: 이 문서를 끝까지 읽고 `ui_tn수정.md` **맨 위 블록**을 읽을 것.
> **가장 중요한 것**: 6절 「다시 열지 말 것」. 이 갈래는 닫은 판단을 다시 여는 낭비를 반복했다.

---

## 0. 프로젝트가 뭔지

- Expo/React Native 앱. 난청 관련 **듣기 연습**(청력 검사가 아니다) — 링 6 · 소리 높낮이 ·
  단어 듣기 · 떨림 4탭
- **대상 사용자가 고령·난청자다.** 이게 판단의 기준선이다. 「세련됨」보다 「보이는가·누를 수 있는가」가 이긴다
- **안드로이드 전용.** `ios/` 없음. RN 0.86 · newArch · 라이트 전용(다크 모드는 2026-08-11 안 하기로 결정)
- 팔레트 이름은 「Clean Clinical」

## 1. **실기기가 붙어 있다. 이게 이 갈래의 전제다**

```
adb 경로     /d/Android/Sdk/platform-tools/adb      ← PATH에 없다. 전체 경로로 부를 것
기기         RF8MC0XZYLK  SC-01M (갤럭시 노트10+)
규격         w411dp · 420dpi · 1080x2280
앱           com.rnhear.app (개발 빌드, 설치돼 있음)
Metro        8081에서 돌고 있었다 — 화면에 보이는 게 워킹 트리의 코드다
```

**앱을 새로 띄울 필요가 없다.** 이미 떠 있다. 확인은 `adb shell dumpsys activity activities | grep mResumedActivity`.

### 이 갈래의 핵심 도구

```bash
ADB=/d/Android/Sdk/platform-tools/adb
$ADB shell settings put system font_scale 1.3    # 파손 재현
$ADB shell settings put system font_scale 1.0    # 반드시 되돌릴 것
$ADB exec-out screencap -p > shot.png
$ADB shell input tap <x> <y>      # 하단 탭 y=2018 · x = 134/403/672/943
```

**끝나면 `font_scale`을 1.0으로 되돌려라.** 사용자의 실제 폰이다.

## 2. 저장소 상태

- 브랜치 **`color_ui`** · 워킹 트리 clean
- **`origin/color_ui`보다 로컬이 2 커밋 앞선다. 아직 푸시 안 했다**
- `main`에 안 들어갔고 PR도 없다

```
f453f28  작업 로그 — 실기기 확인 · 글씨 크기 파손 · 인계문 ③ 오진 정정   ← 미푸시
321c8c5  글씨 크기 — idle 히어로를 스크롤에 넣는다 (3개 화면)          ← 미푸시
cc11bbe  인계문 — color_ui 브랜치 (UI·컬러 작업 종료 시점)
5771b3e  깃 학습 질문지
9d731e9  작업 로그 — 푸시 · 팔레트 이력
f03d3bc  ② 카드 경계 — 현행 유지로 닫는다
...
a1a5bd7  가독성 — 색 4개 + 글자 42곳     ← **되돌리지 말 것.** 이유는 4절
```

### 문서

| 파일 | 무엇 |
|---|---|
| `ui_tn수정.md` | **작업 로그. 누적 문서** — 새 기록은 **맨 위**에. 기존 블록은 **고치지 않는다.** 판단이 뒤집히면 새 블록에서 닫는다. CRLF다 |
| `인계_color_ui.md` | 직전 인계문. **①③은 이 문서에서 닫혔고, ③은 오진이었다.** 그 문서의 미해결 목록을 그대로 믿지 말 것 |
| `인계_컬러.md` | 더 앞 인계문. 역사용 |
| `깃_학습_질문지.md` | 사용자용 학습 자료. 코드와 무관 |

## 3. **지금 문제 — 하나가 사용자를 막고 있다**

### ① `ListeningCheckScreen`(듣기 준비) — **최우선. 미해결**

`src/training/ListeningCheckScreen.tsx`

**`font_scale` 1.2부터 「연습 시작」 버튼이 화면 밖으로 밀려 눌리지 않는다.**
`ScrollView`가 이 파일에 **0개**라 내려볼 방법도 없다. 「소리 높낮이」와 「떨림」이
이 화면을 거치므로 **그 두 경로로는 연습을 시작할 수 없다.**

재현: 소리 높낮이 탭 → 「높낮이 비교」 → 듣기 준비. `font_scale 1.3`.

**두 번 시도했고 두 번 다 기본 배율 1.0에서 회귀가 났다. 되돌렸다.**

| 시도 | 무엇을 했나 | 왜 실패했나 |
|---|---|---|
| 1 | 안내만 `ScrollView`, 버튼은 밖에 고정(세션 화면과 같은 처방) | 1.0에서 버튼이 250px 위로 딸려 오고 토글이 잘렸다. `actions`가 `marginTop: "auto"`로 바닥에 붙는 구조인데, 스크롤이 `flex: 1`로 남은 자리를 다 먹으면 그 `auto`가 죽는다 |
| 2 | 버튼까지 통째로 감싸기(`flexGrow: 1` + `auto` 유지) | 1.0에서 면책 문구와 버튼이 **아예 사라졌다.** `auto` 마진이 뷰포트보다 큰 컨텐트 높이를 기준으로 계산되는 것으로 보인다 |

**다음에 고칠 때는 `marginTop: "auto"`를 먼저 걷어내고** 명시적인 바닥 배치로 바꿀 것.
`auto`를 남긴 채 `ScrollView`를 씌우는 조합은 두 번 다 졌다.

**반드시 1.0과 1.3을 둘 다 찍어서 대조할 것.** 1.3만 보고 고치면 1.0이 깨진다 —
이번에 두 번 겪었다.

### ② 큰 글씨에서 **안 본 화면들**

1.3으로 확인한 것: 4개 탭 목록 · 통계 · 떨림 idle · 다른 음 찾기 진행 중. 전부 정상
(고친 뒤 기준).

**안 봤다**:
- 단어 듣기 3종 — 한 글자 · 두 글자 · **단어 빙고**(`WrsBingoScreen`, 격자라 특히 의심)
- 링 6 진행 화면
- **각 세션의 요약(summary) 화면** — 여긴 이미 `ScrollView`가 있어 확률은 낮다
- 설정 화면

**확인한 4개 중 4개에서 파손이 나왔다.** 안 본 곳에도 있다고 가정하는 편이 맞다.

### ③ `chartMuted` 2.21:1이 충분한가 — **여전히 못 본다**

링 6 아쉬움 막대 색. **기기의 링 6 기록이 2회뿐이다**(8/20, 8/22).
서로 다른 **7일치**가 있어야 패널이 렌더된다(`ling6Session.ts:206`). 그전에는 화면에
**아예 없다.** 「안 보인다」와 「없다」를 헷갈리지 말 것. 기록을 쌓거나 시드를 넣기 전엔 불가.

부족하면 `#8A9EB4`(2.47)까지가 `highlight`(트랙 대비 2.70)를 안 넘는 한계다.

### ④ `allowFontScaling` 상한 — **결정한 적 없다**

```
TEXT_SCALE = 1.2                  앱 내부 배수 (4개 화면)
× 시스템 font_scale               상한 없음. 안드로이드 최대 2.0
= 실효 최대 2.4배
```

**`allowFontScaling` · `maxFontSizeMultiplier`가 저장소 어디에도 없다**(전수 확인함).
상한을 두면 넘침은 줄지만 **고령 사용자가 키운 글씨를 앱이 거부하는 것**이라 접근성과
맞바꾸는 거래다. ②를 훑은 결과를 보고 정할 것 — 넘치는 곳이 두어 곳이면 개별 수정,
열 곳 넘으면 상한이 답이다.

## 4. 이번에 고친 것 — 왜 그렇게 고쳤나

`321c8c5`. `AmSessionScreen` · `FreqSessionScreen` · `PitchCompareScreen`의
**idle 히어로를 `ScrollView`로 감쌌다.**

```
<ScrollView style={styles.heroScroll} contentContainerStyle={styles.hero}>
heroScroll: { flex: 1, alignSelf: "stretch", marginBottom: -(Spacing.two + 2) }
hero:       { flexGrow: 1, ... }        ← flex: 1 에서 바뀜
```

- 버튼은 스크롤 **밖**에 남아 항상 보인다. 자리가 남으면 예전처럼 가운데, 모자랄 때만 스크롤
- 같은 파일의 **요약 단계는 이미 같은 처방이 들어가 있었다.** 그 주석이 이 문제를 정확히
  예견했고, **idle에만 안 넣은 것**이다
- **음수 마진**은 `safeArea`의 `gap`을 이 자식에서만 상쇄한다. 히어로 내용은 **1.0에서도
  이미 상자보다 ~13dp 컸고** 그 넘침이 `gap`으로 흘러들어 안 보였을 뿐이다.
  `ScrollView`가 자르기 시작하자 드러났다. 빌려 쓰던 높이를 돌려주는 값이다

**`a1a5bd7`(글자 42곳 14px)을 되돌리지 마라.** 직전 인계문이 「넘치면 이것만 되돌린다」고
적었지만 **되돌려도 안 고쳐진다.** 파손은 14px이 아니라 시스템 배율 × 무스크롤 때문이고,
글자를 줄이면 1.3에서 여전히 깨지면서 가독성만 잃는다.

## 5. 검증 — 기준선

```
npx tsc --noEmit -p tsconfig.json    → exit 0
npx eslint src --ext .ts,.tsx        → 0 errors / 경고 14
npx jest                             → 23 suites · 230 tests 통과
```

경고 14건은 저장소 테스트 4개의 `import/first`이고 **손대지 않기로 한 것**이다.
**`eslint --fix`를 전체에 돌리지 말 것** — 그 파일들의 `jest.mock` 아래 import를 위로
끌어올려 의도를 깬다. 한 번 겪고 되돌렸다.

## 6. **다시 열지 말 것**

| 항목 | 상태 | 다시 열 조건 |
|---|---|---|
| **안드로이드 그림자**(`Shadows.card`) | **닫힘 — 직전 인계문이 오진했다.** `elevation: 2`/`4`가 이미 있다(`theme.ts:132,140`) | 없음 |
| **`SessionModeToggle`·`SessionTrendPanel` 글자 넘침** | **닫힘.** 411dp·1.0·1.3 전부 멀쩡. 직전 인계문의 1순위 의심은 틀렸다 | 없음 |
| **`a1a5bd7` 되돌리기** | **하지 말 것** | 없음. 4절 참고 |
| **카드 경계 대비**(`border` 1.20) | **닫힘 — 현행 유지** | 실사용자가 「카드가 어디서 끝나는지 모르겠다」고 말할 때. 그때도 테두리가 아니라 간격·그림자부터 |
| **배경을 낮춰 카드를 띄우는 안** | **막힘** | 없음. `textMuted`가 AA 아래로 떨어진다 |
| **다크 모드** | **안 하기로 결정**(2026-08-11) | 없음 |
| **글자 크기(px) 재조정** | **끝남** | 없음. 남은 건 px이 아니라 **시스템 배율** 문제다 |

## 7. 이 저장소에서 걸려 넘어지는 것들

1. **`theme.ts`와 `ui_tn수정.md`는 CRLF, `.tsx`는 LF다.** 스크립트로 고칠 때 줄바꿈을
   파일에 맞출 것. 안 맞으면 조용히 실패한다
2. **Bash 히어독이 큰 한글 문서에서 깨진다.** 문서는 **Write 도구**로 만들고,
   CRLF 파일에 붙일 때는 Node로 변환해 넣을 것
3. **Node에서 `/c/Users/...` 경로가 안 먹는다** — `C:/Users/...`로 줄 것
4. **`adb`가 PATH에 없다.** `/d/Android/Sdk/platform-tools/adb`
5. **화면 오른쪽 가장자리의 잘린 흰 카드는 우리 앱이 아니다** — 삼성 엣지 패널 핸들
   (`com.samsung.android.app.cocktailbarservice`). 한 번 오탐했다
6. **비교 기준선을 먼저 말할 것.** 같은 작업이 부모 브랜치 대비로는 「그대로」,
   `d37a654` 대비로는 「전부 바뀜」이 된다
7. **`git log --oneline --branches --not --remotes`** — 푸시 안 된 커밋을 뽑는다

## 8. 사용자에 대해

- **기록에 의지한다.** 「나는 기록한 내용에 의지하는 편」이라고 직접 말했다.
  중요한 판단은 반드시 `ui_tn수정.md`에 남길 것. 대화에만 두면 잃는다
- **시각적 설명을 원한다.** 색·레이아웃 이야기는 터미널 텍스트로 전달되지 않는다.
  **실기기 스크린샷 대조**가 이번에 가장 잘 통했다
- **깃 개념을 배우는 중이다.** 커밋·푸시·브랜치·업스트림을 설명 없이 쓰지 말 것
- 판단을 물으면 **선택지를 나열하지 말고 추천을 줄 것.** 다만 근거는 수치로
- **속도에 민감하다.** 「멀 그렇게 오래동안 수정한 거지?」라고 물은 적 있다.
  오래 걸릴 일이면 **왜 오래 걸리는지 먼저 말하고** 시작할 것

## 9. 지금 시작한다면

1. **`ListeningCheckScreen`을 고친다**(3절 ①). 사용자를 실제로 막는 유일한 항목
2. **안 본 화면들을 1.3으로 훑는다**(3절 ②). 폰이 붙어 있는 지금이 가장 싸다
3. **`allowFontScaling` 상한을 정한다**(3절 ④) — 2번 결과를 보고
4. 그 다음이 **푸시**(로컬이 2 커밋 앞선다)와 `main` 병합.
   PR 링크: `https://github.com/gimsansan/mmn-amp1/pull/new/color_ui`

③(`chartMuted`)은 기록이 쌓이기 전엔 못 한다. 미결로 두는 게 맞다.

## 10. 작업 후

`ui_tn수정.md` **맨 위**에 새 블록으로 기록할 것. 형식은 기존 블록을 따를 것 —
요청 / 판단 / 실제 변경 / **막힌 곳** / 검증 / 미결.
**틀렸던 판단은 숨기지 말고 새 블록에서 철회한다.** 이 문서 계보가 실제로 그렇게
운영돼 왔다(3번 항목 철회 · 「47곳」→「42곳」 정정 · 이번엔 **인계문 ③이 오진이었음을 정정**).
