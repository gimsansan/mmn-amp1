# 인계문 — `color_ui` 브랜치 (반응형 · 작은 화면 파손 시점)

> **읽는 사람**: 이 대화를 처음 보는 세션.
> **한 줄**: 바닥 여백 정리가 끝났고, 그 과정에서 **작은 화면 + 큰 글씨에서 연습 중 화면이
> 깨지는 걸 새로 찾았다.** 남은 일은 그걸 고치는 것 하나다.
> **먼저 할 일**: 이 문서를 끝까지 읽고 `ui_tn수정.md` **맨 위 두 블록**을 읽을 것.
> **가장 중요한 것**: 3절(다음 할 일)과 6절(다시 열지 말 것).
> 이 갈래는 닫은 판단을 다시 여는 낭비를 반복해 왔다. **6절을 다시 열지 마라.**

---

## 0. 프로젝트가 뭔지

- Expo/React Native 앱. 난청 관련 **듣기 연습**(청력 검사가 아니다) — 링 6 · 소리 높낮이 ·
  단어 듣기 · 떨림 4탭
- **대상 사용자가 고령·난청자다.** 이게 판단의 기준선이다. 「세련됨」보다
  「보이는가·누를 수 있는가」가 이긴다
- **안드로이드 전용.** `ios/` 없음. RN 0.86 · newArch · 라이트 전용(다크 모드는 2026-08-11 안 함)
- 팔레트 이름은 「Clean Clinical」

## 1. **실기기가 붙어 있다. 이게 이 갈래의 전제다**

```
adb 경로     /d/Android/Sdk/platform-tools/adb      ← PATH에 없다. 전체 경로로 부를 것
기기         RF8MC0XZYLK  SC-01M (갤럭시 노트10+)
규격         w411dp · 420dpi · 1080x2280
앱           com.rnhear.app (개발 빌드)
Metro        8081에서 돌고 있다 — 화면에 보이는 게 워킹 트리의 코드다
```

### 이 갈래의 핵심 도구

```bash
export MSYS_NO_PATHCONV=1
ADB=/d/Android/Sdk/platform-tools/adb

$ADB shell settings put system font_scale 1.3    # 파손 재현
$ADB shell settings put system font_scale 1.0    # 반드시 되돌릴 것
$ADB exec-out screencap -p > shot.png
$ADB shell input tap <x> <y>      # 하단 탭 y=2018 · x = 134/403/672/943
```

### ★ **작은 화면을 흉내내는 법 (이번에 파손을 찾은 방법)**

```bash
$ADB shell wm size 720x1280       # + density 320  → 360dp x 640dp (보급형 폰)
$ADB shell wm density 320
# ... 확인 ...
$ADB shell wm size 1080x2280      # 되돌리기
$ADB shell wm density 420
```

> **`wm size reset` / `wm density reset`을 쓰지 마라.** 이 기기는 사용자가
> **물리 1440x3040·560dpi를 1080x2280·420dpi로 낮춰 쓰고 있다.** `reset`은 사용자의
> 그 설정까지 지우고 물리값으로 돌아가 버린다. **반드시 명시값으로 되돌릴 것.**
> (이번에 `reset`을 눌렀다가 명시값으로 복구했다.)

**끝나면 `font_scale`·`wm size`·`wm density`를 전부 되돌려라.** 사용자의 실제 폰이다.
(지금은 `1.0` · `1080x2280` · `420`으로 되돌려 놓은 상태다.)

## 2. 저장소 상태

- 브랜치 **`color_ui`** · 워킹 트리 clean
- **`main`에는 안 들어갔다.** `main` 대비 **72 커밋** 앞선다. PR 없음.
  **사용자가 「병합은 아직 안 해도 된다」고 했다(2026-08-22).** 먼저 병합하지 마라

```
6ca20d3  단어 듣기 — 첫 단어에 0.7초 뜸을 들인다        ← 이번 작업
37e7d7a  바닥 여백 — 10개 화면에서 BottomTabInset을 걷어낸다
878319e  인계문 — 바닥 여백 정리 범위 확정 반영
```

> **원격 상태를 말하기 전에 반드시 `git fetch origin`을 먼저 돌려라.**
> 안 하면 틀린 숫자를 말하게 된다(이 계보가 실제로 두 번 틀렸다).

### 문서

| 파일 | 무엇 |
|---|---|
| `ui_tn수정.md` | **작업 로그. 누적 문서** — 새 기록은 **맨 위**에. 기존 블록은 고치지 않는다. 판단이 뒤집히면 새 블록에서 닫는다. CRLF다 |
| `인계_바닥여백.md` | 직전 인계문. **3절(바닥 여백)은 이 문서에서 끝났다.** 그 미해결 목록을 그대로 믿지 말 것 |
| `인계_글씨크기.md` · `인계_color_ui.md` · `인계_컬러.md` | 더 앞 인계문. 역사용 |

## 3. **지금 할 일 — 하나뿐이다**

### ★ `running`(연습 중) 화면에 스크롤을 씌운다 — 7개 화면

**증상**(실기기에서 재현 확인):
`360dp x 640dp` + `font_scale 2.0`에서 `WrsSessionScreen` 연습 중 화면이
**보기 4개 중 2개만 보이고 「중지」가 화면 밖으로 나간다. 스와이프해도 안 움직인다.**
하드웨어 뒤로가기는 살아 있어 갇히지는 않지만 **연습을 할 수 없다.**

**원인**: `idle`과 `summary`는 `ScrollView` 안인데 **`running` 블록만 맨 `View`다.**
넘치면 잘릴 뿐 스크롤할 방법이 없다.

**처방은 이미 검증돼 있다** — `ListeningCheckScreen`이 `fd23835`에서 쓴 그 방법.

```
<SafeAreaView edges={["top","left","right"]}>
  <ScrollView style={styles.scroll} contentContainerStyle={styles.scrollContent}>
    진행막대 · promptArea · 보기 · (hud 등)
  </ScrollView>
  <View style={styles.actions}>   ← 「중지」는 스크롤 밖 고정. `marginTop:"auto"` 없음
scroll:        { flex: 1 }
scrollContent: { flexGrow: 1, gap: ... }
```

**대상 7개 화면** — 전부 `running` 블록이 `ScrollView` 밖의 맨 `View`인 걸 확인했다.

| 화면 | running 블록 | 비고 |
|---|---|---|
| `WrsSessionScreen` | promptArea · choiceGrid | **파손 직접 확인** |
| `WrsTwoCharScreen` | promptArea · choiceGrid | 구조 동일 |
| `WrsBingoScreen` | promptArea · hud · 3×3 판 | idle 버튼 3개도 2.0에서 한 글자씩 쪼개짐 |
| `AmSessionScreen` | promptArea · choices | |
| `FreqSessionScreen` | promptArea · choices | |
| `PitchCompareScreen` | promptArea · choices | `minHeight: 132`라 **제일 키가 크다. 여기가 가장 위험** |
| `Ling6SessionScreen` | promptArea · choiceGrid · (하나 더) | running 블록이 3개다 |

> **정직하게 적는다**: 실기기에서 **직접 깨지는 걸 본 것은 `WrsSessionScreen` 하나다.**
> 나머지 6개는 **같은 구조라서 같은 처방이 필요하다고 판단한 것**이지 파손을 눈으로
> 본 게 아니다. **화면마다 재현부터 해 보고 고칠 것.** 안 깨지는 화면이 있으면
> 그것도 기록에 남겨라.

### 검증은 네 조합을 다 볼 것

```
411dp(사용자 기기) · 1.0    ← 회귀가 나는 곳. 제일 중요하다
411dp             · 1.3
360dp x 640dp     · 1.3
360dp x 640dp     · 2.0    ← 파손이 나는 곳
```

**1.0을 건너뛰지 마라.** 이 갈래는 「큰 글씨만 보고 고쳤다가 1.0을 깨뜨린」 적이
**두 번** 있다. 스크롤을 씌우면 헛도는 여백이 드러나 버튼이 위로 튀는 게 전형적인 증상이다.
바닥 여백은 `37e7d7a`에서 이미 정리했으므로 이번엔 덜하겠지만, 확인은 해야 한다.

## 4. 이번에 한 것

### `37e7d7a` — 바닥 여백 정리 (10개 화면)

`BottomTabInset`(안드로이드 80dp)은 이 앱에서 죽은 값이었다. 하단 탭이
`NativeTabs`(네이티브)라 **화면 영역이 이미 탭바 위에서 끝난다.**
`SafeAreaView` 하단 inset(≈48dp)도 같은 이유로 헛돌았다.

- 10개 화면에서 `paddingBottom`의 `BottomTabInset` 제거 · 5개 화면에 `edges` 추가
- **`BottomTabInset`을 쓰는 화면은 이제 0곳이다**(`theme.ts:144`의 정의만 남음)
- `WrsBingoScreen`·`WrsVoiceGuideScreen`은 `BottomTabInset` 단독이라 그냥 빼면 **0**이
  되어 버튼이 탭바에 붙는다 → `Spacing.three`(16dp)로 바꿨다
- **직전 인계문의 「남은 9곳」은 셈이 어긋났다. 실제로는 10곳이었다.**
  그리고 인계문이 대상으로 적은 `StatsScreen`은 손댈 게 없었다
- 곁다리였던 **WRS 요약 pill 가림이 저절로 풀렸다**(1.3 확인). 닫혔다

### `6ca20d3` — 단어 듣기 첫 음에 뜸

카드를 누르자마자 소리가 터져 들을 준비를 할 새가 없다는 지적.
**첫 단어 앞에만 0.7초**를 넣었다(`wrsTts.ts`의 `WRS_FIRST_WORD_LEAD_MS`).
두 번째부터는 「고르기 → 다음」 사이가 이미 있어 두지 않는다.

**뜸 동안 「듣는 중」이라고 하지 않는다** — 난청 사용자가 못 들은 줄 안다.
`leadIn` 상태를 두고 **「잠시 뒤 들려 드려요」**로 바꾸고 이퀄라이저를 멈춘다.
`waitFirstWordLeadIn()`을 함수로 뺀 이유는 화면 테스트가 `wrsTts`를 통째로 대신할 때
뜸도 같이 사라지게 하려는 것이다.

**사용자가 0.7초를 「적당하다」고 확정했다(2026-08-22). 길이를 다시 묻지 마라.**

## 5. 검증 — 기준선

```
npx tsc --noEmit -p tsconfig.json    → exit 0
npx eslint src --ext .ts,.tsx        → 0 errors / 경고 14
npx jest                             → 23 suites · 230 tests 통과
```

경고 14건은 저장소 테스트 4개의 `import/first`이고 **손대지 않기로 한 것**이다.
**`eslint --fix`를 전체에 돌리지 말 것** — 그 파일들의 `jest.mock` 아래 import를 위로
끌어올려 의도를 깬다. 한 번 겪고 되돌렸다.

## 6. **다시 열지 말 것**

| 항목 | 상태 | 다시 열 조건 |
|---|---|---|
| **바닥 여백 / `BottomTabInset`** | **닫힘.** 10개 화면 처리 · 1.0/1.3 실기기 확인 | 없음 |
| **WRS 요약 pill 가림** | **닫힘.** 여백 회수로 저절로 풀렸다 | 없음 |
| **첫 음 뜸 0.7초** | **닫힘.** 사용자가 「적당하다」고 확정 | 사용자가 다시 말할 때 |
| **뜸 동안의 문구** | **「잠시 뒤 들려 드려요」로 닫음.** 「듣는 중」은 난청 사용자에게 거짓말이 된다 | 없음 |
| **두 번째 단어부터의 뜸** | **두지 않기로 닫음.** 25번이 늘어진다 | 없음 |
| **세로 고정 · dp 치수 · `MaxContentWidth`** | **잘 돼 있다. 손대지 마라.** 7절 참고 | 없음 |
| **`ListeningCheckScreen` 파손** | **닫힘.** 1.0/1.3/2.0 확인 완료 | 없음 |
| **`allowFontScaling` 상한** | **두지 않기로 닫음.** 고령 사용자가 키운 글씨를 앱이 거부할 근거가 없다 | 3절을 고쳐도 **막는** 파손이 남을 때 |
| **`chartMuted` 2.21:1** | **현행 유지로 닫음.** 진하게 하면 `highlight`와 되레 가까워진다 | 없음 |
| **안드로이드 그림자**(`Shadows.card`) | **닫힘.** `elevation: 2`/`4`가 이미 있다 | 없음 |
| **`a1a5bd7` 되돌리기** | **하지 말 것** | 없음 |
| **카드 경계 대비**(`border` 1.20) | **닫힘 — 현행 유지** | 실사용자가 「카드가 어디서 끝나는지 모르겠다」고 할 때 |
| **배경을 낮춰 카드를 띄우는 안** | **막힘.** `textMuted`가 AA 아래로 떨어진다 | 없음 |
| **다크 모드** | **안 하기로 결정**(2026-08-11) | 없음 |
| **글자 크기(px) 재조정** | **끝남** | 없음 |
| **「설정 화면」** | **그런 화면은 없다.** 우하단 톱니는 **Expo dev-menu**다 | 없음 |
| **`main` 병합** | **사용자가 미루라고 했다**(2026-08-22) | 사용자가 하자고 할 때 |

## 7. 반응형 — **이미 잘 돼 있는 것**(손대지 마라)

3절만 구멍이고 나머지는 튼튼하다. 실기기로 재서 확인한 결과다.

| 조건 | 결과 |
|---|---|
| 411dp x 870dp(사용자 기기) · 1.0 / 1.3 | 전부 온전 |
| 411dp x 870dp · **2.0**(안드로이드 최대) | **연습 중 화면 온전** |
| 360dp x 640dp(보급형) · 1.0 / 1.3 | **전부 온전.** 여유까지 있다 |
| 360dp x 640dp · **2.0** | **파손** ← 3절 |

**튼튼한 이유**:

- **세로 고정**(`app.json` `orientation: "portrait"`) — 가로 조합을 아예 없앴다
- 모든 치수가 **dp**라 화면 밀도를 따라간다. 절대 px이 박힌 곳 없음
- `MaxContentWidth: 800` + `alignSelf: "center"` — 태블릿에서 줄이 길어지지 않는다
- 보기 칸이 `width: "48%"`라 폭을 따라간다
- `SafeAreaView edges={["top","left","right"]}` — 노치·홈바를 피한다
- **화면 크기를 재서 분기하는 코드가 사실상 없다**(`Dimensions.get`은 `animated-icon`
  한 곳뿐). 분기 없이 흐르는 레이아웃이라 조합이 폭발하지 않는다.
  **이게 이 앱이 튼튼한 진짜 이유다. 반응형을 「개선」한다며 분기를 넣지 마라**

## 8. 이 저장소에서 걸려 넘어지는 것들

1. **★ `sed -i`와 `cat >>`가 줄바꿈을 갈아엎는다.** 이 저장소는 파일마다 줄바꿈이
   다르고(`.tsx`도 CRLF인 게 섞여 있다) `core.autocrlf=true`다.
   **`git diff`는 멀쩡해 보이는데 워킹 트리가 바뀐다.**
   **이 함정을 이 계보가 두 번 밟았다(2026-08-22 두 번 다).**
   → **줄바꿈을 보존하는 Node 스크립트로 고칠 것.** 치환 건수를 세서 1건이 아니면
   멈추는 형태가 좋다(`ui_tn수정.md` 맨 위 블록의 `replaceOnce`). 끝나면 `file -b`로 확인
2. **큰 한글 문서를 Bash 히어독이나 `node -e`에 인라인으로 넣지 말 것.** 이스케이프가
   깨진다. **스크립트를 Write 도구로 파일에 쓰고 `node <파일>`로 실행할 것**
3. **커밋 메시지에 PowerShell 히어스트링(`@'...'@`)을 Bash에서 쓰지 말 것.**
   `@`가 그대로 메시지에 박힌다. Bash에서는 `git commit -F - <<'EOF'`
4. **Node에서 `/c/Users/...` 경로가 안 먹는다** — `C:/Users/...`로 줄 것
5. **`adb`가 PATH에 없다.** `/d/Android/Sdk/platform-tools/adb`
6. **Git Bash가 `adb` 인자의 `/sdcard/...`·`/data/...`를 윈도우 경로로 바꾼다.**
   `export MSYS_NO_PATHCONV=1`을 먼저 줄 것
7. **화면 오른쪽 가장자리의 잘린 흰 카드는 우리 앱이 아니다** — 삼성 엣지 패널 핸들.
   우하단 톱니(`Tools`)도 우리 게 아니다 — **Expo dev-menu**. 프로덕션엔 없다
8. **비교 기준선을 먼저 말할 것.** 원격 이야기는 **`git fetch` 먼저**
9. **`git log --oneline --branches --not --remotes`** — 푸시 안 된 커밋을 뽑는다
10. **`font_scale`을 바꾸면 액티비티가 재시작되어 기본 탭(떨림)으로 돌아간다.**
    배율 바꾼 뒤에는 탭 위치를 다시 잡을 것
11. **`adb shell input tap`이 가끔 씹힌다.** 화면이 안 바뀌었으면 좌표를 의심하기 전에
    한 번 더 눌러 보고, 스크린샷으로 현재 위치부터 다시 확인할 것

### 기기 데이터를 심고 보는 방법 (필요해지면)

개발 빌드라 `run-as`가 된다. 저장소는 AsyncStorage = SQLite `RKStorage`.
기기에 `sqlite3`는 없지만 **Node 24의 내장 `node:sqlite`**로 처리하면 된다.

```bash
export MSYS_NO_PATHCONV=1
$ADB shell am force-stop com.rnhear.app
$ADB exec-out run-as com.rnhear.app cat databases/RKStorage > RKStorage.orig
# node:sqlite로 catalystLocalStorage(key,value) 수정 ← 값은 JSON 문자열
$ADB push RKStorage.new /data/local/tmp/RKStorage.new
$ADB shell "run-as com.rnhear.app cp /data/local/tmp/RKStorage.new databases/RKStorage"
$ADB shell "run-as com.rnhear.app sh -c 'rm -f databases/RKStorage-journal'"
$ADB shell monkey -p com.rnhear.app -c android.intent.category.LAUNCHER 1
```

**키**: `training.ling6Daily.v1` · `training.ling6History.v1` · `training.sessionHistory.v1` ·
`training.wrsSessions.v1` · `training.twoCharSessions.v1`

> **사용자의 실제 기록이다. 손대기 전에 허락을 받고, 백업하고, 끝나면 되돌려
> 기기에서 다시 읽어 확인할 것.**

### 화면 이동 지도

```
탭(y=2018)  링6 x=134 · 소리높낮이 x=403 · 단어듣기 x=672 · 떨림 x=943

링6      Ling6SessionScreen
소리높낮이 PtaSessionScreen(고르기) → ListeningCheckScreen → PitchCompareScreen / FreqSessionScreen
단어듣기  WrsTabScreen(고르기) → WrsSessionScreen / WrsTwoCharScreen / WrsBingoScreen
떨림     AmTabScreen → ListeningCheckScreen → AmSessionScreen
통계     각 화면 우상단 막대그래프 버튼 → StatsScreen
```

**`WrsVoiceGuideScreen`은 한국어 TTS가 없는 기기에서만 뜬다**(`WrsTabScreen:81`
`hasKoreanVoice()`). 시험기에는 있어서 **실기기로 진입할 방법이 없다.**
바닥 여백 정리 때도 이 화면만 미검증으로 남았다. jest 테스트 4건은 통과한다.

## 9. 사용자에 대해

- **기록에 의지한다.** 「나는 기록한 내용에 의지하는 편」이라고 직접 말했다.
  중요한 판단은 반드시 `ui_tn수정.md`에 남길 것. 대화에만 두면 잃는다
- **시각적 설명을 원한다.** 색·레이아웃 이야기는 터미널 텍스트로 전달되지 않는다.
  **실기기 스크린샷 수정 전/후를 나란히** 보낼 것
- **깃 개념을 배우는 중이다.** 커밋·푸시·브랜치·업스트림을 설명 없이 쓰지 말 것
- 판단을 물으면 **선택지를 나열하지 말고 추천을 줄 것.** 다만 근거는 수치로
- **속도에 민감하다.** 오래 걸릴 일이면 **왜 오래 걸리는지 먼저 말하고** 시작할 것
- **세션을 자주 새로 연다.** 그래서 인계문이 이 프로젝트의 생명줄이다.
  이번에도 「새 창에서 인계문 이어서 하겠다」며 이 문서를 만들게 했다
- **범위가 정해진 일은 다시 묻지 않기를 바란다.** 직전 갈래에서 「범위는 확정됐으니
  묻지 말고 시작하라」고 인계문에 못 박아 뒀고 그대로 진행해서 잘 됐다

## 10. 지금 시작한다면

1. **3절 — `running` 화면 7개에 스크롤.** 유일하게 열려 있는 작업이다.
   **화면마다 파손을 먼저 재현하고**(360dp x 640dp · 2.0) 고친 뒤
   **네 조합(411/360 × 1.0/1.3~2.0)을 대조**할 것. 대략 40~60분짜리 일이다.
   오래 걸리는 이유를 먼저 말하고 들어갈 것
2. 끝나면 **기기 설정을 명시값으로 되돌릴 것**(1절 경고)
3. `main` 병합은 **사용자가 하자고 할 때까지 하지 마라**

**3절 말고는 열려 있는 항목이 없다.** 6절 표가 나머지를 전부 닫았다.

## 11. 작업 후

`ui_tn수정.md` **맨 위**에 새 블록으로 기록할 것. 형식은 기존 블록을 따를 것 —
요청 / 판단 / 실제 변경 / **막힌 곳** / 검증 / 미결.
**틀렸던 판단은 숨기지 말고 새 블록에서 철회한다.** 이 문서 계보가 실제로 그렇게
운영돼 왔다(3번 항목 철회 · 「47곳」→「42곳」 정정 · 인계문 ③ 오진 정정 ·
「안 본 화면에도 파손이 있다」는 가정 정정 · **「남은 9곳」→ 실제 10곳 정정**).
