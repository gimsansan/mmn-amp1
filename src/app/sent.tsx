import { SentClosedSessionScreen } from "@/training/sentClosed/SentClosedSessionScreen";

/** 하단 오른쪽 탭 — 문장 듣고 그림 고르기. */
export default function SentRoute() {
  return <SentClosedSessionScreen />;
}
