# 깃 · 깃허브 개념 학습 질문지

> **왜 만들었나**: `d37a654` 이후 작업하면서 커밋 · 푸시 · 브랜치 · 업스트림 · 동기화 ·
> 분기 같은 말이 설명 없이 쏟아졌다. 그 말들을 **순서대로 하나씩** 풀어서 익히려고 만든 질문지다.

## 쓰는 법

1. 아래 프롬프트를 **위에서부터 하나씩** 복사해 다른 대화창에 붙여넣는다
2. 각 프롬프트는 **혼자서도 말이 되게** 썼다 — 새 대화에 붙여도 되고, 한 대화에 이어 붙여도 된다
   (한 대화에 이어 붙이면 앞 설명을 기억하니 조금 더 매끄럽다)
3. **순서를 지킬 것.** 뒤 질문이 앞 개념을 쓴다
4. 각 질문 아래 「자가 점검」이 있다. 그 문장에 스스로 답할 수 있으면 다음으로 간다.
   막히면 같은 대화에서 되물어라 — 「방금 설명 중 ○○가 이해가 안 된다」
5. **모든 프롬프트에 그림 요청이 들어 있다.** 그림이 안 나오거나 부실하면 부록 E를 쓴다.
   깃은 「무엇이 무엇을 가리키는가」가 전부라서, 그림 없이 말로만 들으면 대개 안 남는다

---

## 참고 — 내 저장소의 실제 상황

질문에 나오는 이름들은 전부 진짜다. 필요하면 이 표를 프롬프트에 같이 붙여넣어라.

    저장소:  github.com/gimsansan/mmn-amp1   (Expo/React Native 앱)

    로컬 브랜치            리모트 대비
    ─────────────────────────────────────────
    color_ui               origin/color_ui        동기화됨   ← 지금 작업하던 곳
    ui_chips_suisei        업스트림 없음
    feat_wrs_voice_guide   origin/feat_...        동기화됨   ← color_ui 의 뿌리
    main                   origin/main            동기화됨
    two_feat               origin/two_feat        behind 5

    color_ui 의 커밋 (아래가 오래된 것)
      f03d3bc  ② 카드 경계 — 현행 유지로 닫는다
      57ee21a  인계문
      779e102  작업 로그
      fd2e693  링 6 아쉬움 막대 — 토큰화
      95d50ac  옅은 파란 면 위 글자색 규칙
      a45da1c  동작 — 뒤로가기·터치 타깃
      26fe5df  공용 ScreenHeader
      a1a5bd7  가독성 — 색 4개 + 글자 40곳
      0b2c535  통계 화면 칩 → 탭바
      d37a654  6개doc_failed          ← 여기서부터 궁금해졌다

---

# 1부 · 깃이 뭘 하는 물건인가

## 1. 깃은 왜 있나

```
나는 깃(Git)을 거의 처음 쓰는 사람이다. 명령어를 외우기 전에 "왜 이런 게 필요한가"부터
이해하고 싶다.

질문: 폴더를 통째로 복사해서 "작업본_최종", "작업본_최종2", "작업본_진짜최종"처럼
날짜별로 백업하는 방식과 비교했을 때, 깃은 무엇을 더 해주는가?

다음 형식으로 답해 달라.
1) 복사 백업 방식이 실제로 무너지는 상황을 구체적인 예로 2개
2) 깃이 그 상황을 어떻게 다르게 처리하는지
3) 초보가 흔히 하는 오해 하나

전문 용어는 처음 나올 때 괄호로 뜻을 달아 달라.

그림도 넣어 달라: 「복사 백업 폴더가 늘어나는 모습」과 「깃의 커밋이 이어지는 모습」을
나란히 놓은 텍스트 도식으로.
```

**자가 점검**: 「복사 백업으로는 안 되고 깃으로는 되는 일」을 하나 말할 수 있는가?

---

## 2. 깃과 깃허브는 다른 것인가

```
나는 깃(Git)과 깃허브(GitHub)를 같은 것으로 알고 있었다. 그런데 "로컬"과 "리모트"라는
말이 따로 나오는 걸 보니 다른 것 같다.

질문: 깃과 깃허브는 각각 무엇이고, 어떤 관계인가?

이런 점을 짚어 달라.
1) 인터넷이 끊긴 상태에서 할 수 있는 일과 없는 일
2) 깃허브 없이 깃만 쓸 수 있는가
3) 깃허브 말고 같은 역할을 하는 다른 서비스가 있는가

비유를 하나 들어 달라. 단, 그 비유가 어디서 깨지는지도 같이 말해 달라.

그림도 넣어 달라: 내 컴퓨터 안 / 인터넷 너머 를 가르는 경계선을 그리고,
무엇이 어느 쪽에 있는지 배치한 텍스트 도식으로.
```

**자가 점검**: 「내 컴퓨터 안에만 있는 것」과 「깃허브에 있는 것」을 구분해 말할 수 있는가?

---

# 2부 · 커밋 — 가장 작은 단위

## 3. 커밋이란 무엇인가

```
깃에서 "커밋(commit)"이라는 말이 계속 나온다. 파일을 저장(Ctrl+S)하는 것과 뭐가 다른지
잘 모르겠다.

질문: 커밋이란 정확히 무엇을 하는 동작인가?

이렇게 답해 달라.
1) 저장(Ctrl+S)과 커밋의 차이
2) 커밋 하나에 담기는 것 — 파일 내용 말고 또 무엇이 들어가나
3) "커밋 메시지"를 왜 쓰는가 — 안 쓰면 나중에 무엇이 곤란해지나
4) 커밋을 얼마나 자주 해야 하는가에 대한 실무 기준

그림도 넣어 달라: 파일을 여러 번 저장하다가 커밋을 한 번 하는 과정을
시간 순 타임라인으로. 저장은 어디에 흔적이 남고 커밋은 어디에 남는지 표시해 달라.
```

**자가 점검**: 저장과 커밋의 차이를 한 문장으로 말할 수 있는가?

---

## 4. 워킹 트리 · 스테이징 · 커밋 — 왜 단계가 셋인가

```
깃을 쓰다 보니 "워킹 트리(working tree)가 clean하다", "git add로 스테이징한다" 같은 말이
나온다. 파일을 고치고 커밋하면 끝일 것 같은데 왜 중간 단계가 있는지 모르겠다.

질문: 워킹 트리 / 스테이징 영역(index) / 커밋, 이 세 곳은 각각 무엇이고 왜 나뉘어 있는가?

이렇게 답해 달라.
1) 파일 하나를 고쳐서 커밋할 때까지 세 곳을 어떻게 지나가는지 순서대로
2) 스테이징 단계가 없으면 못 하게 되는 일 — 구체적인 예 하나
3) `git status`가 이 세 곳을 각각 어떻게 보여주는지
4) "워킹 트리가 clean하다"는 정확히 무슨 뜻인가

그림을 꼭 넣어 달라 — 이게 이 질문의 핵심이다.
워킹 트리 / 스테이징 / 저장소 세 칸을 그리고, `git add`와 `git commit`이
파일을 어느 칸에서 어느 칸으로 옮기는지 화살표로 표시해 달라.
`git status`가 각 칸의 파일을 뭐라고 부르는지도 칸 안에 적어 달라.
```

**자가 점검**: 「고쳤지만 아직 커밋 안 된 파일」이 어디에 있는지 말할 수 있는가?

---

## 5. `a1a5bd7` 같은 글자는 뭔가

```
내 저장소의 커밋 목록에 `a1a5bd7`, `fd2e693`, `d37a654` 같은 7글자 조합이 붙어 있다.

질문: 이 글자들은 무엇이고 어떻게 만들어지는가?

이렇게 답해 달라.
1) 이 값의 정체 — 왜 순번 1, 2, 3이 아닌가
2) 왜 보통 7글자만 보여주는가 — 원래는 몇 글자인가
3) 똑같은 파일 내용을 다시 커밋하면 같은 값이 나오는가
4) 이 값을 실제로 어디에 쓰는가 — 명령어 예시 2개

그림도 넣어 달라: 커밋 해시를 만들 때 재료로 들어가는 것들을 상자에 넣고
해시가 튀어나오는 모양의 텍스트 도식으로.
```

**자가 점검**: 커밋 해시가 순번이 아닌 이유를 말할 수 있는가?

---

## 6. 커밋은 어떻게 이어지는가

```
내 저장소에 이런 커밋 목록이 있다 (위가 최신).

  f03d3bc  ② 카드 경계 — 현행 유지로 닫는다
  57ee21a  인계문
  779e102  작업 로그
  fd2e693  링 6 아쉬움 막대
  95d50ac  옅은 파란 면 위 글자색 규칙
  a45da1c  동작 — 뒤로가기·터치 타깃
  26fe5df  공용 ScreenHeader
  a1a5bd7  가독성 — 색 4개 + 글자 40곳
  0b2c535  통계 화면 칩 → 탭바
  d37a654  6개doc_failed

질문: 이 커밋들은 서로 어떻게 연결돼 있는가?

이렇게 답해 달라.
1) "부모 커밋"이란 무엇인가 — 위 목록에서 `a1a5bd7`의 부모는 무엇인가
2) 커밋 하나는 "그때의 변경분"을 담는가, "그때의 파일 전체"를 담는가
3) `d37a654`가 `f03d3bc`의 "조상(ancestor)"이라는 말은 무슨 뜻인가
4) 이 연결 구조를 텍스트 그림으로 그려 달라

4)의 그림은 반드시 그려 달라. 커밋을 네모로, 부모를 가리키는 관계를 화살표로 그리고,
화살표가 어느 쪽을 향하는지(최신 → 과거인지 그 반대인지) 분명히 표시해 달라.
```

**자가 점검**: `a1a5bd7`과 `d37a654` 사이에 무엇이 있는지 말할 수 있는가?

---

# 3부 · 브랜치 — 갈라지는 것

## 7. 브랜치란 무엇인가

```
내 저장소에 `main`, `color_ui`, `ui_chips_suisei`, `feat_wrs` 같은 "브랜치"가 여러 개 있다.
브랜치를 "폴더 복사본"처럼 상상하고 있었는데 맞는지 모르겠다.

질문: 브랜치란 정확히 무엇인가?

이렇게 답해 달라.
1) 브랜치가 실제로 저장하고 있는 것은 무엇인가 — 놀랄 만큼 단순하다고 들었다
2) 브랜치를 100개 만들면 디스크를 100배 쓰는가
3) "폴더 복사본"이라는 내 이해가 어디서 맞고 어디서 틀린가
4) 브랜치를 새로 만드는 명령과, 만들자마자의 상태

그림을 꼭 넣어 달라 — 이게 이 질문의 핵심이다.
커밋 여러 개를 그려 놓고, 브랜치 이름이 그중 무엇을 가리키는지 화살표로 표시해 달라.
브랜치가 「커밋 덩어리」가 아니라 「가리키는 손가락」이라는 게 보이게 그려 달라.
그리고 커밋을 하나 더 하면 그 손가락이 어떻게 움직이는지 전후 두 장으로.
```

**자가 점검**: 브랜치가 파일 복사본이 아니라면 대체 무엇인지 한 문장으로 말할 수 있는가?

---

## 8. 분기(branch off)한다는 것

```
내 상황을 설명한다. `color_ui`라는 브랜치는 `ui_chips_suisei`라는 브랜치에서 "분기했다"고
한다. 그리고 `ui_chips_suisei`는 또 `feat_wrs_voice_guide`에서 분기했다고 한다.

질문: "A에서 분기했다"는 정확히 무슨 뜻인가?

이렇게 답해 달라.
1) 분기하는 시점에 실제로 일어나는 일
2) 분기한 뒤 원래 브랜치(`ui_chips_suisei`)에 새 커밋이 생기면 `color_ui`는 어떻게 되나
3) 반대로 `color_ui`에 커밋을 쌓으면 `ui_chips_suisei`는 어떻게 되나
4) 위 세 브랜치의 관계를 텍스트 그림으로 그려 달라
5) "분기점(merge base)"이라는 말의 뜻

4)의 그림은 반드시 그려 달라. 세 브랜치가 한 줄기에서 갈라져 나오는 모양으로,
각 브랜치 이름이 어느 커밋을 가리키는지와 분기점이 어디인지 표시해 달라.
```

**자가 점검**: 분기한 두 브랜치가 서로 영향을 주는지 아닌지 말할 수 있는가?

---

## 9. HEAD와 체크아웃 — 지금 내가 어디 있나

```
깃에서 브랜치를 "옮겨 다닌다"고 하는데, 옮겨 다닌다는 게 무슨 뜻인지 감이 안 온다.
내 폴더 안의 파일이 실제로 바뀌는 건가?

질문: `git checkout`(또는 `git switch`)으로 브랜치를 바꾸면 무슨 일이 일어나는가?
그리고 "HEAD"란 무엇인가?

이렇게 답해 달라.
1) HEAD가 가리키는 것
2) 브랜치를 바꿀 때 내 폴더의 파일이 실제로 바뀌는가
3) 고치던 파일이 커밋 안 된 상태에서 브랜치를 바꾸면 어떻게 되는가
4) `git checkout -b 새이름`은 무엇을 한꺼번에 하는가

그림을 꼭 넣어 달라 — 이게 이 질문의 핵심이다.
HEAD → 브랜치 → 커밋 으로 이어지는 관계를 그리고,
브랜치를 바꿨을 때 무엇이 움직이는지 전후 두 장으로 보여 달라.
```

**자가 점검**: 「지금 내가 어느 브랜치에 있는가」를 확인하는 명령을 말할 수 있는가?

---

## 10. 왜 커밋을 여러 개로 나누나

```
내 작업에서 이런 일이 있었다. 22개 파일을 한꺼번에 고쳐 놓고, 그걸 하나로 커밋하지 않고
성격별로 3개 커밋으로 나눠 담았다. 이유는 "되돌릴 이유가 서로 다르기 때문"이라고 했다.

  ① 가독성 — 색 4개 + 글자 크기 40곳   → 글자가 화면에서 넘치면 되돌린다
  ② 공용 헤더 컴포넌트 도입             → 헤더 구조를 다시 짤 때 되돌린다
  ③ 뒤로가기·터치 영역 수정              → 버그 수정이라 되돌릴 일이 거의 없다

질문: 이렇게 나누는 게 실제로 어떤 이득을 주는가? 하나로 합쳐 두면 무슨 문제가 생기는가?

이렇게 답해 달라.
1) 위 ①만 되돌리고 싶을 때, 나눠 뒀을 때와 합쳐 뒀을 때 각각 어떻게 되는지
2) 커밋을 나누는 기준으로 실무에서 쓰는 원칙
3) 반대로 너무 잘게 쪼개면 생기는 문제
4) 이미 한 덩어리로 커밋해 버렸다면 나중에 나눌 수 있는가

그림도 넣어 달라: 「셋을 한 커밋에 합쳐 둔 히스토리」와 「셋으로 나눈 히스토리」를
나란히 그리고, ①만 되돌리려 할 때 각각 어떻게 되는지 화살표로 표시해 달라.
```

**자가 점검**: 「되돌릴 이유가 다르면 커밋도 나눈다」가 왜 유용한지 말할 수 있는가?

---

# 4부 · 리모트 — 내 컴퓨터 밖

## 11. 리모트와 `origin`

```
깃 명령어에 `origin`이라는 말이 자꾸 나온다. `origin/main`, `origin/color_ui` 같은 식이다.
내 저장소는 github.com/gimsansan/mmn-amp1 에 있다.

질문: "리모트(remote)"란 무엇이고 `origin`은 무엇인가?

이렇게 답해 달라.
1) `origin`은 깃이 정한 예약어인가, 그냥 붙은 이름인가 — 바꿀 수 있는가
2) 리모트를 2개 이상 둘 수 있는가, 그런 경우가 언제인가
3) `git remote -v`를 치면 나오는 내용을 어떻게 읽는가
4) 리모트 저장소도 내 컴퓨터의 저장소와 같은 구조인가

그림도 넣어 달라: 내 컴퓨터의 저장소와 깃허브의 저장소를 두 상자로 그리고,
`origin`이라는 이름이 정확히 무엇에 붙은 이름인지 표시해 달라.
```

**자가 점검**: `origin`이 특별한 예약어인지 아닌지 말할 수 있는가?

---

## 12. `origin/main`은 브랜치인가

```
내 컴퓨터에는 `main`이라는 브랜치가 있고, 동시에 `origin/main`이라는 것도 있다.
둘이 뭐가 다른지 모르겠다.

질문: `origin/main` 같은 이름은 무엇인가? 내 `main`과 어떻게 다른가?

이렇게 답해 달라.
1) `origin/main`의 정식 명칭과 정체
2) 이것은 "깃허브의 현재 상태"인가, 아니면 "내가 마지막으로 확인한 깃허브의 상태"인가
3) 다른 사람이 깃허브에 커밋을 올리면 내 `origin/main`이 자동으로 따라 바뀌는가
4) `origin/main`에 직접 커밋할 수 있는가

그림을 꼭 넣어 달라 — 이게 이 질문의 핵심이다.
「깃허브의 진짜 main」 / 「내 컴퓨터의 origin/main」 / 「내 컴퓨터의 main」
세 개를 각각 그리고, 셋이 서로 어긋나 있는 상황을 한 장 그려 달라.
```

**자가 점검**: `main`과 `origin/main`이 어긋날 수 있는 이유를 말할 수 있는가?

---

## 13. 푸시란 무엇인가

```
"푸시(push)한다"는 말을 쓰는데, 파일 업로드와 같은 것인지 모르겠다.
나는 방금 `git push -u origin color_ui`라는 명령을 실행했고, 커밋 9개가 올라갔다.

질문: 푸시는 정확히 무엇을 보내는 동작인가?

이렇게 답해 달라.
1) 푸시가 보내는 것 — 파일인가, 커밋인가, 브랜치인가
2) 웹하드에 파일 올리는 것과 무엇이 다른가
3) 푸시가 거부되는(rejected) 대표적인 상황과 그 이유
4) 푸시한 뒤 로컬에서 그 커밋을 지우면 깃허브에서도 지워지는가

그림도 넣어 달라: 푸시 직전과 직후를 두 장으로. 커밋이 어느 쪽으로 옮겨 가고
`origin/색이름` 표시가 어떻게 따라 움직이는지 보이게.
```

**자가 점검**: 푸시가 「파일 업로드」와 어떻게 다른지 말할 수 있는가?

---

## 14. 업스트림(upstream)이란 무엇인가

```
내 브랜치 목록을 보면 이렇게 나온다.

  color_ui           origin/color_ui        동기화됨
  ui_chips_suisei    업스트림 없음
  two_feat           origin/two_feat        behind 5

`ui_chips_suisei`만 "업스트림 없음"이라고 한다.

질문: 업스트림이란 무엇이고, 없으면 무엇이 불편한가?

이렇게 답해 달라.
1) 업스트림의 정체 — 로컬 브랜치와 리모트 브랜치를 잇는 무엇인가
2) 업스트림이 있으면 `git push`, `git pull`을 짧게 쓸 수 있는 이유
3) `git push -u origin 브랜치명`에서 `-u`가 하는 일
4) 업스트림이 없는 브랜치의 커밋은 위험한가
   — 내 `ui_chips_suisei`의 커밋은 `origin/color_ui`에 전부 포함돼 있다고 한다.
     이게 무슨 뜻이고 왜 안전한지 같이 설명해 달라

그림도 넣어 달라: 업스트림이 설정된 브랜치와 안 된 브랜치를 나란히 그리고,
둘을 잇는 연결선이 있고 없고의 차이를 표시해 달라.
```

**자가 점검**: `-u`를 붙일 때와 안 붙일 때의 차이를 말할 수 있는가?

---

## 15. "업스트림이 없어서 밀면 리모트에 새 브랜치가 생긴다"

```
내가 `color_ui`라는 브랜치를 푸시하기 전에 이런 설명을 들었다.

  "color_ui는 업스트림이 없다. 지금 밀면 리모트에 새 브랜치가 생긴다 —
   기존 브랜치를 덮어쓰지 않는다."

이 문장이 정확히 무슨 뜻인지 이해하고 싶다. 특히 "덮어쓰지 않는다"가 왜 굳이
언급할 만한 일인지 모르겠다.

질문: 이 문장을 한 조각씩 풀어서 설명해 달라.

이렇게 답해 달라.
1) 푸시할 때 깃이 "어느 리모트 브랜치에 보낼지" 어떻게 정하는가
2) 리모트에 같은 이름의 브랜치가 이미 있으면 어떻게 되는가
3) 그럼 어떤 경우에 남의 작업을 덮어쓰게 되는가 — `--force`가 여기 나오나?
4) 푸시 전에 "무엇이 올라갈지" 미리 확인하는 방법

그림을 꼭 넣어 달라 — 이게 이 질문의 핵심이다.
「리모트에 같은 이름이 없을 때」와 「이미 있을 때」를 두 장으로 그리고,
각각 푸시하면 리모트가 어떻게 되는지 보여 달라.
덮어쓰기가 실제로 일어나는 경우도 따로 한 장 그려 달라.
```

**자가 점검**: 푸시가 안전한 경우와 위험한 경우를 구분해 말할 수 있는가?

---

## 16. 동기화 · ahead · behind

```
내 브랜치 상태에 이런 표시가 붙는다.

  color_ui     origin/color_ui    동기화됨(in sync)
  two_feat     origin/two_feat    behind 5

"behind 5"가 무슨 뜻인지, 그리고 이게 위험한 상태인지 모르겠다.

질문: ahead / behind / diverged 는 각각 무슨 뜻인가?

이렇게 답해 달라.
1) 무엇과 무엇을 비교한 결과인가
2) ahead 3 / behind 5 / ahead 2 behind 4 는 각각 어떤 상황인지 텍스트 그림으로
3) 각 상황에서 해야 할 일
4) "diverged(갈라졌다)"가 가장 까다로운 이유
5) 내 `two_feat`이 behind 5인 건 내가 뭘 잘못한 건가

2)의 그림은 반드시 그려 달라. 내 브랜치와 리모트 브랜치를 두 줄로 놓고,
ahead / behind / diverged 세 경우에서 커밋이 어떻게 어긋나 있는지 각각 그려 달라.
```

**자가 점검**: behind일 때와 ahead일 때 해야 할 일이 다른 이유를 말할 수 있는가?

---

## 17. fetch와 pull — 푸시의 반대

```
푸시는 내 것을 깃허브로 보내는 것이라고 이해했다. 반대로 가져오는 건 `fetch`와 `pull`
두 가지가 있다고 하는데 왜 두 개인지 모르겠다.

질문: fetch와 pull은 각각 무엇이고 왜 나뉘어 있는가?

이렇게 답해 달라.
1) fetch가 하는 일 — 내 작업 파일이 바뀌는가
2) pull이 하는 일 — fetch에 무엇이 더해진 것인가
3) 어느 쪽을 먼저 쓰는 게 안전한가, 왜인가
4) 고치던 파일이 있는 상태에서 pull하면 어떻게 되는가

그림을 꼭 넣어 달라 — 이게 이 질문의 핵심이다.
「fetch 직후」와 「pull 직후」를 두 장으로 그리고,
내 작업 파일이 있는 칸이 각각 바뀌었는지 아닌지 표시해 달라.
```

**자가 점검**: fetch만 했을 때 내 작업 파일이 바뀌는지 아닌지 말할 수 있는가?

---

# 5부 · 합치기와 되돌리기

## 18. 머지(merge)

```
브랜치를 나눠서 작업했으면 언젠가 다시 합쳐야 할 것 같다. 내 경우 `color_ui`에서
작업한 것을 결국 `main`에 넣어야 한다.

질문: 머지란 무엇이고 어떻게 이뤄지는가?

이렇게 답해 달라.
1) 머지할 때 깃이 실제로 하는 계산 — 여기서 "분기점"이 왜 중요한가
2) "충돌(conflict)"은 언제 생기고, 생기면 무엇을 해야 하는가
3) 머지 커밋이란 무엇인가 — 부모가 둘인 커밋
4) fast-forward 머지는 무엇이 다른가

그림도 넣어 달라: 머지 전과 후의 커밋 그래프를 두 장으로.
분기점 · 양쪽 커밋 · 새로 생긴 머지 커밋을 표시하고,
fast-forward 머지는 모양이 어떻게 다른지 따로 한 장 더.
```

**자가 점검**: 충돌이 「에러」가 아니라 왜 「사람이 정해야 할 일」인지 말할 수 있는가?

---

## 19. PR(풀 리퀘스트)

```
내가 브랜치를 푸시했더니 깃허브가 이런 링크를 줬다.

  Create a pull request for 'color_ui' on GitHub by visiting:
  https://github.com/gimsansan/mmn-amp1/pull/new/color_ui

질문: 풀 리퀘스트(PR)란 무엇인가? 그냥 머지하면 안 되는가?

이렇게 답해 달라.
1) PR이 깃의 기능인가, 깃허브의 기능인가
2) PR이 실제로 하는 일 — 머지와 어떻게 다른가
3) 혼자 하는 프로젝트에서도 PR을 쓸 이유가 있는가
4) PR을 열어 두고 그 브랜치에 커밋을 더 쌓으면 어떻게 되는가

그림도 넣어 달라: 브랜치를 푸시하고 → PR을 열고 → 리뷰하고 → 머지되는 흐름을
순서도로. 각 단계에서 어디(내 컴퓨터 / 깃허브)에서 일이 일어나는지 표시해 달라.
```

**자가 점검**: PR이 깃 자체의 개념이 아니라는 점을 말할 수 있는가?

---

## 20. 되돌리기 — revert · reset · checkout

```
내 작업 기록에 "문제가 생기면 그 커밋만 되돌리면 된다"는 말이 자주 나온다.
그런데 되돌리는 명령이 `revert`, `reset`, `checkout` 여러 개라 뭘 써야 할지 모르겠다.

질문: 이 셋은 각각 무엇을 되돌리는가?

이렇게 답해 달라.
1) 셋의 차이를 한 문장씩
2) 이미 깃허브에 푸시한 커밋을 되돌릴 때 반드시 써야 하는 것과, 절대 쓰면 안 되는 것
3) `reset --soft` / `--mixed` / `--hard`의 차이
4) 각각을 실수로 썼을 때 복구할 수 있는가 — `git reflog`가 여기 나오나?

특히 "푸시하기 전"과 "푸시한 뒤"로 나눠서 설명해 달라.

그림을 꼭 넣어 달라 — 이게 이 질문의 핵심이다.
같은 히스토리에서 출발해 `revert`를 썼을 때와 `reset`을 썼을 때
히스토리가 각각 어떤 모양이 되는지 두 장으로 그려 달라.
`reset`이 왜 「이미 남에게 준 기록을 없애는 일」이 되는지 그림에서 보이게.
```

**자가 점검**: 푸시한 커밋을 되돌릴 때 `reset`이 왜 위험한지 말할 수 있는가?

---

# 6부 · 내 상태를 스스로 읽기

## 21. 상태 확인 명령 정리

```
지금까지 깃의 개념을 배웠다. 이제 내 저장소의 상태를 스스로 확인할 수 있게 되고 싶다.
내가 본 명령 중에 이런 것들이 있었다.

  git status -sb
  git log --oneline
  git log --oneline --branches --not --remotes
  git for-each-ref --format='%(refname:short) %(upstream:short) %(upstream:track)' refs/heads

질문: 위 명령들이 각각 무엇을 보여주는지, 출력을 어떻게 읽는지 알려 달라.

이렇게 답해 달라.
1) 각 명령이 답해 주는 질문을 한 줄씩
2) 출력 예시와 그 해석
3) 세 번째 명령(`--branches --not --remotes`)의 뜻을 특히 자세히 —
   왜 이게 "푸시 안 한 커밋"을 찾아 주는가
4) 매일 작업을 시작할 때와 끝낼 때 각각 쳐 보면 좋은 명령

2)의 출력 예시는 실제 터미널 화면처럼 그대로 보여 주고,
각 줄 옆에 화살표로 주석을 달아 설명해 달라.
```

**자가 점검**: 「푸시 안 한 커밋이 있는가」를 스스로 확인할 수 있는가?

---

## 22. 마무리 — 내 상황 전체를 다시 읽기

```
깃의 개념을 처음부터 배웠다. 이제 배운 것으로 내 실제 상황을 스스로 해석해 보고 싶다.
아래는 내 저장소의 진짜 상태다.

  저장소: github.com/gimsansan/mmn-amp1

  로컬 브랜치            리모트 대비
  color_ui               origin/color_ui        동기화됨
  ui_chips_suisei        업스트림 없음
  feat_wrs_voice_guide   origin/feat_...        동기화됨
  main                   origin/main            동기화됨
  two_feat               origin/two_feat        behind 5

  color_ui 는 ui_chips_suisei 에서 분기했고,
  ui_chips_suisei 는 feat_wrs_voice_guide 에서 분기했다.
  color_ui 에는 커밋 9개가 쌓여 있고 방금 푸시했다.

질문: 이 상태를 처음 보는 사람에게 설명한다면 어떻게 설명하겠는가?

이렇게 답해 달라.
1) 브랜치들의 관계를 텍스트 그림으로
2) `ui_chips_suisei`에 업스트림이 없는데도 그 커밋이 안전한 이유
3) `two_feat`의 behind 5는 무엇을 뜻하며 지금 조치가 필요한가
4) 이 상태에서 `color_ui`를 `main`에 넣으려면 어떤 순서로 해야 하는가
5) 내가 앞으로 조심해야 할 점 3가지

1)의 그림은 반드시 그려 달라. 네 브랜치가 갈라져 나온 모양을 한 장에 그리고,
각 브랜치가 어느 커밋을 가리키는지, 어디까지가 리모트에 올라가 있는지 표시해 달라.
```

**자가 점검**: 이 표를 남에게 설명할 수 있으면 한 바퀴 끝난 것이다.

---

# 부록 · 막혔을 때 쓰는 프롬프트

배우다 막히면 아래를 쓴다. `○○`만 바꿔 넣으면 된다.

## A. 한 개념이 이해가 안 될 때

```
깃을 배우는 중인데 「○○」라는 말이 이해가 안 된다.
1) 초등학생에게 설명하듯 한 번
2) 정확한 정의로 한 번
3) 내가 실제로 그 개념을 만나게 되는 상황 예시 하나
이렇게 세 번 설명해 달라.
```

## B. 두 개념이 헷갈릴 때

```
깃에서 「○○」와 「△△」가 헷갈린다. 둘을 나란히 비교해 달라.
표로 정리하고, 각각을 써야 하는 상황을 한 줄씩 달아 달라.
마지막에 "이것만 기억하면 안 헷갈린다"는 한 문장을 달아 달라.
```

## C. 지금 내 상태가 뭔지 모를 때

```
내가 지금 이런 상황이다: (상황을 쓴다)
`git status`를 치면 이렇게 나온다: (붙여넣는다)

이 상태가 무엇을 뜻하는지 설명하고, 내가 다음에 무엇을 해야 하는지 알려 달라.
명령을 알려줄 때는 그 명령이 무엇을 바꾸는지, 되돌릴 수 있는지도 같이 말해 달라.
```

## D. 그림이 부실할 때 — 그림만 다시 받기

```
방금 설명은 이해했는데 그림이 없거나 너무 간단해서 머리에 안 들어온다.
설명은 빼고 **그림만** 다시 그려 달라.

1) 텍스트 도식(ASCII)으로, 상자와 화살표를 써서
2) 상태가 변하는 개념이면 「전」과 「후」 두 장으로
3) 그림 안의 각 요소가 무엇인지 옆에 짧은 이름표를 달아서
4) 마지막에 "이 그림에서 딱 하나만 기억한다면" 한 문장

폭은 60자 안쪽으로 맞춰 달라. 너무 넓으면 화면에서 깨진다.
```

## E. 겁나서 명령을 못 치겠을 때

```
「(명령어)」를 실행하려는데 겁이 난다.
1) 이 명령이 정확히 무엇을 바꾸는가
2) 되돌릴 수 있는가 — 되돌리는 방법
3) 실행하기 전에 확인해야 할 것
4) 더 안전한 대안이 있는가
```
